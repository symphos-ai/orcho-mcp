"""orcho_mcp.services — shared read-layer over orcho-core SDK.

Sibling modules (``run_lookup``, ``read_queries``) host the query
functions used by both ``orcho_mcp.tools`` (MCP tool adapters) and
``orcho_mcp.resources`` (MCP resource adapters). Keep this ``__init__``
empty: import full paths so each symbol has exactly one canonical home
and grep stays useful.
"""
