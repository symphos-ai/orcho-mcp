{
  "prompts": [
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'formats/bullets' prompt template. Resolves project → workspace → core.",
      "name": "formats/bullets"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'formats/compact' prompt template. Resolves project → workspace → core.",
      "name": "formats/compact"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'formats/detailed' prompt template. Resolves project → workspace → core.",
      "name": "formats/detailed"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'formats/handoff' prompt template. Resolves project → workspace → core.",
      "name": "formats/handoff"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'formats/terse' prompt template. Resolves project → workspace → core.",
      "name": "formats/terse"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "parent_run_id",
          "required": true
        },
        {
          "description": "",
          "name": "profile",
          "required": false
        }
      ],
      "description": "Spawn an implementation run that inherits the parsed plan from a parent run (--from-run-plan). Use this when you already have a paused or completed planning-profile run and want to move it to implementation.",
      "name": "orcho_followup_from_plan"
    },
    {
      "arguments": [],
      "description": "First-contact walkthrough: how to run your first Orcho job through MCP. Covers workspace check, profile choice, starting a run, watching progress, the QA gate flow (inspect findings → decide → resume), inspecting final evidence/metrics/history, and verifying changes in your project.",
      "name": "orcho_getting_started"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "run_id",
          "required": true
        },
        {
          "description": "",
          "name": "reason",
          "required": true
        }
      ],
      "description": "Terminate a run via phase-handoff halt with a structured audit-trail note explaining the operator's reason. Use this when a paused run should NOT proceed — e.g. the task scope changed, the plan is fundamentally wrong, or the run hit an unrecoverable defect.",
      "name": "orcho_halt_with_reason"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "run_id",
          "required": true
        }
      ],
      "description": "Decide what to do with a stopped Orcho-managed run by inspecting its post-release delivery / correction gate (orcho_delivery_gate), then resolving the decision with the gate's ready orcho_delivery_decide calls. Use this when a run stopped after release and you must choose approve / apply / fix / skip / halt — it keeps you from applying the retained worktree diff by hand or confusing an Orcho-managed gate with a plain direct checkout edit.",
      "name": "orcho_inspect_delivery_gate"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "run_id",
          "required": true
        }
      ],
      "description": "Follow an in-flight run with a resilient observation loop: a short bounded orcho_run_watch, then orcho_run_events_summary as the reconnect fallback when the watch times out or the client transport drops the long-poll, then watch again from the reconnect cursor. Use this when you want live progress on a running run without treating a dropped watch as a failed run. If you have lost the run id (watcher/session dropped), recover it first with orcho_workspace_pending_decisions, whose default view is the operator inbox of actionable paused runs (noise hidden but counted in hidden_*); pass include_stale=true for a forensic view of every paused run with per-row classification.",
      "name": "orcho_observe_active_run"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "task",
          "required": true
        },
        {
          "description": "",
          "name": "project_dir",
          "required": true
        }
      ],
      "description": "Two-step workflow: spawn a planning-profile run, wait for it to pause at the planning handoff, inspect the plan, then spawn a child run with --from-run-plan that uses the feature profile for actual implementation. Use this when you have a task and want explicit operator review of the plan before agents touch code.",
      "name": "orcho_plan_then_implement"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "run_id",
          "required": true
        }
      ],
      "description": "Continue an interrupted or failed run from its last checkpoint via --resume. Classifies the parent run's state first (terminal-success runs can't checkpoint-resume; halted-by-handoff runs can't resume at all and need --from-run-plan instead).",
      "name": "orcho_resume_failed_run"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "run_id",
          "required": true
        }
      ],
      "description": "Inspect a run that is paused on a phase handoff (typically validate_plan rejected or human-feedback policy fired) and propose a decide action: continue, retry_feedback, continue_with_waiver, or halt. Use this when `orcho_run_status` returns status = awaiting_phase_handoff and you want a structured review before committing to a decision.",
      "name": "orcho_review_paused_run"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/code_reviewer' prompt template. Resolves project → workspace → core.",
      "name": "roles/code_reviewer"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/implementation_engineer' prompt template. Resolves project → workspace → core.",
      "name": "roles/implementation_engineer"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/plan_reviewer' prompt template. Resolves project → workspace → core.",
      "name": "roles/plan_reviewer"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/product_owner' prompt template. Resolves project → workspace → core.",
      "name": "roles/product_owner"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/release_manager' prompt template. Resolves project → workspace → core.",
      "name": "roles/release_manager"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'roles/systems_architect' prompt template. Resolves project → workspace → core.",
      "name": "roles/systems_architect"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/code_review' prompt template. Resolves project → workspace → core.",
      "name": "tasks/code_review"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/commit_message' prompt template. Resolves project → workspace → core.",
      "name": "tasks/commit_message"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/correction_triage' prompt template. Resolves project → workspace → core.",
      "name": "tasks/correction_triage"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/cross_contract_bundle' prompt template. Resolves project → workspace → core.",
      "name": "tasks/cross_contract_bundle"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/cross_final_acceptance' prompt template. Resolves project → workspace → core.",
      "name": "tasks/cross_final_acceptance"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/cross_plan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/cross_plan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/cross_replan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/cross_replan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/cross_validate_plan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/cross_validate_plan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/decompose' prompt template. Resolves project → workspace → core.",
      "name": "tasks/decompose"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/final_acceptance' prompt template. Resolves project → workspace → core.",
      "name": "tasks/final_acceptance"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/handoff_advice' prompt template. Resolves project → workspace → core.",
      "name": "tasks/handoff_advice"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/hypothesis' prompt template. Resolves project → workspace → core.",
      "name": "tasks/hypothesis"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/implement' prompt template. Resolves project → workspace → core.",
      "name": "tasks/implement"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/plan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/plan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/readonly_plan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/readonly_plan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/repair_changes' prompt template. Resolves project → workspace → core.",
      "name": "tasks/repair_changes"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/replan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/replan"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/review_uncommitted' prompt template. Resolves project → workspace → core.",
      "name": "tasks/review_uncommitted"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/validate_hypothesis' prompt template. Resolves project → workspace → core.",
      "name": "tasks/validate_hypothesis"
    },
    {
      "arguments": [
        {
          "description": "",
          "name": "project_dir",
          "required": false
        }
      ],
      "description": "Orcho 'tasks/validate_plan' prompt template. Resolves project → workspace → core.",
      "name": "tasks/validate_plan"
    }
  ],
  "resourceTemplates": [
    {
      "description": "A single pipeline profile by name.",
      "mimeType": "application/json",
      "name": "orcho_profile",
      "uriTemplate": "orcho://profiles/{name}"
    },
    {
      "description": "Specialist skill registry under <project>/.agent/multiagent/skills/. Project path is URL-safe base64 (see encode_project_dir).",
      "mimeType": "application/json",
      "name": "orcho_project_skills",
      "uriTemplate": "orcho://projects/{project_b64}/skills"
    },
    {
      "description": "Captured diff.patch artifact for a run.",
      "mimeType": "text/x-patch",
      "name": "orcho_run_diff_patch",
      "uriTemplate": "orcho://runs/{run_id}/diff.patch"
    },
    {
      "description": "Full events.jsonl stream for a run (newline-delimited JSON).",
      "mimeType": "application/x-ndjson",
      "name": "orcho_run_events",
      "uriTemplate": "orcho://runs/{run_id}/events"
    },
    {
      "description": "Composed evidence bundle body for a run.",
      "mimeType": "application/json",
      "name": "orcho_run_evidence_bundle",
      "uriTemplate": "orcho://runs/{run_id}/evidence"
    },
    {
      "description": "meta.json for a single run.",
      "mimeType": "application/json",
      "name": "orcho_run_meta",
      "uriTemplate": "orcho://runs/{run_id}/meta"
    },
    {
      "description": "metrics.json for a single run (token counts, durations, per-phase).",
      "mimeType": "application/json",
      "name": "orcho_run_metrics",
      "uriTemplate": "orcho://runs/{run_id}/metrics"
    },
    {
      "description": "Durable parsed_plan.json artifact for a run.",
      "mimeType": "application/json",
      "name": "orcho_run_parsed_plan",
      "uriTemplate": "orcho://runs/{run_id}/parsed_plan.json"
    },
    {
      "description": "Captured per-phase diff.patch artifact for a run phase.",
      "mimeType": "text/x-patch",
      "name": "orcho_run_phase_diff_patch",
      "uriTemplate": "orcho://runs/{run_id}/phases/{phase}/diff.patch"
    },
    {
      "description": "Latest bounded event summary for a run.",
      "mimeType": "application/json",
      "name": "orcho_run_events_summary",
      "uriTemplate": "orcho://runs/{run_id}/summary"
    }
  ],
  "resources": [
    {
      "description": "Same content as the orcho_getting_started prompt, addressable as a resource for clients that prefer URI-based discovery.",
      "mimeType": "text/markdown",
      "name": "orcho_getting_started_doc",
      "uri": "orcho://docs/getting-started"
    },
    {
      "description": "Catalogue of pipeline profiles keyed by semantic work-kind (feature, small_task, planning, code_review, …) plus any custom/plugin profiles. Engine-internal profiles are included but flagged internal=true.",
      "mimeType": "application/json",
      "name": "orcho_profiles",
      "uri": "orcho://profiles"
    },
    {
      "description": "Recent pipeline runs, newest first.",
      "mimeType": "application/json",
      "name": "orcho_runs",
      "uri": "orcho://runs"
    },
    {
      "description": "Machine-readable catalogue of workflow recipes (plan-then-implement, review-paused-run, resume-failed-run, inspect-terminal-run).",
      "mimeType": "application/json",
      "name": "orcho_workflows",
      "uri": "orcho://workflows"
    },
    {
      "description": "Resolved workspace dir, runs dir, and recent project paths.",
      "mimeType": "application/json",
      "name": "orcho_workspace",
      "uri": "orcho://workspace"
    }
  ],
  "tools": [
    {
      "description": "Resolve a parked post-release delivery / correction gate.\n\n    Pure state transition through the orcho-core SDK — never spawns a\n    process and never applies a patch in MCP. Use ``orcho_delivery_gate`` first\n    to inspect the gate and read its ``available_actions`` / ``blocked_actions``.\n\n    Actions:\n      - ``approve``: commit the retained worktree diff into the target checkout.\n      - ``apply``: apply the retained diff without committing it.\n      - ``fix``: mark the run correction-ready after a rejected release verdict.\n      - ``skip``: close the gate without changing the target checkout.\n      - ``halt``: leave the retained worktree in place for manual inspection.\n\n    The return value mirrors core's typed decision result. Refused business\n    decisions are returned as ``accepted=False`` with a ``blocker`` such as\n    ``no_pending_delivery_gate``, ``release_blocked``, or\n    ``verification_blocked``; missing runs / bad arguments use MCP errors.\n    ",
      "inputSchema": {
        "properties": {
          "action": {
            "enum": [
              "approve",
              "apply",
              "fix",
              "skip",
              "halt"
            ],
            "title": "Action",
            "type": "string"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Note"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "action"
        ],
        "title": "orcho_delivery_decideArguments",
        "type": "object"
      },
      "name": "orcho_delivery_decide",
      "outputSchema": {
        "description": "Outcome of ``orcho_delivery_decide``.\n\nMirrors orcho-core's ``DeliveryDecisionResult`` field-for-field. A\nsuccessful decision has ``accepted=True`` and applies the requested\ndelivery action. A refused decision is still typed data, not a transport\nerror: ``accepted=False`` and ``blocker`` names the current guard\n(for example ``no_pending_delivery_gate`` / ``release_blocked`` /\n``verification_blocked``).\n\n``terminal_outcome`` is deliberately narrow: only ``\"done\"`` or\n``\"halted\"``. A correction request is expressed through\n``status=\"fix_requested\"`` + ``halt_reason=\"commit_decision_fix\"`` and\nan optional ``followup_run_id``.",
        "properties": {
          "accepted": {
            "title": "Accepted",
            "type": "boolean"
          },
          "action": {
            "title": "Action",
            "type": "string"
          },
          "artifact_paths": {
            "items": {
              "type": "string"
            },
            "title": "Artifact Paths",
            "type": "array"
          },
          "blocker": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Blocker"
          },
          "commit_sha": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Commit Sha"
          },
          "followup_run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Followup Run Id"
          },
          "halt_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Halt Reason"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "scope_disclosure": {
            "description": "Per-alias sibling files (``[alias]/rel/path``) implicated when shipping was refused with ``blocker='delivery_scope_violation'``. Empty for every non-scope outcome.",
            "items": {
              "type": "string"
            },
            "title": "Scope Disclosure",
            "type": "array"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "terminal_outcome": {
            "enum": [
              "done",
              "halted"
            ],
            "title": "Terminal Outcome",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "action",
          "accepted",
          "status",
          "terminal_outcome"
        ],
        "title": "DeliveryDecideResult",
        "type": "object"
      }
    },
    {
      "description": "Inspect an Orcho-managed run's post-release delivery / correction gate.\n\n    Read-only typed projection that tells an agent, WITHOUT parsing terminal\n    prose, whether a run is sitting at an Orcho-managed delivery decision or\n    is just a direct checkout edit. The classification (``kind``), available\n    actions, blocked actions, and default action are derived from\n    orcho-core's SDK ``delivery_decision_state``:\n\n      - ``delivery_decision_required`` — a pending delivery on an APPROVED\n        release; core may allow approve / apply / skip / halt\n        (``approve`` is the only action that creates a commit).\n      - ``correction_decision_required`` — a rejected release or a\n        ``fix_requested`` state; core normally allows fix / halt for a current\n        rejected release and lists any refused actions in\n        ``blocked_actions``. This is an available correction-flow state, NOT\n        an executed delivery.\n      - ``direct_checkout_or_running`` — no pending commit-delivery gate\n        (terminal delivery, a direct checkout edit, or a still-running run);\n        no approve / apply / fix is offered.\n\n    ``diff`` summarises the retained change (``files_changed`` +\n    ``changed_paths`` + ``untracked_paths``). If a secondary artifact\n    (``commit_decisions`` / ``diff.patch``) is missing or unreadable on a\n    pending gate, ``diff.degraded`` is ``True`` and ``message`` names the\n    missing artifact — the gate ``kind`` is never hidden by that.\n\n    ``next_actions`` carries one ``ready_call`` to ``orcho_delivery_decide``\n    per available action. Each record contains the full required args for the\n    selected decision call. MCP itself never applies the retained diff; the\n    decision tool delegates to orcho-core's SDK.\n\n    Raises RunNotFoundError when ``run_id`` doesn't exist on disk; a missing\n    or corrupt ``meta.json`` yields a ``direct_checkout_or_running``\n    projection rather than an error.\n    ",
      "inputSchema": {
        "properties": {
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_delivery_gateArguments",
        "type": "object"
      },
      "name": "orcho_delivery_gate",
      "outputSchema": {
        "$defs": {
          "DeliveryActionRecord": {
            "description": "One delivery / correction action the operator may take at the gate.\n\n``creates_commit`` is the load-bearing flag: only ``approve`` writes a\nnew commit to the target checkout. ``apply`` lands the diff uncommitted;\n``fix`` / ``skip`` / ``halt`` never commit. ``effect`` is a one-line\nhuman-readable description of what choosing the action does.",
            "properties": {
              "action": {
                "title": "Action",
                "type": "string"
              },
              "creates_commit": {
                "default": false,
                "title": "Creates Commit",
                "type": "boolean"
              },
              "effect": {
                "title": "Effect",
                "type": "string"
              }
            },
            "required": [
              "action",
              "effect"
            ],
            "title": "DeliveryActionRecord",
            "type": "object"
          },
          "DeliveryGateDiffSummary": {
            "description": "Diff summary for an Orcho-managed delivery / correction gate.\n\nBuilt first from the authoritative ``meta`` commit-delivery decision\n(``changed_paths`` / ``untracked_paths``); the durable\n``commit_decisions`` artifact and ``diff.patch`` only enrich it.\n``degraded`` is ``True`` when one of those *secondary* artifacts is\nmissing or unreadable — the gate ``kind`` is never affected by that,\nonly this summary's completeness (the projection message names the\nmissing artifact).",
            "properties": {
              "changed_paths": {
                "items": {
                  "type": "string"
                },
                "title": "Changed Paths",
                "type": "array"
              },
              "degraded": {
                "default": false,
                "description": "True when a secondary artifact (commit_decisions / diff.patch) was missing or unreadable, so this summary is partial. The gate kind is unaffected — it is derived only from the authoritative meta commit-delivery status.",
                "title": "Degraded",
                "type": "boolean"
              },
              "files_changed": {
                "default": 0,
                "title": "Files Changed",
                "type": "integer"
              },
              "untracked_paths": {
                "items": {
                  "type": "string"
                },
                "title": "Untracked Paths",
                "type": "array"
              }
            },
            "title": "DeliveryGateDiffSummary",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          }
        },
        "description": "Typed projection of an Orcho-managed post-release delivery gate.\n\n``kind`` is the single classification signal and is derived ONLY from\nthe authoritative ``meta`` commit-delivery ``status`` (never from\nterminal log prose):\n\n  - ``delivery_decision_required`` — a ``pending`` delivery on an\n    APPROVED release; the operator chooses approve / apply / skip / halt.\n  - ``correction_decision_required`` — a rejected release or a\n    ``fix_requested`` state; the operator normally chooses fix / halt for a\n    current rejected release. This is an *available correction-flow state*,\n    NOT an executed delivery.\n  - ``direct_checkout_or_running`` — no pending commit-delivery gate in\n    meta (terminal delivery, or a direct checkout edit / still-running\n    run); no approve / apply / fix is offered.\n\n``available_actions`` comes from orcho-core's read-only\n``delivery_decision_state`` surface and lists only actions the SDK says are\ncurrently safe. ``blocked_actions`` names delivery actions currently\nrefused by hard guards such as a rejected release verdict or incomplete\nrequired verification. ``next_actions`` carries one ``ready_call`` to\n``orcho_delivery_decide`` per available action.",
        "properties": {
          "available_actions": {
            "items": {
              "$ref": "#/$defs/DeliveryActionRecord"
            },
            "title": "Available Actions",
            "type": "array"
          },
          "blocked_actions": {
            "description": "Actions core currently refuses for this gate, usually shipping actions blocked by release, required-verification, or delivery-scope guards.",
            "items": {
              "type": "string"
            },
            "title": "Blocked Actions",
            "type": "array"
          },
          "default_action": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The resolved default action from meta (e.g. approve / fix).",
            "title": "Default Action"
          },
          "diff": {
            "$ref": "#/$defs/DeliveryGateDiffSummary"
          },
          "kind": {
            "enum": [
              "delivery_decision_required",
              "correction_decision_required",
              "direct_checkout_or_running"
            ],
            "title": "Kind",
            "type": "string"
          },
          "message": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Message"
          },
          "next_actions": {
            "items": {
              "$ref": "#/$defs/NextActionRecord"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "release": {
            "default": "none",
            "description": "Release verdict outcome from meta ``release_verdict``.",
            "enum": [
              "approved",
              "rejected",
              "none"
            ],
            "title": "Release",
            "type": "string"
          },
          "retained_worktree": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The retained source worktree holding the change.",
            "title": "Retained Worktree"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "scope_blocker": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Set to ``delivery_scope_violation`` when shipping is refused because the run resolved under strict mono but sibling-repo changes were found outside its delivery scope. ``None`` for any gate not blocked by a delivery-scope violation.",
            "title": "Scope Blocker"
          },
          "scope_disclosure": {
            "description": "Per-alias sibling files (``[alias]/rel/path``) implicated by a delivery-scope decision — the concrete changes behind a ``delivery_scope_violation`` block. Empty for a gate with no delivery-scope dimension.",
            "items": {
              "type": "string"
            },
            "title": "Scope Disclosure",
            "type": "array"
          },
          "target_checkout": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The checkout a delivery would land in (run project path).",
            "title": "Target Checkout"
          }
        },
        "required": [
          "run_id",
          "kind"
        ],
        "title": "DeliveryGateProjection",
        "type": "object"
      }
    },
    {
      "description": "Ask the read-only advisor for a recommendation on a paused phase handoff.\n\n    For a run paused at ``status=awaiting_phase_handoff`` on a rejected /\n    incomplete verdict, this runs orcho-core's one-shot read-only advisor and\n    returns its typed recommendation: ``recommended_action`` (``continue`` /\n    ``retry_feedback`` / ``halt`` / ``continue_with_waiver``), ``confidence``,\n    ``rationale``, ``retry_feedback`` text, ``risks``, ``expected_files``,\n    ``operator_note``, ``parse_warnings``, a ``safety`` classification, the\n    durable ``advice_artifact`` path, the ``provenance_note``, and a\n    deterministic ``ready_next_action``.\n\n    Read-only by contract: the advisor writes exactly ONE durable artifact (the\n    advice record) and NEVER records a phase-handoff decision, flips\n    ``meta.status``, or auto-applies anything. This tool only recommends.\n\n    ``ready_next_action`` is a pre-filled suggestion you MAY forward — it is NOT\n    applied here:\n      - ``recommended_action == 'retry_feedback'`` → a ``ready_call`` to the\n        EXISTING ``orcho_phase_handoff_decide`` with ``action='retry_feedback'``,\n        ``feedback`` set to the advisor's retry text, and a MANDATORY\n        ``note=<provenance_note>`` linking the decision back to the advice\n        artifact. No new verb is introduced; forwarding it (and only then) records\n        the retry.\n      - ``continue`` / ``halt`` → a ``ready_call`` mirroring that verb;\n        ``continue_with_waiver`` → ``operator_input_required`` (you must supply\n        the waiver verdict as ``feedback``).\n\n    ``handoff_id`` is optional — when omitted, the run's active handoff is used;\n    when supplied it must match the active handoff id.\n\n    Errors:\n      - RunNotFoundError — unknown run_id.\n      - WorkspaceNotResolvedError — no workspace could be resolved.\n      - InvalidPlanError — no active handoff, a mismatched ``handoff_id``, a run\n        not paused on a decidable handoff, or a handoff ineligible for advice\n        (wrong trigger/verdict, no ``retry_feedback`` offered, no finding /\n        last output).\n    ",
      "inputSchema": {
        "properties": {
          "handoff_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Handoff Id"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_handoff_adviceArguments",
        "type": "object"
      },
      "name": "orcho_handoff_advice",
      "outputSchema": {
        "$defs": {
          "HandoffAdviceSafetyRecord": {
            "description": "Safety classification of an advisory recommendation.\n\nProjected verbatim from orcho-core's ``classify_advice_safety`` (via the SDK\n``request_handoff_advice`` accessor): ``auto_apply_ok`` is True only for a\nnon-low-confidence ``retry_feedback`` (the sole auto-appliable advisory\naction); ``needs_confirmation`` flags low confidence; ``blocked_reason`` is\nset for any non-retry recommendation; ``waiver_blocked`` marks a render-only\nwaiver when blocking-severity findings exist. This is advisory metadata — the\n``orcho_handoff_advice`` tool never applies the recommendation.",
            "properties": {
              "auto_apply_ok": {
                "default": false,
                "title": "Auto Apply Ok",
                "type": "boolean"
              },
              "blocked_reason": {
                "default": "",
                "title": "Blocked Reason",
                "type": "string"
              },
              "needs_confirmation": {
                "default": false,
                "title": "Needs Confirmation",
                "type": "boolean"
              },
              "waiver_blocked": {
                "default": false,
                "title": "Waiver Blocked",
                "type": "boolean"
              }
            },
            "title": "HandoffAdviceSafetyRecord",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_handoff_advice``.\n\nA read-only advisory recommendation for a run paused at\n``status=awaiting_phase_handoff`` on a rejected/incomplete verdict. The\nadvisor is a one-shot read-only LLM pass (orcho-core's\n``request_handoff_advice``): it writes exactly ONE durable artifact — the\nadvice record at ``advice_artifact`` — and NEVER records a phase-handoff\ndecision, flips ``meta.status``, or auto-applies anything. The tool only\nrecommends.\n\n``ready_next_action`` is the deterministic, pre-filled follow-up the operator\nMAY forward — it is a suggestion, not an applied decision:\n\n- ``recommended_action == 'retry_feedback'`` → a ``ready_call`` to the\n  EXISTING ``orcho_phase_handoff_decide`` with\n  ``args = {run_id, handoff_id, action='retry_feedback', feedback=<advice\n  retry_feedback>, note=<provenance_note>}``. The ``note`` carries provenance\n  back to ``advice_artifact`` so an applied retry is auditable. This reuses\n  the existing decision verb — there is no new runtime verb and the tool does\n  not call decide itself.\n- ``continue`` / ``halt`` → a ``ready_call`` mirroring that verb (with the\n  provenance ``note``); ``continue_with_waiver`` →\n  ``operator_input_required`` because the operator must supply the waiver\n  verdict as ``feedback`` (the advisor's retry text is not a waiver).\n\n``provenance_note`` / ``advice_artifact`` are empty only when the advisor\nresponse was unparseable (no durable advice write — handled like the existing\nadvisory paths, never auto-applied).",
        "properties": {
          "advice_artifact": {
            "default": "",
            "description": "Run-relative path of the durable advice artifact (``phase_handoff_advice/<id>.json``). Empty when the advisor response was unparseable.",
            "title": "Advice Artifact",
            "type": "string"
          },
          "confidence": {
            "description": "Advisor confidence: ``high`` / ``medium`` / ``low``.",
            "title": "Confidence",
            "type": "string"
          },
          "expected_files": {
            "items": {
              "type": "string"
            },
            "title": "Expected Files",
            "type": "array"
          },
          "handoff_id": {
            "title": "Handoff Id",
            "type": "string"
          },
          "operator_note": {
            "default": "",
            "title": "Operator Note",
            "type": "string"
          },
          "parse_warnings": {
            "items": {
              "type": "string"
            },
            "title": "Parse Warnings",
            "type": "array"
          },
          "phase": {
            "title": "Phase",
            "type": "string"
          },
          "provenance_note": {
            "default": "",
            "description": "The ``feedback_source=...; advice_artifact=...`` note an applied ``retry_feedback`` decision must carry. Empty when the advisor response was unparseable.",
            "title": "Provenance Note",
            "type": "string"
          },
          "rationale": {
            "title": "Rationale",
            "type": "string"
          },
          "ready_next_action": {
            "anyOf": [
              {
                "$ref": "#/$defs/NextActionRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Deterministic pre-filled follow-up. For ``retry_feedback`` a ``ready_call`` to ``orcho_phase_handoff_decide`` carrying the mandatory provenance ``note``; for non-retry recommendations it reflects the recommendation without auto-applying it."
          },
          "recommended_action": {
            "description": "One of ``continue`` / ``retry_feedback`` / ``halt`` / ``continue_with_waiver`` — the advisor's recommended path.",
            "title": "Recommended Action",
            "type": "string"
          },
          "retry_feedback": {
            "description": "The corrective feedback a retry round should act on. Non-empty only for a ``retry_feedback`` recommendation.",
            "title": "Retry Feedback",
            "type": "string"
          },
          "risks": {
            "items": {
              "type": "string"
            },
            "title": "Risks",
            "type": "array"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "safety": {
            "$ref": "#/$defs/HandoffAdviceSafetyRecord"
          },
          "usage": {
            "additionalProperties": true,
            "description": "Advisor invocation usage (tokens / duration / cost when the provider supplied accounting).",
            "title": "Usage",
            "type": "object"
          }
        },
        "required": [
          "run_id",
          "handoff_id",
          "phase",
          "recommended_action",
          "confidence",
          "rationale",
          "retry_feedback",
          "safety"
        ],
        "title": "HandoffAdviceResult",
        "type": "object"
      }
    },
    {
      "description": "Resolve a pipeline paused at ``status=awaiting_phase_handoff``.\n\n    Pure state transition — never spawns a process. The flow:\n\n      1. The pipeline pauses because a phase's declared ``handoff``\n         policy fired (e.g. ``human_feedback_on_reject`` on\n         ``validate_plan`` after the plan loop exhausted its budget).\n         ``meta.status`` becomes ``awaiting_phase_handoff`` and\n         ``meta.phase_handoff`` carries the active payload, including\n         ``id`` (the ``handoff_id``) and ``available_actions``.\n      2. Caller reads the active payload via ``orcho_run_status`` and\n         decides: ``continue`` (manual override), ``retry_feedback``\n         (one extra human-directed plan round; requires ``feedback``),\n         ``continue_with_waiver`` (accept the rejected verdict and\n         proceed, recording a durable operator waiver; requires\n         ``feedback``), or ``halt`` (terminate the run). ``note`` is\n         optional audit. If a client advertises MCP form elicitation\n         and calls ``retry_feedback`` or ``continue_with_waiver``\n         without ``feedback``, Orcho requests the missing feedback\n         natively. Clients without elicitation support ask the user in\n         chat and pass ``feedback`` explicitly.\n      3. The decision is persisted to\n         ``<run_dir>/phase_handoff_decisions/{safe_handoff_id}.json``.\n         For ``halt``, ``meta.status`` is flipped to ``halted``\n         synchronously and ``meta.phase_handoff`` is cleared.\n      4. ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` do\n         not advance the run on their own. Follow up with\n         ``orcho_run_resume`` to actually resume execution. ``halt`` is\n         terminal — no resume.\n\n    Decisions are exact-payload idempotent: the same\n    ``(handoff_id, action, feedback, note)`` may be replayed and\n    returns the persisted record unchanged. Any field divergence for\n    the same ``handoff_id`` is a conflict, not a silent overwrite.\n\n    Args:\n        run_id: the paused run.\n        handoff_id: id from the active ``meta.phase_handoff.id``\n            (e.g. ``\"validate_plan:plan_round:2\"``).\n        action: ``\"continue\"``, ``\"retry_feedback\"``,\n            ``\"continue_with_waiver\"``, or ``\"halt\"``. Must be in the\n            active handoff's ``available_actions``.\n        feedback: human direction injected into the next round (or the\n            waiver text). Required for ``retry_feedback`` and\n            ``continue_with_waiver``; rejected for ``continue`` / ``halt``.\n        note: free-form audit text.\n\n    A run NOT started by this MCP server (no durable ``mcp_supervisor.json``\n    with a resolvable ``project_dir`` — ``control='inspect_only'`` on\n    ``orcho_run_diagnose``) is refused before any SDK call or decision-artifact\n    write by raising ``InspectOnlyControlError`` — it does NOT widen this tool's\n    success ``outputSchema``. MCP can only inspect such a foreign / CLI-started\n    run; manage it through the CLI that started it and inspect it from MCP via\n    ``orcho_run_status`` / ``orcho_run_evidence``. Read the typed ``control`` /\n    ``control_reason`` classification first with ``orcho_run_diagnose``.\n\n    Errors:\n      - InspectOnlyControlError — the run is ``inspect_only`` (not started by\n        this MCP server); refused before any SDK call or decision-artifact\n        write. The carried ``result`` holds the typed read-only next actions.\n      - RunNotFoundError — unknown run_id.\n      - InvalidPlanError — ``action`` not canonical, ``retry_feedback``\n        or ``continue_with_waiver`` without feedback, ``handoff_id``\n        mismatch, run not in ``awaiting_phase_handoff``, or a different\n        decision already recorded for the same ``handoff_id``.\n    ",
      "inputSchema": {
        "properties": {
          "action": {
            "title": "Action",
            "type": "string"
          },
          "feedback": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Feedback"
          },
          "handoff_id": {
            "title": "Handoff Id",
            "type": "string"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Note"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "handoff_id",
          "action"
        ],
        "title": "orcho_phase_handoff_decideArguments",
        "type": "object"
      },
      "name": "orcho_phase_handoff_decide",
      "outputSchema": {
        "$defs": {
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          }
        },
        "description": "Outcome of ``orcho_phase_handoff_decide``.\n\nA phase-handoff decision is a pure state transition — it writes\nthe decision artifact under\n``<run_dir>/phase_handoff_decisions/{safe_handoff_id}.json`` and\n(for ``action=\"halt\"``) flips ``meta.status`` to ``halted``\nsynchronously. ``action=\"continue\"``, ``action=\"retry_feedback\"``,\nand ``action=\"continue_with_waiver\"`` do **not** spawn a process;\nthe caller follows up with ``orcho_run_resume`` to actually continue\nexecution.\n\nFields mirror the persisted decision artifact one-to-one so the\ncaller can confirm exactly what was recorded.",
        "properties": {
          "action": {
            "title": "Action",
            "type": "string"
          },
          "decided_at": {
            "description": "ISO 8601 UTC timestamp when the decision was recorded.",
            "title": "Decided At",
            "type": "string"
          },
          "feedback": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Feedback"
          },
          "handoff_id": {
            "title": "Handoff Id",
            "type": "string"
          },
          "next_actions": {
            "description": "Suggested follow-up tool calls. For ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` actions this carries a single non-optional ``orcho_run_resume`` action — the decision API only writes the artifact; the run advances via resume. For ``halt`` the suggestions are derived from the post-halt run state.",
            "items": {
              "$ref": "#/$defs/NextActionRecord"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Note"
          },
          "phase": {
            "title": "Phase",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "handoff_id",
          "phase",
          "action",
          "decided_at"
        ],
        "title": "PhaseHandoffDecideResult",
        "type": "object"
      }
    },
    {
      "description": "Validate an architect plan document. Provide exactly one of ``markdown`` or ``path``.\n\n    Wraps ``pipeline.plan_parser.parse_plan`` — same semantics as the\n    pipeline's own DECOMPOSE_QA gate. Returns ``ok=False`` with a\n    human-readable error rather than raising for parse/DAG failures, so\n    the LLM caller can read the error and try again.\n\n    InvalidPlanError is raised only for invalid arguments (both sources or neither).\n    ",
      "inputSchema": {
        "properties": {
          "markdown": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Markdown"
          },
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Path"
          }
        },
        "title": "orcho_plan_validateArguments",
        "type": "object"
      },
      "name": "orcho_plan_validate",
      "outputSchema": {
        "$defs": {
          "SubTaskRecord": {
            "properties": {
              "depends_on": {
                "items": {
                  "type": "string"
                },
                "title": "Depends On",
                "type": "array"
              },
              "done_criteria": {
                "items": {
                  "type": "string"
                },
                "title": "Done Criteria",
                "type": "array"
              },
              "files": {
                "items": {
                  "type": "string"
                },
                "title": "Files",
                "type": "array"
              },
              "goal": {
                "title": "Goal",
                "type": "string"
              },
              "id": {
                "title": "Id",
                "type": "string"
              },
              "model": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Model"
              },
              "skill": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Skill"
              },
              "spec": {
                "default": "",
                "title": "Spec",
                "type": "string"
              }
            },
            "required": [
              "id",
              "goal"
            ],
            "title": "SubTaskRecord",
            "type": "object"
          }
        },
        "properties": {
          "error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Human-readable parse/DAG error. Set iff ok=False.",
            "title": "Error"
          },
          "ok": {
            "title": "Ok",
            "type": "boolean"
          },
          "planning_context": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Planning Context"
          },
          "short_summary": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Short Summary"
          },
          "source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "\"json\" if the plan came from a ```json``` fence, \"markdown\" if from section headers. None when ok=False.",
            "title": "Source"
          },
          "subtasks": {
            "items": {
              "$ref": "#/$defs/SubTaskRecord"
            },
            "title": "Subtasks",
            "type": "array"
          }
        },
        "required": [
          "ok"
        ],
        "title": "PlanValidateResult",
        "type": "object"
      }
    },
    {
      "description": "Return the catalogue of pipeline profiles.\n\n    Reads the canonical v2 profile catalogue exclusively.\n\n    Result shape:\n      * ``source=\"json_v2\"`` — v2 file loaded successfully;\n        ``profiles`` carries the catalogue; ``diagnostic`` is None.\n      * ``source=\"missing\"`` — no v2 file at the resolved path;\n        ``profiles=[]`` and ``diagnostic`` explains the cause +\n        expected location. MCP clients SHOULD surface the\n        diagnostic to the user (it's actionable: \"install\n        orcho-core ≥ 0.X\" or \"set ORCHO_PROFILES_V2_PATH\").\n\n    ``source=\"missing\"`` means no profile catalogue was found at the\n    resolved path; the diagnostic explains where the server looked and\n    how to override the path.\n    ",
      "inputSchema": {
        "properties": {},
        "title": "orcho_profiles_listArguments",
        "type": "object"
      },
      "name": "orcho_profiles_list",
      "outputSchema": {
        "$defs": {
          "ProfileHypothesisRecord": {
            "description": "Plan-step hypothesis prelude policy exposed from v2 profiles.",
            "properties": {
              "attempts": {
                "description": "Number of hypothesis attempts before PLAN. Zero disables it.",
                "title": "Attempts",
                "type": "integer"
              },
              "format": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Prompt format used for the hypothesis and its QA. None means inherit the owning plan step's prompt format.",
                "title": "Format"
              }
            },
            "required": [
              "attempts"
            ],
            "title": "ProfileHypothesisRecord",
            "type": "object"
          },
          "ProfileRecord": {
            "description": "Profile catalogue entry.\n\nSupports both flat phase-list entries and v2 kind/variant/steps\nentries. v2 fields are optional and ``None`` for flat entries.",
            "properties": {
              "cross_gates": {
                "anyOf": [
                  {
                    "additionalProperties": {
                      "additionalProperties": true,
                      "type": "object"
                    },
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: profile-level policy for runner-owned cross gates (``contract_check`` / ``cross_final_acceptance``). Each entry is a JSON object with ``enabled``, ``run``, ``on_skip``, and optional ``mode``. None when the profile does not declare an explicit ``cross_gates`` block — consumers should treat missing as 'use documented defaults', not as 'no policy'.",
                "title": "Cross Gates"
              },
              "default_mode": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: operating mode this profile defaults to (``fast`` / ``pro`` / ``governed``). ``None`` when the profile declares no default mode.",
                "title": "Default Mode"
              },
              "description": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: human-readable purpose (one-line).",
                "title": "Description"
              },
              "hypothesis": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/ProfileHypothesisRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: plan-step hypothesis prelude policy. None when the profile has no plan-step hypothesis block or sets attempts=0."
              },
              "internal": {
                "default": false,
                "description": "v2: ``True`` for engine-internal profiles (e.g. ``task`` / ``correction``) that are not a normal public work-kind choice. The catalogue still lists them so callers can resolve them, but clients should not offer them as a primary selection.",
                "title": "Internal",
                "type": "boolean"
              },
              "kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: 'full_cycle' | 'scoped' | 'custom'. None for v1 entries.",
                "title": "Kind"
              },
              "name": {
                "title": "Name",
                "type": "string"
              },
              "phases": {
                "description": "v1 flat phase list. Empty when entry is v2-only.",
                "items": {
                  "type": "string"
                },
                "title": "Phases",
                "type": "array"
              },
              "recipe_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: recipe family — ``full_cycle`` (end-to-end), ``focused`` (scoped single-concern), or ``internal`` (engine-internal). ``None`` when not declared.",
                "title": "Recipe Kind"
              },
              "semantic_profile": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: semantic work-kind identity for built-in profiles (e.g. ``feature``, ``small_task``, ``planning``, ``code_review``). ``None`` for custom/plugin or internal profiles that carry no semantic identity.",
                "title": "Semantic Profile"
              },
              "variant": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "v2: plugin/custom typology coordinate (the ``variant`` half of a ``kind × variant`` pair) used by custom or plugin-supplied profiles. Built-in profiles leave this ``null`` — their public identity is the semantic ``name`` / ``semantic_profile``, not a variant string.",
                "title": "Variant"
              }
            },
            "required": [
              "name"
            ],
            "title": "ProfileRecord",
            "type": "object"
          },
          "ProfileSelectorRecord": {
            "description": "A profile *selector* — a ``profile`` value that picks a profile\ndynamically rather than naming an executable recipe.\n\nDistinct from :class:`ProfileRecord` (an executable profile with a\nconcrete phase recipe): a selector is consumed by orcho-core *before*\nprofile resolution to choose a semantic profile. ``auto-detect`` is the\ncanonical example — core classifies the work kind and selects the\nmatching semantic profile + mode. Surfaced separately from\n``profiles`` so a client never tries to run a selector as if it were a\nrecipe, and never mistakes it for a missing executable profile.",
            "properties": {
              "description": {
                "description": "One-line purpose of the selector.",
                "title": "Description",
                "type": "string"
              },
              "is_selector": {
                "const": true,
                "default": true,
                "description": "Always ``True`` — marks this entry as a dynamic selector, not an executable profile recipe.",
                "title": "Is Selector",
                "type": "boolean"
              },
              "name": {
                "description": "The selector token to pass as ``profile`` (e.g. ``auto-detect``).",
                "title": "Name",
                "type": "string"
              }
            },
            "required": [
              "name",
              "description"
            ],
            "title": "ProfileSelectorRecord",
            "type": "object"
          }
        },
        "properties": {
          "diagnostic": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Human-readable explanation when ``source != 'json_v2'``. MCP clients SHOULD surface this to the user when present. None when profiles loaded successfully.",
            "title": "Diagnostic"
          },
          "profiles": {
            "items": {
              "$ref": "#/$defs/ProfileRecord"
            },
            "title": "Profiles",
            "type": "array"
          },
          "selectors": {
            "description": "Profile *selectors* — ``profile`` values that pick a profile dynamically (e.g. ``auto-detect``: core classifies the work kind and selects the matching semantic profile) rather than naming an executable recipe. Kept disjoint from ``profiles``: a selector never appears in ``profiles`` and vice versa. Surfaced even when ``source='missing'`` because selectors do not depend on the v2 catalogue file.",
            "items": {
              "$ref": "#/$defs/ProfileSelectorRecord"
            },
            "title": "Selectors",
            "type": "array"
          },
          "source": {
            "description": "\"json_v2\" when the v2 profile file (_config/pipeline_profiles_v2.json) was loaded; \"missing\" when no v2 file is present (orcho-core is required to supply one — empty result + diagnostic).",
            "title": "Source",
            "type": "string"
          }
        },
        "required": [
          "profiles",
          "source"
        ],
        "title": "ProfilesListResult",
        "type": "object"
      }
    },
    {
      "description": "Resolve a prompt template through the 3-level chain (project → workspace → core).\n\n    Returns the chain (with exists flags) and the content of the winning\n    entry. If ``project_dir`` is None, only the core level is consulted.\n    ",
      "inputSchema": {
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          },
          "project_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Project Dir"
          }
        },
        "required": [
          "name"
        ],
        "title": "orcho_prompts_resolveArguments",
        "type": "object"
      },
      "name": "orcho_prompts_resolve",
      "outputSchema": {
        "$defs": {
          "PromptChainEntry": {
            "properties": {
              "exists": {
                "title": "Exists",
                "type": "boolean"
              },
              "level": {
                "title": "Level",
                "type": "string"
              },
              "path": {
                "title": "Path",
                "type": "string"
              }
            },
            "required": [
              "level",
              "path",
              "exists"
            ],
            "title": "PromptChainEntry",
            "type": "object"
          }
        },
        "properties": {
          "chain": {
            "items": {
              "$ref": "#/$defs/PromptChainEntry"
            },
            "title": "Chain",
            "type": "array"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "resolved_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Path of the winning chain entry (first existing). None if no level exists.",
            "title": "Resolved Path"
          },
          "resolved_text": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Content of the winning entry. None if no level exists.",
            "title": "Resolved Text"
          }
        },
        "required": [
          "name",
          "chain"
        ],
        "title": "PromptResolveResult",
        "type": "object"
      }
    },
    {
      "description": "Stop a running pipeline.\n\n    ``mode=\"graceful\"`` sends SIGTERM to the run's process group; pipeline\n    catches it, flushes the checkpoint, and emits a ``run.interrupted``\n    event. ``mode=\"hard\"`` sends SIGKILL; in-flight LLM HTTP requests drop\n    immediately and the checkpoint reflects only fully-completed phases.\n\n    Works for runs spawned this server lifetime as well as orphans picked\n    up by restart-recovery (cancel via raw ``os.kill`` on the persisted pid).\n    ",
      "inputSchema": {
        "properties": {
          "mode": {
            "default": "graceful",
            "title": "Mode",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_cancelArguments",
        "type": "object"
      },
      "name": "orcho_run_cancel",
      "outputSchema": {
        "description": "Outcome of ``orcho_run_cancel``.\n\nPossible status values:\n  - ``signal_sent(graceful)`` — SIGTERM delivered; pipeline catches it,\n    flushes checkpoint, exits with ``run.interrupted`` event.\n  - ``signal_sent(hard)`` — SIGKILL delivered; in-flight LLM HTTP sockets\n    drop, checkpoint reflects only fully-completed phases.\n  - ``already_dead`` — process exit not initiated by us; already gone.\n  - ``already_done`` — process exited cleanly before the cancel reached it.",
        "properties": {
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "status"
        ],
        "title": "CancelResult",
        "type": "object"
      }
    },
    {
      "description": "Diagnose a run's resume situation before acting on it — read-only.\n\n    Returns a typed verdict so a control-loop client never has to guess\n    whether resuming is safe, wasted, or wrong. Call this BEFORE any risky\n    ``orcho_run_resume``: it classifies the run deterministically and hands\n    back unambiguously typed next steps.\n\n    ``condition`` (first-match priority):\n      - ``active`` — running; watch / poll it (no resume needed).\n      - ``needs_decision`` — paused on ``awaiting_phase_handoff``; an operator\n        must record a phase-handoff decision before it can resume.\n      - ``resume_inert_terminal`` — terminal (terminal success or a terminal\n        halt reason); resuming is inert, so only inspection is suggested.\n      - ``superseded_by_child`` — a newer unfinished follow-up child is\n        continuing this run; resume the child (``recommended_run_id``), not\n        this parent.\n      - ``blocked_worktree`` — a follow-up blocked because the parent's\n        undelivered diff is not replayable here; resume the parent when known,\n        otherwise inspect (never a blind resume).\n      - ``halted`` / ``failed`` / ``interrupted`` — a resumable non-terminal\n        stop; resume this run or inspect its errors.\n\n    ``control`` is a SEPARATE, orthogonal axis from ``condition``: whether this\n    MCP server can actually mutate the run. ``mcp_controllable`` means the run\n    carries durable ``mcp_supervisor.json`` state with a resolvable project_dir\n    (it was started by this server, so resume / decide can act on it);\n    ``inspect_only`` means the run was NOT started by this MCP server and has no\n    durable supervisor metadata (a foreign / CLI-started run dir), so MCP can\n    only inspect it — a suggested ``orcho_run_resume`` cannot be applied even\n    when the ``condition`` is resumable. ``control_reason`` is the one-line fact\n    behind the verdict. ``control`` is ``None`` only when the classification\n    could not be read (never defaulted to controllable).\n\n    ``next_actions`` are typed by call-readiness via the ``kind`` field, which\n    clients MUST branch on instead of reading ``intent`` prose:\n      - ``kind=\"ready_call\"`` — ``args`` already carry every required\n        parameter of the target tool, so the call is safe to forward verbatim\n        (e.g. ``orcho_run_resume`` with ``run_id``).\n      - ``kind=\"operator_input_required\"`` — a final decision argument is\n        intentionally omitted; ``choices`` and/or ``input_schema`` describe the\n        operator input still needed (e.g. the required ``feedback`` for a\n        ``retry_feedback`` decision). Such records are never ``ready_call``.\n\n    Read-only: this tool spawns no process and mutates no state.\n\n    Raises RunNotFoundError if ``run_id`` doesn't exist on disk.\n    ",
      "inputSchema": {
        "properties": {
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_diagnoseArguments",
        "type": "object"
      },
      "name": "orcho_run_diagnose",
      "outputSchema": {
        "$defs": {
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "RecoveryLineage": {
            "description": "Durable recovery-lineage facts behind a continuation recommendation.\n\nThe structured facts a captain can branch on without reading output logs:\nwhich source run was resolved and whether it is checkpoint-resumable,\nwhether an active follow-up child exists, whether a persisted plan\nartifact is the subject, and — for an ``unknown`` dead-end — exactly which\ndurable facts are missing. All fields are projected from ``meta`` / SDK\nreaders (never parsed from log prose), so a missing fact reads as\n``None`` / ``False`` / an explicit ``missing_facts`` entry.\n\nReused verbatim by ``RunDiagnosis`` (``orcho_run_diagnose``) and\n``RecoveryRecommendation`` (``orcho_run_status``) so the two surfaces never\ndrift.",
            "properties": {
              "active_child_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The newest unfinished follow-up child of the inspected run, when one supersedes it; ``None`` otherwise.",
                "title": "Active Child Run Id"
              },
              "missing_facts": {
                "description": "For an ``unknown`` dead-end, the exact durable facts that are absent (e.g. ``no source/parent run id`` / ``no plan artifact`` / ``no delivery gate`` / ``no active child``). Empty when a continuation subject was resolved.",
                "items": {
                  "type": "string"
                },
                "title": "Missing Facts",
                "type": "array"
              },
              "plan_subject_available": {
                "default": false,
                "description": "True when a durable, readable ``parsed_plan.json`` artifact is the continuation subject (a plan-only / research run with no undelivered diff).",
                "title": "Plan Subject Available",
                "type": "boolean"
              },
              "source_resumable": {
                "default": false,
                "description": "True when the source run is NOT a terminal-resume-parent and still owns retained work (a preserved worktree or a persisted plan) — i.e. resuming it would advance the change session.",
                "title": "Source Resumable",
                "type": "boolean"
              },
              "source_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The resolved source/parent run a terminal recovery run should continue, when one is durably linked; ``None`` otherwise.",
                "title": "Source Run Id"
              },
              "source_status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Merged status of ``source_run_id`` when readable.",
                "title": "Source Status"
              }
            },
            "title": "RecoveryLineage",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_run_diagnose`` — a typed, read-only verdict on a\nrun's resume situation plus unambiguously typed next steps.\n\n``condition`` is the deterministic classification (first-match priority,\ncomputed by ``services.run_projection.project_run_diagnosis``):\n\n- ``active`` — the run is executing; watch / poll it.\n- ``needs_decision`` — paused on ``awaiting_phase_handoff``; an operator\n  must record a phase-handoff decision before it can resume.\n- ``needs_delivery_decision`` — paused at an Orcho-managed post-release\n  delivery / correction gate; inspect the gate via\n  ``orcho_delivery_gate`` and choose one of its ready\n  ``orcho_delivery_decide`` calls.\n- ``correction_followup_required`` — the release was rejected and a\n  correction was already requested (only ``halt`` remains on the gate);\n  resuming this run is inert and a repeated ``fix`` is a no-op, so the typed\n  next step is an ``orcho_run_start`` from_run_plan follow-up carrying the\n  parent's plan + retained ``diff.patch`` (``recommended_next_action=\n  'start_followup'``).\n- ``closed_by_followup`` — this run is a rejected-FA / correction parent\n  that a successful ``from_run_plan`` follow-up CLOSED (orcho-core\n  finalization stamped ``superseded_by_followup`` and settled it to\n  ``done``). It is terminal/settled, never an active correction: its old\n  release_blockers are NOT authoritative, resume is inert, and the\n  superseding child rides in ``recommended_run_id``.\n- ``recover_via_source_run`` — this run is a terminal / rejected recovery\n  run, but durable lineage points at a *resumable source* run; resume the\n  source (``recommended_run_id``), NOT a fresh ``from_run_plan`` against\n  this inert run.\n- ``resume_inert_terminal`` — terminal (terminal success or a terminal\n  halt reason); resuming is inert, so only inspection is offered. The\n  typed ``continuation_subject`` / ``recommended_next_action`` still\n  distinguish a plan-only continuation, a clean start-followup, and a\n  ``stop_unknown`` dead-end.\n- ``superseded_by_child`` — a newer unfinished follow-up child is\n  continuing this run; resume the child (``recommended_run_id``).\n- ``blocked_worktree`` — a follow-up blocked because the parent's\n  undelivered diff is not replayable here; resume the parent when known.\n- ``provider_pressure`` — a residual ``halted`` / ``failed`` /\n  ``interrupted`` stop that core typed as a provider runtime/access failure\n  (rate-limit, transient runtime fault, access loss) in its errors/halt\n  rollup, rather than a generic code/test/review failure. The typed\n  ``provider_pressure`` field carries the core-typed facts and the\n  conservative resume/retry (or wait-for-reset) next steps; this is NOT a\n  rejected review / failed acceptance / operator halt.\n- ``halted`` / ``failed`` / ``interrupted`` — a resumable non-terminal\n  stop; resume this run or inspect its errors.\n\nThe typed ``continuation_subject`` / ``recommended_next_action`` (projected\nfrom the same ``services.run_lineage`` resolver that backs\n``orcho_run_status.recovery_recommendation``) let an agent pick the next\nMCP call without reading output logs, and ``recovery_lineage`` carries the\ndurable facts behind that choice. ``from_run_plan`` is recommended ONLY for\na durable plan artifact (``recommended_next_action='plan_artifact_continuation'``),\nnever as a generic way to finish a retained diff or checkpoint.\n\n``next_actions`` carry the typed call-readiness contract: a\n``ready_call`` record's ``args`` already hold every required parameter of\nthe target tool (safe to forward verbatim), while an\n``operator_input_required`` record intentionally omits a final decision\nargument and surfaces ``choices`` / ``input_schema`` for the operator to\nfill. Clients MUST branch on the typed ``kind`` field, never on\n``intent`` prose.",
        "properties": {
          "available_actions": {
            "description": "For ``needs_decision``, the phase-handoff decision verbs the runtime published (``continue`` / ``retry_feedback`` / ``halt`` / ``continue_with_waiver``). Empty otherwise.",
            "items": {
              "type": "string"
            },
            "title": "Available Actions",
            "type": "array"
          },
          "condition": {
            "description": "Deterministic, first-match resume-situation classification for the run.",
            "enum": [
              "active",
              "needs_decision",
              "needs_delivery_decision",
              "correction_followup_required",
              "closed_by_followup",
              "recover_via_source_run",
              "resume_inert_terminal",
              "superseded_by_child",
              "blocked_worktree",
              "provider_pressure",
              "halted",
              "failed",
              "interrupted"
            ],
            "title": "Condition",
            "type": "string"
          },
          "continuation_subject": {
            "anyOf": [
              {
                "enum": [
                  "source_run_checkpoint",
                  "active_child_run",
                  "delivery_gate",
                  "plan_artifact",
                  "none",
                  "unknown"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The typed durable subject this run's continuation should act on (``source_run_checkpoint`` / ``active_child_run`` / ``delivery_gate`` / ``plan_artifact`` / ``none`` / ``unknown``). ``None`` for conditions that carry no lineage subject (e.g. ``needs_decision``).",
            "title": "Continuation Subject"
          },
          "control": {
            "anyOf": [
              {
                "enum": [
                  "mcp_controllable",
                  "inspect_only"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Durable controllability axis — ORTHOGONAL to ``condition``. Whether *this* MCP server can mutate (resume / decide) the run or can only inspect it: ``mcp_controllable`` when the run carries durable ``mcp_supervisor.json`` state with a resolvable project_dir (it was started by this MCP server); ``inspect_only`` when the run was NOT started by this MCP server and has no durable supervisor metadata, so MCP can only inspect it (a foreign / CLI-started run dir). A run can be ``active`` / ``needs_decision`` / terminal AND ``inspect_only`` at the same time — the two axes never collapse into one another. ``None`` when the controllability classification could not be read (never defaulted to ``mcp_controllable``).",
            "title": "Control"
          },
          "control_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "One-line factual reason behind ``control`` (e.g. the resolved durable project_dir, or that no ``mcp_supervisor.json`` exists). ``None`` when ``control`` is ``None``.",
            "title": "Control Reason"
          },
          "decision_recorded": {
            "default": false,
            "description": "For ``needs_decision`` only: ``True`` when a phase-handoff decision artifact already exists for the active handoff. The run stays ``awaiting_phase_handoff`` (continue / retry_feedback / continue_with_waiver do not flip the status), so the next step is ``orcho_run_resume`` to apply the recorded decision — NOT a second ``orcho_phase_handoff_decide``. ``next_actions`` reflects this: a ready resume instead of decide verbs. ``False`` (default) for every other condition and for a pending handoff with no decision yet.",
            "title": "Decision Recorded",
            "type": "boolean"
          },
          "next_actions": {
            "description": "Typed follow-up tool calls for this condition. Each record carries an unambiguous ``kind`` (``ready_call`` vs ``operator_input_required``).",
            "items": {
              "$ref": "#/$defs/NextActionRecord"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "provider_pressure": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderPressure"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Set ONLY for ``condition == 'provider_pressure'``: the core-typed provider runtime/access failure (failure_kind, recoverable, phase, sanitized message, retry/reset when core gives them) plus the conservative typed ``next_actions`` from the shared ``build_provider_pressure_next_actions`` helper. ``None`` for every other condition and for a generic failure with no core-typed provider source."
          },
          "reason": {
            "description": "One-line factual explanation assembled from persisted state (status / halt_reason / lineage ids); never parsed from log prose.",
            "title": "Reason",
            "type": "string"
          },
          "recommended_next_action": {
            "anyOf": [
              {
                "enum": [
                  "resume_source_run",
                  "resume_active_child",
                  "delivery_decision",
                  "start_followup",
                  "plan_artifact_continuation",
                  "stop_unknown"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The typed next action a captain should take (``resume_source_run`` / ``resume_active_child`` / ``delivery_decision`` / ``start_followup`` / ``plan_artifact_continuation`` / ``stop_unknown``). ``None`` when no lineage recommendation applies.",
            "title": "Recommended Next Action"
          },
          "recommended_run_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The run to resume instead of this one — the active follow-up child (``superseded_by_child``) or the known parent (``blocked_worktree``). ``None`` otherwise.",
            "title": "Recommended Run Id"
          },
          "recovery_lineage": {
            "anyOf": [
              {
                "$ref": "#/$defs/RecoveryLineage"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Durable recovery-lineage facts behind the continuation recommendation (source pointer + resumability, active child, plan-subject availability, and — for a ``stop_unknown`` dead-end — the missing durable facts). ``None`` when no recovery lineage was projected."
          },
          "run_id": {
            "description": "The diagnosed run.",
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Merged run status at diagnosis time, when resolvable.",
            "title": "Status"
          }
        },
        "required": [
          "run_id",
          "condition",
          "reason"
        ],
        "title": "RunDiagnosis",
        "type": "object"
      }
    },
    {
      "description": "Read a captured ``diff.patch`` artifact for a run.\n\n    The pipeline writes diff artifacts at run lifecycle time; this\n    tool is the read side. Missing artifact is a typed\n    ``found=False`` result — not a JSON-RPC error — because clean\n    runs / quiet phases and runs predating the artifact are all\n    valid.\n\n    ``mode``:\n      - ``\"preview\"`` (default) — Claude-style grouped view with\n        per-file ``+A -R`` headers; good for context windows.\n      - ``\"stat\"`` — per-file ``+A -R`` table only.\n      - ``\"full\"`` — raw unified patch; suitable for piping to\n        ``git apply`` after byte-cap stripping. Never colored.\n\n    ``path`` (optional): restrict to files at this path. Exact match\n    first; falls back to prefix match. Matches renames and deletes by\n    either old or new name. Empty / whitespace-only string raises\n    ``InvalidPlanError``.\n\n    ``phase`` (optional): artifact key.\n      - ``None`` (default) reads the run-level cumulative\n        ``diff.patch`` — today's behaviour, unchanged.\n      - ``\"<phase>\"`` reads the per-phase artifact\n        ``phases/<phase>/diff.patch`` written by the pipeline during\n        that phase (e.g. ``\"implement\"``, ``\"repair_changes\"``).\n      Quiet phase (no artifact captured) → ``found=False``, never a\n      silent fallback to the cumulative diff.\n      Empty / whitespace-only, or values containing ``/``, ``\\``,\n      or ``..``, raise ``InvalidPlanError``.\n\n    ``max_bytes`` caps ``content`` in bytes. Default 200_000; hard\n    ceiling 2_000_000. ``0`` or above-ceiling values are rejected\n    (not silently clamped) so callers know they hit the limit. The\n    truncation is UTF-8 safe.\n\n    The result echoes ``scope`` (``\"run\"`` or ``\"phase\"``) and\n    ``phase`` so clients don't need to track what they asked for.\n\n    Errors:\n      - RunNotFoundError — unknown run_id.\n      - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE / $ORCHO_WORKTREE.\n      - InvalidPlanError — bad ``max_bytes`` (≤0, >2_000_000), empty\n        ``path``, or empty / traversal-bearing ``phase``.\n    ",
      "inputSchema": {
        "properties": {
          "max_bytes": {
            "default": 200000,
            "title": "Max Bytes",
            "type": "integer"
          },
          "mode": {
            "default": "preview",
            "enum": [
              "preview",
              "stat",
              "full"
            ],
            "title": "Mode",
            "type": "string"
          },
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Path"
          },
          "phase": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Phase"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_diffArguments",
        "type": "object"
      },
      "name": "orcho_run_diff",
      "outputSchema": {
        "$defs": {
          "RunDiffFile": {
            "description": "Per-file +A -R summary in a :class:`RunDiffResult`.",
            "properties": {
              "added": {
                "title": "Added",
                "type": "integer"
              },
              "path": {
                "title": "Path",
                "type": "string"
              },
              "removed": {
                "title": "Removed",
                "type": "integer"
              }
            },
            "required": [
              "path",
              "added",
              "removed"
            ],
            "title": "RunDiffFile",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_run_diff``.\n\n``found=False`` (with ``files=[]`` and ``content=\"\"``) signals \"no\ndiff artifact recorded for this run / phase\" — typed not-found,\nnot a JSON-RPC error.\n\n``found=True`` with empty ``files`` signals \"path filter matched\nnothing\"; ``message`` quotes the filter.\n\n``mode`` is constrained to the literal set so the published JSON\nSchema enumerates the choices, not a free ``str``. ``max_bytes``\nechoes the cap that produced ``truncated`` so clients can render\na footer without an out-of-band argument.\n\n``scope`` echoes which artifact was read: ``\"run\"`` for the\ncumulative ``diff.patch`` (default behaviour), ``\"phase\"`` for a\nper-phase ``phases/<phase>/diff.patch``. ``phase`` carries the\nnormalized phase name on phase calls, ``None`` on run calls — so\nclients don't have to remember what they asked for.",
        "properties": {
          "content": {
            "default": "",
            "title": "Content",
            "type": "string"
          },
          "diff_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Diff Path"
          },
          "files": {
            "items": {
              "$ref": "#/$defs/RunDiffFile"
            },
            "title": "Files",
            "type": "array"
          },
          "found": {
            "title": "Found",
            "type": "boolean"
          },
          "max_bytes": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Max Bytes"
          },
          "message": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Message"
          },
          "mode": {
            "enum": [
              "preview",
              "stat",
              "full"
            ],
            "title": "Mode",
            "type": "string"
          },
          "phase": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Phase"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "scope": {
            "default": "run",
            "enum": [
              "run",
              "phase"
            ],
            "title": "Scope",
            "type": "string"
          },
          "truncated": {
            "default": false,
            "title": "Truncated",
            "type": "boolean"
          }
        },
        "required": [
          "run_id",
          "found",
          "mode"
        ],
        "title": "RunDiffResult",
        "type": "object"
      }
    },
    {
      "description": "Return a bounded summary of a run's recent events.\n\n    The agent's typical question while a run is in flight is \"what\n    changed and what should I do next?\" — not \"give me every raw\n    event.\" This tool answers the first question in a single bounded\n    payload, so status polling no longer triggers MCP tool-result\n    auto-spill on the client side.\n\n    This tool is also the fallback half of the resilient observation\n    loop ``orcho_run_watch`` → ``orcho_run_events_summary`` →\n    ``orcho_run_watch``. When a bounded ``orcho_run_watch`` times out or\n    the client transport drops the long-poll, call this with\n    ``since_seq`` set to the watch's ``trigger.seq`` /\n    ``summary.next_seq`` to catch up on what happened while\n    disconnected, then resume watching from this summary's ``next_seq``.\n    A disconnected watch is not a failed run — the run keeps executing,\n    and decisions are taken from ``status`` / ``pending_handoff`` /\n    terminal signals here, never from the watch call ending.\n\n    The wire shape is `RunEventsSummary`. Counts, ``by_phase``,\n    ``by_kind``, and ``last_n`` are computed over the ``(since_seq,\n    limit)`` window; ``current_phase`` is computed over the *full*\n    event stream up to ``next_seq`` so polling-style callers don't\n    lose phase context when the window misses the original\n    ``phase.start``. ``status`` mirrors what ``orcho_run_status``\n    returns (meta + supervisor merge). ``next_actions`` are short\n    imperative strings derived conservatively from status only.\n\n    Args:\n        run_id: the run to summarise.\n        since_seq: only consider events with seq > since_seq. Default\n            ``0`` includes everything from the start. Must be ≥ 0.\n        limit: cap on events considered in this batch. Default 50,\n            hard ceiling 1000. ``next_seq`` advances by at most\n            ``limit`` events per call.\n        last_n: number of compact events to include in the tail.\n            Default 5, hard ceiling 100. ``0`` returns an empty list.\n\n    Errors:\n      - RunNotFoundError — unknown run_id.\n      - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE / $ORCHO_WORKTREE.\n      - InvalidPlanError — input out of range.\n\n    For raw event dumps (forensic replay, exact payload inspection)\n    keep using ``orcho_run_events_tail``; this tool deliberately drops\n    payload bodies.\n    ",
      "inputSchema": {
        "properties": {
          "last_n": {
            "default": 5,
            "title": "Last N",
            "type": "integer"
          },
          "limit": {
            "default": 50,
            "title": "Limit",
            "type": "integer"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "since_seq": {
            "default": 0,
            "title": "Since Seq",
            "type": "integer"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_events_summaryArguments",
        "type": "object"
      },
      "name": "orcho_run_events_summary",
      "outputSchema": {
        "$defs": {
          "CompactRunEvent": {
            "description": "Bounded mirror of ``EventRecord``.\n\nNever carries the full raw payload. The handler extracts a small set of\nwell-known payload keys (``summary``/``text``/``message``, ``tool``,\n``status``) and applies hard character-length caps so a single event\nwith a 10 KB ``command`` field cannot inflate the wire payload of a\n200-event summary past the bounded-observe budget. Truncation uses\nPython slicing (Unicode-safe at code-point boundaries); the limits\nare in characters, not bytes.",
            "properties": {
              "kind": {
                "description": "Event kind. Truncated to 64 chars to keep the bounded summary honest under future emitter changes.",
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase tag if present. Truncated to 64 chars (same reasoning as ``kind``).",
                "title": "Phase"
              },
              "seq": {
                "title": "Seq",
                "type": "integer"
              },
              "status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Status value when the event reports a phase/run state transition. Truncated to 64 chars.",
                "title": "Status"
              },
              "summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable summary pulled from one of the canonical payload keys (summary / text / message) and truncated to 256 chars.",
                "title": "Summary"
              },
              "tool": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Tool name when the event reports an agent tool call. Truncated to 64 chars.",
                "title": "Tool"
              },
              "ts": {
                "title": "Ts",
                "type": "string"
              }
            },
            "required": [
              "seq",
              "ts",
              "kind"
            ],
            "title": "CompactRunEvent",
            "type": "object"
          },
          "CurrentSubtaskRecord": {
            "description": "Live progress coordinate for the in-flight ``subtask_dag`` subtask.\n\nLets a watcher render step-by-step progress (\"subtask 3/12 (Patch\ntarget)\") during a long implement phase without scanning raw events.\nComputed from the latest ``subtask.start`` / ``subtask.end`` event up to\nthe summary horizon; cleared when the implement phase ends. ``None`` on\nthe summary means no subtask is currently in flight.",
            "properties": {
              "goal": {
                "default": "",
                "description": "One-line subtask goal, bounded.",
                "title": "Goal",
                "type": "string"
              },
              "index": {
                "description": "1-based position of this subtask in the whole DAG.",
                "title": "Index",
                "type": "integer"
              },
              "seq": {
                "description": "Event seq of the start/end boundary that produced this snapshot. Advances on every subtask transition — the ``until=\"subtask\"`` watch fires when it passes the caller's cursor.",
                "title": "Seq",
                "type": "integer"
              },
              "state": {
                "description": "\"running\" right after start; \"done\" / \"incomplete\" / \"failed\" once the subtask ends (incomplete = done-criteria attestation gate did not close).",
                "title": "State",
                "type": "string"
              },
              "subtask_id": {
                "title": "Subtask Id",
                "type": "string"
              },
              "total": {
                "description": "Total subtask count in the DAG (the denominator).",
                "title": "Total",
                "type": "integer"
              }
            },
            "required": [
              "subtask_id",
              "index",
              "total",
              "state",
              "seq"
            ],
            "title": "CurrentSubtaskRecord",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "PendingHandoffSummary": {
            "description": "Active phase-handoff state surfaced inside a run events summary.\n\nPopulated only when the merged run status is\n``awaiting_phase_handoff`` so polling callers see the pause —\nhandoff id, phase, trigger, machine verdict, a coherent round label,\nthe available decision actions, a bounded preview of the reviewer's\nlast output, whether a decision artifact already exists, and a\none-line ``suggested_next_action`` — without having to fire an\n``orcho_run_watch`` handoff trigger. The full structured decision\npacket (findings, choices, elicitation) still rides in\n``orcho_run_watch``'s ``HandoffDecisionHint``; this is the compact\nat-a-glance projection for status polling.",
            "properties": {
              "available_actions": {
                "description": "Decision verbs the runtime published for this handoff, verbatim.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "decision_artifact_exists": {
                "default": false,
                "description": "True when a phase-handoff decision artifact already exists for this handoff (the operator has decided and only ``orcho_run_resume`` remains).",
                "title": "Decision Artifact Exists",
                "type": "boolean"
              },
              "handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Handoff identifier from ``meta.phase_handoff.id``.",
                "title": "Handoff Id"
              },
              "last_output_preview": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded preview of the reviewer's last critique / output. Truncated; never the full raw text.",
                "title": "Last Output Preview"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the handoff (falls back to the run's current phase when the payload omits it).",
                "title": "Phase"
              },
              "round_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Coherent operator round label — ``<phase> automatic round R/M`` for an auto round, ``<phase> human retry K`` for a human-directed retry round. Never an impossible ``R/M`` with ``R > M``.",
                "title": "Round Label"
              },
              "suggested_next_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "One-line pointer at the right next tool: decide then resume when no decision exists yet, resume when one already does.",
                "title": "Suggested Next Action"
              },
              "trigger": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Why the handoff fired (e.g. ``rejected`` / ``approved`` / ``incomplete``).",
                "title": "Trigger"
              },
              "verdict": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Machine verdict label carried verbatim from the payload (``REJECTED`` / ``APPROVED``).",
                "title": "Verdict"
              }
            },
            "title": "PendingHandoffSummary",
            "type": "object"
          },
          "PhaseEventSummary": {
            "description": "Per-phase aggregate over the windowed event slice.",
            "properties": {
              "count": {
                "title": "Count",
                "type": "integer"
              },
              "kinds": {
                "description": "Distinct event kinds observed in this phase, sorted.",
                "items": {
                  "type": "string"
                },
                "title": "Kinds",
                "type": "array"
              },
              "phase": {
                "description": "Phase name, or ``\"(unknown)\"`` for events emitted without a phase tag.",
                "title": "Phase",
                "type": "string"
              }
            },
            "required": [
              "phase",
              "count",
              "kinds"
            ],
            "title": "PhaseEventSummary",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "ProviderSessionFallback": {
            "description": "A recovered missing-provider-session fallback, surfaced in a summary.\n\norcho-core emits ``phase.provider_session_fallback`` ONLY after a\nfresh-session retry already succeeded — the stale-session error was\ncaught and fully handled, no ``failed`` record leaks out, and the run\ncontinues in the same worktree with persisted context. This record\nmakes that recovery observable as structured data and, via\n``phase_succeeded=True``, makes explicit that the fallback is a\nrecovery notice — never a phase failure.",
            "properties": {
              "fallback_mode": {
                "const": "fresh_provider_session",
                "default": "fresh_provider_session",
                "description": "Recovery mode — a fresh provider session was started in place of the missing one.",
                "title": "Fallback Mode",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase whose provider session was missing and recovered.",
                "title": "Phase"
              },
              "phase_succeeded": {
                "default": true,
                "description": "True for a recovered fallback — the event is emitted only after the fresh-session retry succeeded, so this is NOT a phase failure.",
                "title": "Phase Succeeded",
                "type": "boolean"
              },
              "stale_session_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Redacted preview of the missing provider session id (a short prefix + ``…``); never the full identifier. The sentinel ``unknown`` passes through when core had no id.",
                "title": "Stale Session Id"
              },
              "worktree_preserved": {
                "default": true,
                "description": "True — the fallback continues in the same run worktree with persisted context (no checkout was lost).",
                "title": "Worktree Preserved",
                "type": "boolean"
              }
            },
            "title": "ProviderSessionFallback",
            "type": "object"
          },
          "RetryState": {
            "description": "Human-retry / repeated-reject lifecycle state, alongside the pause.\n\nDerived from raw persisted inputs (round counters, verdict, and the\n``retry_feedback`` decision artifacts) — never a re-rendered CLI\nbanner. Distinguishes an automatic reject, a human-directed retry in\nprogress, a human retry that was rejected again, and a retry that was\naccepted (handoff closed).",
            "properties": {
              "operator_feedback_preview": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded preview of the operator's most recent retry_feedback text; truncated, never the full text. ``None`` when no retry feedback was recorded.",
                "title": "Operator Feedback Preview"
              },
              "pending_operator_decision": {
                "default": false,
                "description": "True while the run is paused awaiting an operator decision on the active handoff (no decision artifact recorded for it yet).",
                "title": "Pending Operator Decision",
                "type": "boolean"
              },
              "retry_attempt_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Coherent operator label for the attempt — ``<phase> automatic round R/M``, ``<phase> human retry K after REJECTED verdict``, or ``<phase> human retry K rejected; operator decision required``. Never an impossible ``R/M`` with ``R > M``.",
                "title": "Retry Attempt Label"
              },
              "retry_context": {
                "description": "Reject / retry lifecycle state. ``automatic_reject`` — paused on a rejected verdict at an automatic round; ``human_retry_in_progress`` — a retry_feedback decision exists and the run is executing the human-directed round; ``retry_rejected_again`` — paused again on a rejected verdict at a human-directed round; ``retry_accepted_closed`` — the retry was accepted and the handoff closed. The two non-paused states are best-effort lifecycle hints.",
                "enum": [
                  "automatic_reject",
                  "human_retry_in_progress",
                  "retry_rejected_again",
                  "retry_accepted_closed"
                ],
                "title": "Retry Context",
                "type": "string"
              }
            },
            "required": [
              "retry_context"
            ],
            "title": "RetryState",
            "type": "object"
          }
        },
        "description": "Bounded summary of a run's recent events.\n\nDesigned for status polling: an operator-side LLM can read this once\nper check instead of dumping the raw event list. ``status`` and\n``current_phase`` come from the authoritative meta/supervisor merge so\ncallers see the same answers ``orcho_run_status`` would give. The\nrest of the aggregate is computed over the ``(since_seq, limit)``\nwindow. ``next_actions`` are short imperative strings derived from\n``status`` only — conservative, no invention.\n\nThis is also the fallback half of the resilient observation loop:\n``orcho_run_watch`` → ``orcho_run_events_summary`` → ``orcho_run_watch``.\nWhen a bounded watch times out or the client transport drops the\nlong-poll, poll this summary with ``since_seq=next_seq`` and then\nresume watching from the returned ``next_seq``. A client-side watch\ndisconnect is observer loss, not run failure — the run keeps\nexecuting in its worktree, and run decisions are taken only from the\ntyped ``status`` / ``pending_handoff`` / terminal / evidence signals,\nnever from the fact that a watch call ended.",
        "properties": {
          "by_kind": {
            "additionalProperties": {
              "type": "integer"
            },
            "description": "Total event count per kind across the window.",
            "title": "By Kind",
            "type": "object"
          },
          "by_phase": {
            "description": "Per-phase counts and kind vocabulary, in first-seen phase order.",
            "items": {
              "$ref": "#/$defs/PhaseEventSummary"
            },
            "title": "By Phase",
            "type": "array"
          },
          "current_phase": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Latest ``phase.start`` seen anywhere up to ``next_seq`` and not yet closed by a ``phase.end``. Computed over the full event stream — not just the windowed slice — so polling stays accurate.",
            "title": "Current Phase"
          },
          "current_subtask": {
            "anyOf": [
              {
                "$ref": "#/$defs/CurrentSubtaskRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Live progress coordinate for the in-flight subtask_dag subtask (index/total/goal/state), or ``None`` when no subtask is currently running. Computed over the full event stream up to ``next_seq``; cleared when the implement phase ends."
          },
          "eof": {
            "description": "True when every event past ``since_seq`` fit in this batch.",
            "title": "Eof",
            "type": "boolean"
          },
          "last_n": {
            "description": "The most recent ``last_n`` events from the window, in seq order. Each entry is a ``CompactRunEvent`` — no raw payload spill.",
            "items": {
              "$ref": "#/$defs/CompactRunEvent"
            },
            "title": "Last N",
            "type": "array"
          },
          "next_actions": {
            "description": "Conservative operator guidance derived from ``status`` only. Empty when status is unknown.",
            "items": {
              "type": "string"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "next_seq": {
            "description": "Reconnect cursor: the seq to pass as the next ``since_seq``. Carry it back into ``orcho_run_watch`` (or another ``orcho_run_events_summary``) to resume observation exactly where this batch ended after a bounded watch times out or the client transport drops the long-poll.",
            "title": "Next Seq",
            "type": "integer"
          },
          "pending_handoff": {
            "anyOf": [
              {
                "$ref": "#/$defs/PendingHandoffSummary"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Active phase-handoff state when the run is paused on ``awaiting_phase_handoff``; ``None`` otherwise. Lets a polling caller see the pause and its decision surface without firing an ``orcho_run_watch`` handoff trigger."
          },
          "provider_pressure": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderPressure"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Core-typed provider runtime/access failure for a terminal run, projected from the same ``project_provider_pressure`` source and shared ``build_provider_pressure_next_actions`` helper as ``orcho_run_status`` / ``orcho_run_diagnose`` / ``orcho_run_evidence`` — so every surface agrees on the condition and its conservative typed next_actions. ``None`` for a non-terminal run or a generic failure with no core-typed provider source. Additive: the legacy ``next_actions: list[str]`` field is unchanged."
          },
          "provider_session_fallbacks": {
            "description": "Recovered missing-provider-session fallbacks observed up to ``next_seq`` (phase / redacted stale session id / fallback mode / worktree preserved / phase succeeded). Each entry is a recovery notice, not a phase failure — ``phase_succeeded`` is True. Empty when none occurred.",
            "items": {
              "$ref": "#/$defs/ProviderSessionFallback"
            },
            "title": "Provider Session Fallbacks",
            "type": "array"
          },
          "retry_state": {
            "anyOf": [
              {
                "$ref": "#/$defs/RetryState"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Human-retry / repeated-reject lifecycle state when the run is in a reject / retry lifecycle (alongside ``pending_handoff``); ``None`` otherwise. Lets a client tell an automatic reject from a human retry, a repeated reject, and an accepted-and-closed retry."
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Merged run status (meta.json + supervisor terminal fallback). Same value ``orcho_run_status`` returns.",
            "title": "Status"
          },
          "total_count": {
            "description": "Number of events in the considered window (after ``since_seq`` and ``limit``).",
            "title": "Total Count",
            "type": "integer"
          }
        },
        "required": [
          "run_id",
          "total_count",
          "next_seq",
          "eof"
        ],
        "title": "RunEventsSummary",
        "type": "object"
      }
    },
    {
      "description": "Return events with seq > since_seq, newest events at the end.\n\n    Designed for catch-up after a client reconnect: client persists the\n    last-seen seq, calls this tool with ``since_seq=last_seen`` and gets\n    everything missed since. ``next_seq`` is the highest seq returned (or\n    ``since_seq`` if nothing new). ``eof=True`` means no events remain\n    after ``next_seq`` at this snapshot — caller can stop polling until\n    the run state changes.\n\n    Args:\n        run_id: the run to tail.\n        since_seq: only return events with seq strictly greater. Default 0\n            returns all events from the start.\n        limit: cap on returned events; default 25. Caller paginates by\n            advancing ``since_seq`` to the previous ``next_seq``.\n    ",
      "inputSchema": {
        "properties": {
          "limit": {
            "default": 25,
            "title": "Limit",
            "type": "integer"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "since_seq": {
            "default": 0,
            "title": "Since Seq",
            "type": "integer"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_events_tailArguments",
        "type": "object"
      },
      "name": "orcho_run_events_tail",
      "outputSchema": {
        "$defs": {
          "EventRecord": {
            "description": "One event from events.jsonl. Mirrors the on-disk shape verbatim.",
            "properties": {
              "kind": {
                "title": "Kind",
                "type": "string"
              },
              "payload": {
                "additionalProperties": true,
                "title": "Payload",
                "type": "object"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Phase"
              },
              "seq": {
                "title": "Seq",
                "type": "integer"
              },
              "ts": {
                "title": "Ts",
                "type": "string"
              }
            },
            "required": [
              "seq",
              "ts",
              "kind"
            ],
            "title": "EventRecord",
            "type": "object"
          }
        },
        "properties": {
          "eof": {
            "description": "True when no events remain after next_seq at this snapshot. False means more events exist and another tail call will return them.",
            "title": "Eof",
            "type": "boolean"
          },
          "events": {
            "items": {
              "$ref": "#/$defs/EventRecord"
            },
            "title": "Events",
            "type": "array"
          },
          "next_seq": {
            "title": "Next Seq",
            "type": "integer"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "events",
          "next_seq",
          "eof"
        ],
        "title": "EventsTailResult",
        "type": "object"
      }
    },
    {
      "description": "Inspect a run via typed slices — no raw log scraping required.\n\n    The full evidence bundle (``collect_evidence``) is exhaustive; this\n    tool surfaces narrow projections control-loop clients actually\n    need: what the plan said, which findings blocked the run, which\n    commands the pipeline shelled out to, what artifacts landed, why\n    the run halted, and the cross-run alias linkage for cross-project\n    parents.\n\n    ``slice``:\n      - ``\"all\"`` (default) — every slice populated in one response.\n      - ``\"plan\"`` — plan summary only.\n      - ``\"findings\"`` — flattened findings list (filterable).\n      - ``\"commands\"`` — pipeline shell-outs.\n      - ``\"artifacts\"`` — files the run wrote.\n      - ``\"errors\"`` — errors + halt reason.\n      - ``\"sub_runs\"`` — cross-run child alias links (empty for\n        single-project runs).\n      - ``\"receipts\"`` — per-subtask delivery receipts for a\n        ``subtask_dag`` run: state (``done|incomplete|failed|skipped``)\n        plus the done-criteria self-attestation (``criteria_report``,\n        ``attestation_summary``, ``attestation_error``). A subtask that\n        executed but did not close its done-criteria is ``incomplete``\n        with the reason in ``attestation_error``. Empty for\n        ``whole_plan`` runs.\n      - ``\"verification_receipts\"`` — durable verification-environment\n        receipts for the developer-side phases: which\n        interpreter (``python``) and working directory (``cwd``) ran the\n        checks, the import checks (name / expected / actual / passed),\n        the exact commands + exit codes, the clean-tree note\n        (``temp_env_outside_checkout``), and the on-disk\n        ``artifact_path``. Empty when the run recorded no receipts.\n      - ``\"verification_timeline\"`` — the official verification-gate\n        timeline as typed data: per-gate ``status`` (exactly one of\n        ``PASS`` / ``FAIL`` / ``MISSING`` / ``STALE`` / ``SKIPPED`` /\n        ``FRESH`` — a manual/operator-only gate is ``SKIPPED`` with\n        ``policy='manual_only'``), each missing/stale/failed required gate\n        carrying its own ``rerun_hint`` + ``searched_run_dirs``, plus the\n        residual / manual-only / inherited aggregates and the auto-run\n        events. ``has_contract=False`` when the project declares no\n        verification contract.\n      - ``\"verification_cockpit\"`` — the SAME verification gates as a typed\n        cockpit (built from one shared SDK read, never hiding the timeline):\n        a header (``has_contract`` / ``mode`` / ``envs`` / ``policy_summary``\n        / ``effect``) plus one row per gate. Each row keeps the command name\n        AND its planning properties visible together — ``hook``/phase,\n        ``trigger``, ``policy``, ``required``, ``gate_class`` + ``class_source``,\n        the same six-value ``status``, ``env``, and the deciding-receipt\n        evidence (``receipt_path`` / ``inherited`` / ``source_run_id``) with\n        ``stale_reason`` / ``rerun_hint`` where applicable. ``trigger`` is\n        derived deterministically: ``operator_only`` for a manual-only gate\n        (membership or ``policy='manual_only'``) — present on purpose, NOT an\n        automation failure; ``auto`` when the run's automation acted on the\n        command; else ``manual``. ``has_contract=False`` when the project\n        declares no verification contract.\n      - ``\"handoff_advice\"`` — Stage 0/1 phase-handoff advisor evidence:\n        one record per advisor call (``handoff_id`` / ``phase`` /\n        ``recommended_action`` / ``applied_action`` / ``confidence`` /\n        ``resolved`` / ``repeated`` / ``outcome`` / ``finding_fingerprint`` /\n        token-usage + cost / ``advice_artifact``) plus an aggregate ``summary``\n        (``calls`` / ``applied_retries`` / ``resolved_retries`` / ``repeated`` /\n        ``stopped`` / ``unknown`` / ``usage``). ``resolved`` is tri-state\n        (True / False / None). A run with no advisor surface returns an empty\n        slice (``calls=[]``, zeroed summary), never an error.\n\n    ``severity_min`` (only honoured when findings are returned):\n    minimum-criticality cutoff. ``\"P0\"`` returns only P0; ``\"P1\"``\n    returns P0 + P1; etc. ``None`` returns all severities.\n\n    ``phases`` (only honoured for findings): restrict to specific\n    finding-bearing phases (``plan_qa``, ``review``, ``final_qa``,\n    ``compliance_check``). ``None`` returns findings from all four.\n\n    Errors:\n      - RunNotFoundError — unknown run_id.\n      - InvalidPlanError — invalid ``slice`` value or invalid\n        ``severity_min`` value.\n    ",
      "inputSchema": {
        "properties": {
          "phases": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Phases"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "severity_min": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Severity Min"
          },
          "slice": {
            "default": "all",
            "title": "Slice",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_evidenceArguments",
        "type": "object"
      },
      "name": "orcho_run_evidence",
      "outputSchema": {
        "$defs": {
          "CriterionReportRecord": {
            "description": "One developer claim against a subtask done-criterion (P7).\n\n``met`` is the developer's explicit self-attestation and ``evidence`` is a\none-sentence claim — NOT verified truth. The reviewer / final_acceptance /\ntest gates remain the verification layer; this record only reports what was\nclaimed.",
            "properties": {
              "criterion": {
                "title": "Criterion",
                "type": "string"
              },
              "evidence": {
                "title": "Evidence",
                "type": "string"
              },
              "index": {
                "description": "1-based position in the subtask's declared done_criteria.",
                "title": "Index",
                "type": "integer"
              },
              "met": {
                "title": "Met",
                "type": "boolean"
              }
            },
            "required": [
              "index",
              "criterion",
              "met",
              "evidence"
            ],
            "title": "CriterionReportRecord",
            "type": "object"
          },
          "ErrorsHaltSliceRecord": {
            "properties": {
              "error_summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Error Summary"
              },
              "errors": {
                "items": {
                  "additionalProperties": true,
                  "type": "object"
                },
                "title": "Errors",
                "type": "array"
              },
              "halt_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Halt Reason"
              },
              "halted_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Halted At"
              },
              "implement_delivery": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/ImplementDeliveryRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Typed delivery/waiver audit projected from the same errors-rollup. None for a clean delivery (no 'implement_delivery' breadcrumb in the rollup)."
              },
              "provider_pressure": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/ProviderPressure"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core-typed provider runtime/access failure projected from the same ``project_provider_pressure`` source (and the same shared ``build_provider_pressure_next_actions`` helper) as ``orcho_run_status`` / ``orcho_run_diagnose`` / ``orcho_run_events_summary`` — so the errors slice never loses the core fact and reports an identical condition / failure_kind / phase / next_actions. ``None`` when core attached no typed provider failure."
              },
              "status": {
                "title": "Status",
                "type": "string"
              }
            },
            "required": [
              "status"
            ],
            "title": "ErrorsHaltSliceRecord",
            "type": "object"
          },
          "EvidenceArtifactSliceRecord": {
            "properties": {
              "kind": {
                "title": "Kind",
                "type": "string"
              },
              "path": {
                "title": "Path",
                "type": "string"
              },
              "size_bytes": {
                "title": "Size Bytes",
                "type": "integer"
              }
            },
            "required": [
              "path",
              "kind",
              "size_bytes"
            ],
            "title": "EvidenceArtifactSliceRecord",
            "type": "object"
          },
          "EvidenceCommandSliceRecord": {
            "properties": {
              "argv_summary": {
                "title": "Argv Summary",
                "type": "string"
              },
              "cwd": {
                "title": "Cwd",
                "type": "string"
              },
              "duration_s": {
                "title": "Duration S",
                "type": "number"
              },
              "exit_code": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Exit Code"
              },
              "outcome": {
                "title": "Outcome",
                "type": "string"
              }
            },
            "required": [
              "argv_summary",
              "cwd",
              "duration_s",
              "outcome"
            ],
            "title": "EvidenceCommandSliceRecord",
            "type": "object"
          },
          "FindingRecord": {
            "description": "One reviewer finding flattened from a phase attempt.",
            "properties": {
              "attempt": {
                "title": "Attempt",
                "type": "integer"
              },
              "body": {
                "title": "Body",
                "type": "string"
              },
              "file": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "File"
              },
              "id": {
                "title": "Id",
                "type": "string"
              },
              "line": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Line"
              },
              "phase": {
                "title": "Phase",
                "type": "string"
              },
              "required_fix": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Required Fix"
              },
              "severity": {
                "title": "Severity",
                "type": "string"
              },
              "title": {
                "title": "Title",
                "type": "string"
              }
            },
            "required": [
              "id",
              "severity",
              "title",
              "body",
              "phase",
              "attempt"
            ],
            "title": "FindingRecord",
            "type": "object"
          },
          "HandoffAdviceCallRecord": {
            "description": "One Stage 0/1 advisor invocation, projected from a durable advice artifact.\n\nA 1:1 typed view of one entry in the SDK ``list_handoff_advice`` projection\n(itself a verbatim wrapper over ``collect_handoff_advice``). The outcome\nclassification (``resolved`` / ``repeated`` / ``outcome``) is the\nnormalizer's — this wire model adds no policy. ``advice_artifact`` is the\nrun-relative path to the advice JSON; the usage fields are ``None`` when the\nartifact carried no accounting.\n\n``resolved`` is tri-state: ``True`` (the retry cleared the finding),\n``False`` (it recurred), or ``None`` (unknown / not an applied retry).",
            "properties": {
              "advice_artifact": {
                "title": "Advice Artifact",
                "type": "string"
              },
              "applied_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Applied Action"
              },
              "confidence": {
                "title": "Confidence",
                "type": "string"
              },
              "cost_usd_equivalent": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Cost Usd Equivalent"
              },
              "duration_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Duration S"
              },
              "feedback_source": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Feedback Source"
              },
              "finding_fingerprint": {
                "default": "",
                "title": "Finding Fingerprint",
                "type": "string"
              },
              "handoff_id": {
                "title": "Handoff Id",
                "type": "string"
              },
              "model": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Model"
              },
              "outcome": {
                "title": "Outcome",
                "type": "string"
              },
              "phase": {
                "title": "Phase",
                "type": "string"
              },
              "recommended_action": {
                "title": "Recommended Action",
                "type": "string"
              },
              "repeated": {
                "default": false,
                "title": "Repeated",
                "type": "boolean"
              },
              "resolved": {
                "anyOf": [
                  {
                    "type": "boolean"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Resolved"
              },
              "severity_counts": {
                "additionalProperties": {
                  "type": "integer"
                },
                "title": "Severity Counts",
                "type": "object"
              },
              "tokens_cached": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens Cached"
              },
              "tokens_in": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens In"
              },
              "tokens_out": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens Out"
              },
              "trigger": {
                "title": "Trigger",
                "type": "string"
              },
              "verdict": {
                "title": "Verdict",
                "type": "string"
              }
            },
            "required": [
              "handoff_id",
              "phase",
              "advice_artifact",
              "trigger",
              "verdict",
              "recommended_action",
              "confidence",
              "outcome"
            ],
            "title": "HandoffAdviceCallRecord",
            "type": "object"
          },
          "HandoffAdviceSliceRecord": {
            "description": "Typed projection of a run's Stage 0/1 handoff-advice evidence surface.\n\nFolds the durable ``phase_handoff_advice/`` artifacts, their matching\n``phase_handoff_decisions/`` provenance, and the per-phase verdicts into a\nper-call list plus an aggregate summary. A run with no Stage 0/1 advisor\nsurface yields an empty slice (``calls=[]`` and a zeroed summary), never an\nerror — read-only forensic data the operator can correlate with the\n``orcho_handoff_advice`` recommendation tool.",
            "properties": {
              "calls": {
                "items": {
                  "$ref": "#/$defs/HandoffAdviceCallRecord"
                },
                "title": "Calls",
                "type": "array"
              },
              "summary": {
                "$ref": "#/$defs/HandoffAdviceSummaryRecord"
              }
            },
            "title": "HandoffAdviceSliceRecord",
            "type": "object"
          },
          "HandoffAdviceSummaryRecord": {
            "description": "Run-level rollup over the handoff-advice calls (mirrors the SDK summary).",
            "properties": {
              "applied_retries": {
                "default": 0,
                "title": "Applied Retries",
                "type": "integer"
              },
              "calls": {
                "default": 0,
                "title": "Calls",
                "type": "integer"
              },
              "repeated": {
                "default": 0,
                "title": "Repeated",
                "type": "integer"
              },
              "resolved_retries": {
                "default": 0,
                "title": "Resolved Retries",
                "type": "integer"
              },
              "stopped": {
                "default": 0,
                "title": "Stopped",
                "type": "integer"
              },
              "unknown": {
                "default": 0,
                "title": "Unknown",
                "type": "integer"
              },
              "usage": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/HandoffAdviceUsageRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null
              }
            },
            "title": "HandoffAdviceSummaryRecord",
            "type": "object"
          },
          "HandoffAdviceUsageRecord": {
            "description": "Aggregated advisor usage across a run's handoff-advice calls.\n\nEvery field is ``None`` when no call carried that signal — never a\nfabricated zero (an absent cost is meaningful: cost unknown). Mirrors the\nSDK ``HandoffAdviceUsage`` projection verbatim.",
            "properties": {
              "cost_usd_equivalent": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Cost Usd Equivalent"
              },
              "duration_s": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Duration S"
              },
              "tokens_cached": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens Cached"
              },
              "tokens_in": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens In"
              },
              "tokens_out": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Tokens Out"
              }
            },
            "title": "HandoffAdviceUsageRecord",
            "type": "object"
          },
          "ImplementDeliveryRecord": {
            "description": "First-class typed projection of the implement delivery / waiver audit.\n\nThe delivery/waiver audit fields are surfaced as a typed projection on\nthe ``errors`` evidence slice — its natural home, since the breadcrumbs\narrive in the same errors-rollup. The projector in\n``inspection/evidence.py`` builds this from the already-fetched\n``eh.errors`` list, never from a second raw-meta read, so the typed\nrecord cannot drift from the raw breadcrumbs. ``RunStatus.meta`` carries\nthe same scalar audit fields in its summary projection\n(``meta.phases.implement`` + ``meta.phase_handoff_waiver``), and callers\nthat need the full persisted meta can pass ``include=[\"all\"]``; this\nrecord does not replace it.\n\nSourced by merging two rollup breadcrumb dicts:\n  - ``kind == 'implement_delivery'`` → ``delivery_status``,\n    ``delivery_waived``, ``waiver_id``, ``action``,\n    ``incomplete_subtasks``, ``missing_subtask_receipts``;\n  - ``kind == 'phase_handoff_waiver'`` → ``decided_by``.",
            "properties": {
              "action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "'continue' | 'continue_with_waiver'.",
                "title": "Action"
              },
              "decided_by": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "'operator' | 'auto:on_exhausted'.",
                "title": "Decided By"
              },
              "delivery_status": {
                "description": "'clean' | 'repaired' | 'waived' | 'incomplete'.",
                "title": "Delivery Status",
                "type": "string"
              },
              "delivery_waived": {
                "default": false,
                "title": "Delivery Waived",
                "type": "boolean"
              },
              "incomplete_subtasks": {
                "items": {
                  "type": "string"
                },
                "title": "Incomplete Subtasks",
                "type": "array"
              },
              "missing_subtask_receipts": {
                "items": {
                  "type": "string"
                },
                "title": "Missing Subtask Receipts",
                "type": "array"
              },
              "waiver_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Waiver Id"
              }
            },
            "required": [
              "delivery_status"
            ],
            "title": "ImplementDeliveryRecord",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "PlanSliceRecord": {
            "description": "Compact plan projection — short enough for an LLM context window.",
            "properties": {
              "acceptance_criteria": {
                "items": {
                  "type": "string"
                },
                "title": "Acceptance Criteria",
                "type": "array"
              },
              "commands_to_run": {
                "items": {
                  "type": "string"
                },
                "title": "Commands To Run",
                "type": "array"
              },
              "goal": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Goal"
              },
              "has_contract": {
                "title": "Has Contract",
                "type": "boolean"
              },
              "owned_files": {
                "items": {
                  "type": "string"
                },
                "title": "Owned Files",
                "type": "array"
              },
              "planning_context": {
                "title": "Planning Context",
                "type": "string"
              },
              "review_focus": {
                "items": {
                  "type": "string"
                },
                "title": "Review Focus",
                "type": "array"
              },
              "risks": {
                "items": {
                  "type": "string"
                },
                "title": "Risks",
                "type": "array"
              },
              "short_summary": {
                "title": "Short Summary",
                "type": "string"
              },
              "source": {
                "title": "Source",
                "type": "string"
              },
              "subtask_count": {
                "title": "Subtask Count",
                "type": "integer"
              }
            },
            "required": [
              "source",
              "short_summary",
              "planning_context",
              "subtask_count",
              "has_contract"
            ],
            "title": "PlanSliceRecord",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "SubRunLinkRecord": {
            "properties": {
              "name": {
                "title": "Name",
                "type": "string"
              },
              "run_dir": {
                "title": "Run Dir",
                "type": "string"
              },
              "status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Status"
              }
            },
            "required": [
              "name",
              "run_dir"
            ],
            "title": "SubRunLinkRecord",
            "type": "object"
          },
          "SubtaskReceiptRecord": {
            "description": "One subtask's terminal delivery receipt for a ``subtask_dag`` run.\n\n``state`` is ``\"done\" | \"incomplete\" | \"failed\" | \"skipped\"``.\n``\"incomplete\"`` means execution succeeded but the typed done-criteria\nself-attestation was missing / malformed / mismatched / not-all-met —\ndistinct from a hard ``\"failed\"`` execution error. ``criteria_report`` /\n``attestation_summary`` / ``attestation_error`` carry the P7 attestation\nand are empty / ``None`` for criteria-less subtasks and pre-P7 runs.",
            "properties": {
              "attestation_error": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Why the done-criteria attestation gate did not close, when ``state == 'incomplete'``. None on a clean ``done`` receipt.",
                "title": "Attestation Error"
              },
              "attestation_repaired": {
                "default": false,
                "description": "True when Orcho recovered a malformed machine-readable attestation with one non-mutating repair turn.",
                "title": "Attestation Repaired",
                "type": "boolean"
              },
              "attestation_summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Attestation Summary"
              },
              "criteria_report": {
                "items": {
                  "$ref": "#/$defs/CriterionReportRecord"
                },
                "title": "Criteria Report",
                "type": "array"
              },
              "depends_on": {
                "items": {
                  "type": "string"
                },
                "title": "Depends On",
                "type": "array"
              },
              "done_criteria": {
                "items": {
                  "type": "string"
                },
                "title": "Done Criteria",
                "type": "array"
              },
              "duration": {
                "default": 0.0,
                "title": "Duration",
                "type": "number"
              },
              "error": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Error"
              },
              "model": {
                "default": "",
                "title": "Model",
                "type": "string"
              },
              "runtime": {
                "default": "",
                "title": "Runtime",
                "type": "string"
              },
              "skill": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Skill"
              },
              "state": {
                "title": "State",
                "type": "string"
              },
              "subtask_id": {
                "title": "Subtask Id",
                "type": "string"
              }
            },
            "required": [
              "subtask_id",
              "state"
            ],
            "title": "SubtaskReceiptRecord",
            "type": "object"
          },
          "VerificationAutorunEventRecord": {
            "description": "One durable auto-run firing mirrored from the run's phase log.\n\n``ran_pass`` are commands executed and passing this firing; ``ran_fail``\nthose that failed; ``skipped_fresh`` were already present (fresh receipt,\nnot executed); ``skipped_manual`` were intentionally not auto-run.\n``receipt_paths`` are the durable env / command receipts this firing wrote.",
            "properties": {
              "hook_label": {
                "title": "Hook Label",
                "type": "string"
              },
              "ran_fail": {
                "items": {
                  "type": "string"
                },
                "title": "Ran Fail",
                "type": "array"
              },
              "ran_pass": {
                "items": {
                  "type": "string"
                },
                "title": "Ran Pass",
                "type": "array"
              },
              "receipt_paths": {
                "items": {
                  "type": "string"
                },
                "title": "Receipt Paths",
                "type": "array"
              },
              "skipped_fresh": {
                "items": {
                  "type": "string"
                },
                "title": "Skipped Fresh",
                "type": "array"
              },
              "skipped_manual": {
                "items": {
                  "type": "string"
                },
                "title": "Skipped Manual",
                "type": "array"
              },
              "source": {
                "title": "Source",
                "type": "string"
              }
            },
            "required": [
              "hook_label",
              "source"
            ],
            "title": "VerificationAutorunEventRecord",
            "type": "object"
          },
          "VerificationCheckRecord": {
            "description": "One environment check from a verification-environment receipt.\n\nThe canonical check is ``pipeline_import``: it proves which\n``pipeline/__init__.py`` the phase's interpreter actually imported.\n``expected`` / ``actual`` are absolute paths (``None`` when the check\ncould not be confirmed); ``passed`` is True only when they matched.",
            "properties": {
              "actual": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Actual"
              },
              "expected": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Expected"
              },
              "name": {
                "title": "Name",
                "type": "string"
              },
              "passed": {
                "default": false,
                "title": "Passed",
                "type": "boolean"
              }
            },
            "required": [
              "name"
            ],
            "title": "VerificationCheckRecord",
            "type": "object"
          },
          "VerificationCockpit": {
            "description": "Typed cockpit projection of a run's verification gates.\n\nA read-only, actionable view derived from the SAME SDK call that feeds\n``VerificationTimelineRecord`` — it augments the timeline, never replaces\nor hides it. The header carries contract presence, the (best-effort) work\nmode, the environments seen across gates, an aggregate ``policy_summary``,\nand a human-readable ``effect`` string. ``gates`` is the per-gate cockpit\nrows; the residual / manual-only / inherited aggregates mirror the\ntimeline's so a manual-only ``SKIPPED`` gate never lands in\nresidual missing / failed.\n\n``has_contract`` is ALWAYS set explicitly by the builder from\n``proj.has_contract`` (the default here exists only so the constructor is\nusable). ``mode`` stays ``None`` until core exposes ``work_mode`` through the\ntimeline projection.",
            "properties": {
              "effect": {
                "description": "Human-readable derived effect of the policy aggregate.",
                "title": "Effect",
                "type": "string"
              },
              "envs": {
                "items": {
                  "type": "string"
                },
                "title": "Envs",
                "type": "array"
              },
              "gates": {
                "items": {
                  "$ref": "#/$defs/VerificationGateCockpitRow"
                },
                "title": "Gates",
                "type": "array"
              },
              "has_contract": {
                "default": false,
                "title": "Has Contract",
                "type": "boolean"
              },
              "inherited": {
                "items": {
                  "type": "string"
                },
                "title": "Inherited",
                "type": "array"
              },
              "manual_only": {
                "items": {
                  "type": "string"
                },
                "title": "Manual Only",
                "type": "array"
              },
              "mode": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Mode"
              },
              "policy_summary": {
                "description": "Aggregate of gate policies: 'require' | 'warn' | 'suggest' | 'mixed' | 'none'.",
                "title": "Policy Summary",
                "type": "string"
              },
              "residual_failed": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Failed",
                "type": "array"
              },
              "residual_missing": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Missing",
                "type": "array"
              },
              "residual_stale": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Stale",
                "type": "array"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "suggested_commands": {
                "items": {
                  "type": "string"
                },
                "title": "Suggested Commands",
                "type": "array"
              }
            },
            "required": [
              "run_id",
              "policy_summary",
              "effect"
            ],
            "title": "VerificationCockpit",
            "type": "object"
          },
          "VerificationCommandRecord": {
            "description": "One command a verification-environment receipt recorded.\n\n``argv`` is the exact command line (normalised to a list of strings);\n``exit_code`` is its return code (``None`` when the subprocess could\nnot be launched / timed out).",
            "properties": {
              "argv": {
                "items": {
                  "type": "string"
                },
                "title": "Argv",
                "type": "array"
              },
              "exit_code": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Exit Code"
              }
            },
            "title": "VerificationCommandRecord",
            "type": "object"
          },
          "VerificationGateCockpitRow": {
            "description": "One verification gate as a cockpit row — a typed, actionable\n\nprojection of a single ``VerificationTimelineGateRecord`` that makes the\ngate's *planning* properties (how it fires, who owns it, what it gates on)\nlegible alongside its status, without collapsing any of them.\n\n``trigger`` is derived deterministically (never read from core):\n\n  - ``operator_only`` — the command is in the timeline's ``manual_only``\n    set OR ``policy == 'manual_only'``. A manual / operator-only gate is\n    present here on purpose; ``status='SKIPPED'`` for it is NOT an\n    automation failure and must not be read as missing.\n  - ``auto`` — the command appears in an autorun event's ``ran_pass`` /\n    ``ran_fail`` / ``skipped_fresh`` (the run's automation acted on it).\n  - ``manual`` — neither of the above (a declared gate the automation has\n    not acted on and that is not operator-only).\n\n``class_source`` honours provenance: ``'core'`` ONLY when ``gate_class``\ncame from a durable core field. When the class is inferred locally it is\n``'derived'``; when there is no class signal at all it is ``'unspecified'``.\n``'core'`` must never be claimed for a guessed taxonomy.\n\n``status`` is the same six-value enum as the timeline\n(``PASS`` / ``FAIL`` / ``MISSING`` / ``STALE`` / ``SKIPPED`` / ``FRESH``);\nthere is no ``MANUAL`` status — manual is expressed via\n``trigger`` + ``policy``. ``inherited`` / ``source_run_id`` / ``receipt_path``\ncarry the deciding-receipt evidence; ``stale_reason`` / ``rerun_hint`` are\npopulated where applicable.",
            "properties": {
              "class_source": {
                "default": "unspecified",
                "enum": [
                  "core",
                  "derived",
                  "unspecified"
                ],
                "title": "Class Source",
                "type": "string"
              },
              "command": {
                "title": "Command",
                "type": "string"
              },
              "detail": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable operator note, populated when an environment-provenance break downgrades the gate to ``FAIL`` — names the failing verification_environment check with its expected/actual (e.g. ``pipeline_import: expected <X> actual <Y>``). ``None`` for an ordinary gate.",
                "title": "Detail"
              },
              "env": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Env"
              },
              "gate_class": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Gate Class"
              },
              "hook": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Delivery hook / phase the gate is scheduled at (e.g. ``after_phase(implement)``, ``before_delivery``).",
                "title": "Hook"
              },
              "inherited": {
                "default": false,
                "title": "Inherited",
                "type": "boolean"
              },
              "policy": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Policy"
              },
              "receipt_path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Receipt Path"
              },
              "required": {
                "default": false,
                "title": "Required",
                "type": "boolean"
              },
              "rerun_hint": {
                "items": {
                  "type": "string"
                },
                "title": "Rerun Hint",
                "type": "array"
              },
              "source_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Source Run Id"
              },
              "stale_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Stale Reason"
              },
              "status": {
                "enum": [
                  "PASS",
                  "FAIL",
                  "MISSING",
                  "STALE",
                  "SKIPPED",
                  "FRESH"
                ],
                "title": "Status",
                "type": "string"
              },
              "trigger": {
                "enum": [
                  "auto",
                  "manual",
                  "operator_only"
                ],
                "title": "Trigger",
                "type": "string"
              }
            },
            "required": [
              "command",
              "trigger",
              "status"
            ],
            "title": "VerificationGateCockpitRow",
            "type": "object"
          },
          "VerificationReceiptRecord": {
            "description": "A durable verification-environment receipt.\n\nA developer-side phase (``implement`` / ``repair_changes``) records\n*where* it ran its work and checks: which interpreter (``python``),\nwhich working directory (``cwd``), the import checks it ran, the exact\ncommands + exit codes, and whether any throwaway environment lived\noutside the source checkout (``temp_env_outside_checkout`` — the\nclean-tree note). ``all_passed`` rolls up the checks; ``artifact_path``\nis the on-disk receipt path under the run directory.",
            "properties": {
              "all_passed": {
                "default": false,
                "description": "True when every recorded check passed.",
                "title": "All Passed",
                "type": "boolean"
              },
              "artifact_path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "On-disk path of the receipt JSON under the run directory (``<run_dir>/verification_receipts/...``).",
                "title": "Artifact Path"
              },
              "checks": {
                "items": {
                  "$ref": "#/$defs/VerificationCheckRecord"
                },
                "title": "Checks",
                "type": "array"
              },
              "commands": {
                "items": {
                  "$ref": "#/$defs/VerificationCommandRecord"
                },
                "title": "Commands",
                "type": "array"
              },
              "cwd": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Working directory the phase ran its checks from.",
                "title": "Cwd"
              },
              "kind": {
                "default": "verification_environment",
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Phase"
              },
              "python": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Interpreter identity: ``<version> (<executable>)``.",
                "title": "Python"
              },
              "round": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Round"
              },
              "temp_env_outside_checkout": {
                "default": true,
                "description": "Clean-tree note: True when any throwaway environment lived outside the source checkout (so it cannot pollute ``git status`` in the checkout).",
                "title": "Temp Env Outside Checkout",
                "type": "boolean"
              }
            },
            "title": "VerificationReceiptRecord",
            "type": "object"
          },
          "VerificationTimelineGateRecord": {
            "description": "One required delivery command's official verification-gate state.\n\nProjected verbatim from the SDK's durable\n``sdk.get_verification_timeline`` per-gate record. ``status`` is EXACTLY\none of the six values ``PASS`` / ``FAIL`` / ``MISSING`` / ``STALE`` /\n``SKIPPED`` / ``FRESH`` — there is no ``MANUAL`` value. A manual /\noperator-only gate is surfaced as ``status='SKIPPED'`` with\n``policy='manual_only'`` and membership in the timeline's ``manual_only``,\nnever as missing.\n\n``searched_run_dirs`` and ``rerun_hint`` are populated ONLY for a\nnon-present required gate (``MISSING`` / ``STALE`` / ``FAIL``); present and\nmanual gates carry empty lists. ``inherited`` is True when the deciding\nreceipt came from a parent run (``source_run_id`` differs from this run).",
            "properties": {
              "command": {
                "title": "Command",
                "type": "string"
              },
              "detail": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable operator note, populated when an environment-provenance break downgrades the gate to ``FAIL`` — names the failing verification_environment check with its expected/actual (e.g. ``pipeline_import: expected <X> actual <Y>``). ``None`` for an ordinary gate.",
                "title": "Detail"
              },
              "env": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Env"
              },
              "hook": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Delivery hook the gate is scheduled at (e.g. ``after_phase(implement)``, ``before_delivery``).",
                "title": "Hook"
              },
              "inherited": {
                "default": false,
                "title": "Inherited",
                "type": "boolean"
              },
              "policy": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Effective delivery policy (``require`` / ``warn`` / ``suggest`` / ``manual_only``).",
                "title": "Policy"
              },
              "receipt_path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Receipt Path"
              },
              "required": {
                "default": false,
                "title": "Required",
                "type": "boolean"
              },
              "rerun_hint": {
                "items": {
                  "type": "string"
                },
                "title": "Rerun Hint",
                "type": "array"
              },
              "searched_run_dirs": {
                "items": {
                  "type": "string"
                },
                "title": "Searched Run Dirs",
                "type": "array"
              },
              "source": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Source"
              },
              "source_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Source Run Id"
              },
              "stale_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Stale Reason"
              },
              "status": {
                "enum": [
                  "PASS",
                  "FAIL",
                  "MISSING",
                  "STALE",
                  "SKIPPED",
                  "FRESH"
                ],
                "title": "Status",
                "type": "string"
              }
            },
            "required": [
              "command",
              "status"
            ],
            "title": "VerificationTimelineGateRecord",
            "type": "object"
          },
          "VerificationTimelineRecord": {
            "description": "Typed projection of a run's official verification-gate timeline.\n\nRead-only durable source of truth from ``sdk.get_verification_timeline``:\nper-gate status + remediation (``gates``), the aggregate residual / manual /\ninherited sets, the shared ``suggested_commands`` hints, and the durable\nauto-run events. ``scheduled_trail_available`` is ``False`` until core\npersists a durable per-firing scheduled-gate trail — see GC-10 in\n``docs/ux/flagship_gap_spec.md`` for the exact core follow-up gap.",
            "properties": {
              "autorun_events": {
                "items": {
                  "$ref": "#/$defs/VerificationAutorunEventRecord"
                },
                "title": "Autorun Events",
                "type": "array"
              },
              "gates": {
                "items": {
                  "$ref": "#/$defs/VerificationTimelineGateRecord"
                },
                "title": "Gates",
                "type": "array"
              },
              "has_contract": {
                "default": false,
                "title": "Has Contract",
                "type": "boolean"
              },
              "inherited": {
                "items": {
                  "type": "string"
                },
                "title": "Inherited",
                "type": "array"
              },
              "manual_only": {
                "items": {
                  "type": "string"
                },
                "title": "Manual Only",
                "type": "array"
              },
              "residual_failed": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Failed",
                "type": "array"
              },
              "residual_missing": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Missing",
                "type": "array"
              },
              "residual_stale": {
                "items": {
                  "type": "string"
                },
                "title": "Residual Stale",
                "type": "array"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "scheduled_trail_available": {
                "default": false,
                "title": "Scheduled Trail Available",
                "type": "boolean"
              },
              "searched_run_dirs": {
                "items": {
                  "type": "string"
                },
                "title": "Searched Run Dirs",
                "type": "array"
              },
              "suggested_commands": {
                "items": {
                  "type": "string"
                },
                "title": "Suggested Commands",
                "type": "array"
              }
            },
            "required": [
              "run_id"
            ],
            "title": "VerificationTimelineRecord",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_run_evidence``.\n\nThe ``slice`` field echoes back the requested view; the matching\nfield on the result is populated, others stay ``None``. ``slice=\"all\"``\npopulates every slice for one-shot inspection.\n\nSlices:\n  - ``\"plan\"`` — plan summary (PlanSliceRecord)\n  - ``\"findings\"`` — flattened reviewer findings (list)\n  - ``\"commands\"`` — pipeline shell-outs (list)\n  - ``\"artifacts\"`` — files the run wrote (list)\n  - ``\"errors\"`` — errors + halt reason (ErrorsHaltSliceRecord)\n  - ``\"sub_runs\"`` — cross-run child aliases (list)\n  - ``\"receipts\"`` — per-subtask delivery receipts incl. done-criteria\n    attestation (list of SubtaskReceiptRecord)\n  - ``\"verification_receipts\"`` — durable verification-environment\n    receipts (interpreter / cwd / import checks / commands / exit\n    codes / clean-tree note) (list of VerificationReceiptRecord)\n  - ``\"verification_timeline\"`` — official verification-gate timeline:\n    per-gate status (six-value enum, no MANUAL) with per-gate\n    rerun_hint / searched_run_dirs, the residual / manual-only /\n    inherited aggregates, and the auto-run events\n    (VerificationTimelineRecord)\n  - ``\"verification_cockpit\"`` — typed cockpit projection of the SAME\n    timeline (one SDK read): header (has_contract / mode / envs /\n    policy_summary / effect) plus per-gate cockpit rows that surface\n    trigger (auto / manual / operator_only), policy, requiredness, gate\n    class + provenance, status, and evidence (VerificationCockpit)\n  - ``\"handoff_advice\"`` — Stage 0/1 phase-handoff advisor evidence:\n    per-call records (handoff_id / phase / recommended_action /\n    applied_action / confidence / resolved / repeated / outcome /\n    finding_fingerprint / usage+cost / advice_artifact) plus an aggregate\n    summary (calls / applied_retries / resolved_retries / repeated /\n    stopped / unknown / usage). Empty slice when the run has no advisor\n    surface (HandoffAdviceSliceRecord)\n  - ``\"all\"`` — every slice in one response",
        "properties": {
          "artifacts": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/EvidenceArtifactSliceRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Artifacts"
          },
          "commands": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/EvidenceCommandSliceRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Commands"
          },
          "errors": {
            "anyOf": [
              {
                "$ref": "#/$defs/ErrorsHaltSliceRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          },
          "findings": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/FindingRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Findings"
          },
          "handoff_advice": {
            "anyOf": [
              {
                "$ref": "#/$defs/HandoffAdviceSliceRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          },
          "plan": {
            "anyOf": [
              {
                "$ref": "#/$defs/PlanSliceRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          },
          "receipts": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/SubtaskReceiptRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Receipts"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "slice": {
            "title": "Slice",
            "type": "string"
          },
          "sub_runs": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/SubRunLinkRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Sub Runs"
          },
          "verification_cockpit": {
            "anyOf": [
              {
                "$ref": "#/$defs/VerificationCockpit"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          },
          "verification_receipts": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/VerificationReceiptRecord"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Verification Receipts"
          },
          "verification_timeline": {
            "anyOf": [
              {
                "$ref": "#/$defs/VerificationTimelineRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          }
        },
        "required": [
          "run_id",
          "slice"
        ],
        "title": "EvidenceResult",
        "type": "object"
      }
    },
    {
      "description": "List the most recent runs, newest first.\n\n    Args:\n        limit: max number of runs to return (default 10).\n        project_dir: optional filter — only include runs whose meta.json\n            ``project`` field equals this path. Path is compared as-is (no\n            resolution); pass an absolute path to match how orcho records it.\n    ",
      "inputSchema": {
        "properties": {
          "limit": {
            "default": 10,
            "title": "Limit",
            "type": "integer"
          },
          "project_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Project Dir"
          }
        },
        "title": "orcho_run_historyArguments",
        "type": "object"
      },
      "name": "orcho_run_history",
      "outputSchema": {
        "$defs": {
          "RunRecord": {
            "description": "Compact summary of a single run for history listings.",
            "properties": {
              "project": {
                "title": "Project",
                "type": "string"
              },
              "rounds": {
                "default": 0,
                "title": "Rounds",
                "type": "integer"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "status": {
                "title": "Status",
                "type": "string"
              },
              "task": {
                "title": "Task",
                "type": "string"
              },
              "timestamp": {
                "title": "Timestamp",
                "type": "string"
              },
              "total_duration_s": {
                "default": 0.0,
                "title": "Total Duration S",
                "type": "number"
              },
              "total_tokens": {
                "default": 0,
                "title": "Total Tokens",
                "type": "integer"
              }
            },
            "required": [
              "run_id",
              "project",
              "task",
              "status",
              "timestamp"
            ],
            "title": "RunRecord",
            "type": "object"
          }
        },
        "properties": {
          "runs": {
            "items": {
              "$ref": "#/$defs/RunRecord"
            },
            "title": "Runs",
            "type": "array"
          }
        },
        "required": [
          "runs"
        ],
        "title": "HistoryResult",
        "type": "object"
      }
    },
    {
      "description": "Return a bounded operator-safe live status card for a mono run.\n\n    One typed snapshot that answers \"where is this run right now, and\n    what should I do?\" in a single bounded payload — built for\n    high-frequency polling, with every embedded preview truncated and no\n    full phase bodies, critiques, or raw logs ever riding along. It\n    unites the durable meta status (with supervisor terminal fallback),\n    the live phase/subtask position, the last significant activity, any\n    pending phase-handoff, and terminal consistency — without scraping\n    raw event logs.\n\n    Branch on ``state_class`` (a closed set):\n\n    - ``running_phase`` — executing a phase, no subtask in flight\n      (``current_phase`` set).\n    - ``running_subtask`` — executing a ``subtask_dag`` subtask\n      (``current_subtask`` carries index/total/goal/state).\n    - ``awaiting_handoff`` — paused on a phase-handoff decision\n      (``pending_handoff`` carries handoff id / phase / available actions\n      / default action / verdict / findings summary / recommended action).\n    - ``terminal_success`` — a clean terminal success\n      (``terminal.resume_meaningful`` is ``False`` — resume is inert).\n    - ``terminal_halted`` — a halted / failed / interrupted terminal\n      (``terminal.halt_reason`` set; ``resume_meaningful`` ``True``).\n    - ``terminal_inconsistent`` — a terminal success whose\n      ``final_acceptance`` contradicts it (e.g. ``done`` + ``REJECTED``);\n      the contradiction is surfaced in ``consistency_flags``, never hidden.\n\n    ``next_seq`` is the latest event seq — carry it into\n    ``orcho_run_watch`` / ``orcho_run_events_summary`` to resume\n    observation from here.\n\n    Args:\n        run_id: the run to inspect.\n\n    Errors:\n      - RunNotFoundError — unknown run_id.\n      - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE / $ORCHO_WORKTREE.\n\n    For the windowed event aggregate use ``orcho_run_events_summary``;\n    for raw payload replay use ``orcho_run_events_tail``.\n    ",
      "inputSchema": {
        "properties": {
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_live_statusArguments",
        "type": "object"
      },
      "name": "orcho_run_live_status",
      "outputSchema": {
        "$defs": {
          "CurrentSubtaskRecord": {
            "description": "Live progress coordinate for the in-flight ``subtask_dag`` subtask.\n\nLets a watcher render step-by-step progress (\"subtask 3/12 (Patch\ntarget)\") during a long implement phase without scanning raw events.\nComputed from the latest ``subtask.start`` / ``subtask.end`` event up to\nthe summary horizon; cleared when the implement phase ends. ``None`` on\nthe summary means no subtask is currently in flight.",
            "properties": {
              "goal": {
                "default": "",
                "description": "One-line subtask goal, bounded.",
                "title": "Goal",
                "type": "string"
              },
              "index": {
                "description": "1-based position of this subtask in the whole DAG.",
                "title": "Index",
                "type": "integer"
              },
              "seq": {
                "description": "Event seq of the start/end boundary that produced this snapshot. Advances on every subtask transition — the ``until=\"subtask\"`` watch fires when it passes the caller's cursor.",
                "title": "Seq",
                "type": "integer"
              },
              "state": {
                "description": "\"running\" right after start; \"done\" / \"incomplete\" / \"failed\" once the subtask ends (incomplete = done-criteria attestation gate did not close).",
                "title": "State",
                "type": "string"
              },
              "subtask_id": {
                "title": "Subtask Id",
                "type": "string"
              },
              "total": {
                "description": "Total subtask count in the DAG (the denominator).",
                "title": "Total",
                "type": "integer"
              }
            },
            "required": [
              "subtask_id",
              "index",
              "total",
              "state",
              "seq"
            ],
            "title": "CurrentSubtaskRecord",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "RunLiveActivity": {
            "description": "Compact \"what just happened\" coordinate for the live status card.\n\nProjected from the run's most recent event — bounded the same way a\n``CompactRunEvent`` is, so the high-frequency live-status poll never\nspills raw payload text. ``preview`` is the first non-empty of the\nevent's summary / status / tool, truncated; ``None`` when the event\ncarried nothing human-readable.",
            "properties": {
              "kind": {
                "description": "Kind of the most recent event (e.g. ``phase.start`` / ``subtask.end`` / ``run.end``).",
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase tag of the most recent event, if any.",
                "title": "Phase"
              },
              "preview": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded preview of the event's human-readable text (summary / status / tool). Truncated; never the full raw payload.",
                "title": "Preview"
              },
              "ts": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Timestamp of the most recent event, verbatim.",
                "title": "Ts"
              }
            },
            "required": [
              "kind"
            ],
            "title": "RunLiveActivity",
            "type": "object"
          },
          "RunLiveHandoff": {
            "description": "Compact pending-handoff slice for the live status card.\n\nReuses the existing handoff projections (``project_pending_handoff``\nfor the operator fields + ``build_handoff_hint`` for the\n``default_action`` heuristic and ``findings_summary``) — the\n``meta.phase_handoff`` payload is parsed only in the projection owner,\nnever here. Carries just enough to render the pause at a glance; the\nfull structured decision packet still rides in ``orcho_run_watch``'s\n``HandoffDecisionHint``.",
            "properties": {
              "available_actions": {
                "description": "Decision verbs the runtime published, verbatim.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "default_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Suggested action from the handoff-hint heuristic (``retry_feedback`` > ``continue`` > ``halt`` > ``continue_with_waiver``); ``None`` when none offered.",
                "title": "Default Action"
              },
              "findings_summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded one-line severity+title aggregate of the reviewer findings; ``None`` when no findings.",
                "title": "Findings Summary"
              },
              "handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Handoff identifier from the pending-handoff projection.",
                "title": "Handoff Id"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the handoff.",
                "title": "Phase"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "One-line pointer at the right next tool — decide then resume when no decision exists yet, resume when one already does.",
                "title": "Recommended Action"
              },
              "verdict": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Machine verdict label (``REJECTED`` / ``APPROVED``) carried verbatim from the projection.",
                "title": "Verdict"
              }
            },
            "title": "RunLiveHandoff",
            "type": "object"
          },
          "RunLiveTerminal": {
            "description": "Terminal-state slice for the live status card.\n\nPresent only for a terminal ``state_class`` (``terminal_success`` /\n``terminal_halted`` / ``terminal_inconsistent``). Carries the resolved\n``halt_reason``, the ``final_acceptance`` verdict, whether a resume is\nmeaningful, and any detected consistency violations — so a caller can\nrender a coherent terminal card without scraping logs.",
            "properties": {
              "final_acceptance": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Normalised final-acceptance verdict (``APPROVED`` / ``REJECTED``) from ``meta.phases.final_acceptance``; ``None`` when the run carries no final-acceptance phase.",
                "title": "Final Acceptance"
              },
              "final_acceptance_rejected": {
                "default": false,
                "description": "True when the final-acceptance verdict is a rejection — the explicit rejection flag a halted terminal card surfaces alongside ``halt_reason``.",
                "title": "Final Acceptance Rejected",
                "type": "boolean"
              },
              "halt_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Resolved halt reason (meta + supervisor fallback); ``None`` for a clean terminal success.",
                "title": "Halt Reason"
              },
              "inconsistencies": {
                "description": "Detected terminal contradictions (e.g. a terminal success status while final_acceptance is REJECTED). Empty for a coherent terminal card.",
                "items": {
                  "type": "string"
                },
                "title": "Inconsistencies",
                "type": "array"
              },
              "resume_meaningful": {
                "description": "Whether resuming the run can still make progress — ``True`` for halted / awaiting states, ``False`` for a clean terminal success (resume would be inert).",
                "title": "Resume Meaningful",
                "type": "boolean"
              }
            },
            "required": [
              "resume_meaningful"
            ],
            "title": "RunLiveTerminal",
            "type": "object"
          }
        },
        "description": "Bounded operator-safe live status of a mono run.\n\nA single typed snapshot uniting the durable meta status (with\nsupervisor terminal fallback), the live phase/subtask position, the\nlast significant activity, any pending phase-handoff, and terminal\nconsistency — without raw log scraping. Designed for high-frequency\npolling: every embedded preview is truncated, and no full phase\nbodies / critiques / raw logs ride in the payload.\n\n``state_class`` is the single classification a caller branches on:\n\n- ``running_phase`` — executing a phase, no subtask in flight;\n- ``running_subtask`` — executing a ``subtask_dag`` subtask\n  (``current_subtask`` carries index/total/goal/state);\n- ``awaiting_handoff`` — paused on a phase-handoff decision\n  (``pending_handoff`` populated);\n- ``terminal_success`` — a clean terminal success;\n- ``terminal_halted`` — a halted / failed / interrupted terminal;\n- ``terminal_inconsistent`` — a terminal success whose\n  final_acceptance contradicts it (e.g. ``done`` + ``REJECTED``); the\n  contradiction is surfaced in ``consistency_flags``, never hidden.",
        "properties": {
          "consistency_flags": {
            "description": "Detected status/verdict contradictions surfaced explicitly (e.g. terminal success while final_acceptance is REJECTED). Empty when coherent.",
            "items": {
              "type": "string"
            },
            "title": "Consistency Flags",
            "type": "array"
          },
          "current_phase": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Latest open phase (``phase.start`` not yet closed by a ``phase.end``); ``None`` between phases or once terminal.",
            "title": "Current Phase"
          },
          "current_subtask": {
            "anyOf": [
              {
                "$ref": "#/$defs/CurrentSubtaskRecord"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Live progress coordinate for the in-flight subtask_dag subtask (index/total/goal/state), or ``None`` when no subtask is currently running."
          },
          "last_activity": {
            "anyOf": [
              {
                "$ref": "#/$defs/RunLiveActivity"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Compact projection of the most recent event (kind/ts/phase/bounded preview); ``None`` when the run has no events yet."
          },
          "next_action": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "One-line conservative next-step pointer derived from ``state_class`` only — never invented.",
            "title": "Next Action"
          },
          "next_seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Reconnect cursor: the latest event seq, carry it into ``orcho_run_watch`` / ``orcho_run_events_summary`` to resume observation from here. ``None`` when the run has no events.",
            "title": "Next Seq"
          },
          "pending_handoff": {
            "anyOf": [
              {
                "$ref": "#/$defs/RunLiveHandoff"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Compact pending phase-handoff slice when the run is paused on ``awaiting_phase_handoff``; ``None`` otherwise."
          },
          "provider_pressure": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderPressure"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Core-typed provider runtime/access failure when this terminal card represents a provider-pressure stop, projected from the same ``project_provider_pressure`` source / shared helper as the other surfaces. When set, ``next_action`` reads as a resume-later/inspect pointer, NOT a review/delivery/operator-halt rejection. ``None`` otherwise."
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "state_class": {
            "description": "Single typed classification of the run's live state.",
            "enum": [
              "running_phase",
              "running_subtask",
              "awaiting_handoff",
              "terminal_success",
              "terminal_halted",
              "terminal_inconsistent"
            ],
            "title": "State Class",
            "type": "string"
          },
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Merged run status (meta.json + supervisor terminal fallback). Same value ``orcho_run_status`` returns.",
            "title": "Status"
          },
          "terminal": {
            "anyOf": [
              {
                "$ref": "#/$defs/RunLiveTerminal"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Terminal-state slice (halt_reason / final_acceptance / resume_meaningful / inconsistencies) for a terminal ``state_class``; ``None`` while the run is live."
          }
        },
        "required": [
          "run_id",
          "state_class"
        ],
        "title": "RunLiveStatusCard",
        "type": "object"
      }
    },
    {
      "description": "Raw metrics.json for a single run (token counts, durations, per-phase breakdown).",
      "inputSchema": {
        "properties": {
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_metricsArguments",
        "type": "object"
      },
      "name": "orcho_run_metrics",
      "outputSchema": {
        "properties": {
          "metrics": {
            "additionalProperties": true,
            "title": "Metrics",
            "type": "object"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "metrics"
        ],
        "title": "RunMetrics",
        "type": "object"
      }
    },
    {
      "description": "Drive a single-project pipeline run as a foreground library call.\n\n    Pilot tool that calls orcho-core's typed silent boundary\n    (``run_project_pipeline`` with\n    ``presentation=PresentationPolicy.SILENT, no_interactive=True``)\n    in-process instead of spawning a subprocess. **Blocks until the\n    pipeline completes** and returns a compact\n    :class:`TypedRunResult` carrying ``status``, ``halt_reason``, and\n    the ordered event-spine ``kind`` values.\n\n    Scope:\n      * ``mock=true`` only — pilot does not drive real-provider runs.\n        For real runs use ``orcho_run_start``; it spawns a background\n        subprocess, returns immediately, and supports\n        ``orcho_run_watch`` / ``orcho_run_cancel``.\n      * Single-project; cross-project is not in scope for this pilot.\n      * Best for short flows: mock smokes, fixture generation,\n        integration scaffolding.\n\n    Inputs:\n      ``task``: inline task description (non-empty).\n      ``project_dir``: absolute or MCP-cwd-relative project root.\n      ``output_dir``: absolute path where ``meta.json`` / ``events.jsonl``\n                      / ``progress.log`` will land.\n      ``profile``: pipeline profile name (default ``\"task\"`` — an\n                   internal scoped profile, not a public work-kind\n                   choice; used here only because it is the leanest\n                   mock-completable shape for this pilot. For real\n                   runs select a semantic profile via\n                   ``orcho_run_start``).\n      ``mock``: must be ``True`` (pilot constraint; see scope above).\n      ``max_rounds``: rounds budget; default ``1``.\n\n    Response shape (compact on purpose — call ``orcho_run_status`` /\n    ``orcho_run_events_tail`` with the returned ``run_id`` for the\n    full surface):\n\n      ``run_id``: pipeline session identifier.\n      ``output_dir``: absolute path to the run directory.\n      ``status``: ``done`` / ``failed`` / ``awaiting_phase_handoff`` /\n                  ``halted`` — read from the persisted session, never\n                  parsed from any transcript.\n      ``halt_reason``: structured halt reason; ``None`` on the done path.\n      ``event_kinds``: ordered ``kind`` values from ``events.jsonl``;\n                       the canonical spine is ``run.start`` →\n                       (``phase.start`` / ``phase.end``)+ →\n                       ``run.end``. Presence under SILENT proves the\n                       event sink stayed wired.\n\n    No stdout / stderr is captured or parsed by this tool. Status is\n    pulled from the in-memory session dict; events come from the file\n    sink. The MCP stdio transport invariant (no stdout pollution from\n    handlers) is upheld by the boundary's SILENT contract.\n    ",
      "inputSchema": {
        "properties": {
          "max_rounds": {
            "default": 1,
            "title": "Max Rounds",
            "type": "integer"
          },
          "mock": {
            "default": true,
            "title": "Mock",
            "type": "boolean"
          },
          "output_dir": {
            "title": "Output Dir",
            "type": "string"
          },
          "profile": {
            "default": "task",
            "title": "Profile",
            "type": "string"
          },
          "project_dir": {
            "title": "Project Dir",
            "type": "string"
          },
          "task": {
            "title": "Task",
            "type": "string"
          }
        },
        "required": [
          "task",
          "project_dir",
          "output_dir"
        ],
        "title": "orcho_run_project_typedArguments",
        "type": "object"
      },
      "name": "orcho_run_project_typed",
      "outputSchema": {
        "description": "Returned by ``orcho_run_project_typed``.\n\nCompact response from a *foreground* run driven via the orcho-core\ntyped silent boundary (``pipeline.project.app.run_project_pipeline``\nwith ``presentation=SILENT, no_interactive=True``). Unlike the\nsupervisor-backed ``orcho_run_start`` (which returns immediately\nand runs in the background), this tool **blocks** until the run\ncompletes and returns the result in one call.\n\nThe pilot is intentionally scoped to short, mock-provider runs.\nLong-running real-provider runs continue to use\n``orcho_run_start`` + ``orcho_run_watch`` / ``orcho_run_status``\nso the MCP client can stream progress and cancel.\n\nThe response stays compact on purpose. Callers that want the persisted\nsession summary / metrics / events follow up with the standard read tools\nusing the returned ``run_id``:\n\n  * ``orcho_run_status(run_id)`` — summary meta + metrics snapshot\n    (``include=[\"all\"]`` restores full persisted meta).\n  * ``orcho_run_metrics(run_id)`` — phase / cost breakdown.\n  * ``orcho_run_events_tail(run_id)`` — full events stream.",
        "properties": {
          "event_kinds": {
            "description": "Ordered list of event ``kind`` values from ``events.jsonl``. The canonical spine is ``run.start`` → (``phase.start`` / ``phase.end``)+ → ``run.end``; presence under SILENT proves the file + event sinks stayed wired. Full event payloads are available via ``orcho_run_events_tail``.",
            "items": {
              "type": "string"
            },
            "title": "Event Kinds",
            "type": "array"
          },
          "halt_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Structured halt reason. Present on every non-``done`` terminal status; ``None`` when ``status == 'done'``.",
            "title": "Halt Reason"
          },
          "output_dir": {
            "description": "Absolute path to the run directory on disk. The same directory backs ``orcho_run_status`` reads for this run.",
            "title": "Output Dir",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "description": "Final pipeline status: ``done`` / ``failed`` / ``awaiting_phase_handoff`` / ``halted`` — pulled directly from the persisted session, not parsed from any transcript.",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "output_dir",
          "status"
        ],
        "title": "TypedRunResult",
        "type": "object"
      }
    },
    {
      "description": "Spawn a typed silent run in the background; return run_id immediately.\n\n    Non-blocking sibling to ``orcho_run_project_typed``. The pipeline\n    body executes inside ``asyncio.to_thread`` so the MCP server's\n    event loop stays responsive while the run is in flight. Returns\n    a :class:`TypedRunStartedResult` with ``status=\"running\"``; the\n    final state is discovered by polling the existing read tools.\n\n    Workspace-aware: the run directory lives at\n    ``<workspace>/runspace/runs/<run_id>/`` (workspace resolved via\n    ``$ORCHO_WORKSPACE`` or walk-up). The directory name **is** the\n    ``run_id``, so the standard read tools resolve the run by id:\n\n      * ``orcho_run_status(run_id)`` — summary meta + metrics; flips to\n        ``done`` / ``failed`` / ``awaiting_phase_handoff`` once the\n        background task settles.\n      * ``orcho_run_events_tail(run_id)`` — incremental events.\n      * ``orcho_run_metrics(run_id)`` — phase / cost breakdown.\n\n    Differences vs ``orcho_run_start`` (subprocess-backed):\n      * In-process execution via the orcho-core typed silent\n        boundary; no detached subprocess, no pid, no signal-based\n        cancel surface for this pilot.\n      * Mock provider only. Real-provider runs continue to use\n        ``orcho_run_start``.\n      * No ``progressToken`` notifications yet — observation is\n        polling-based against the existing read tools.\n\n    Differences vs ``orcho_run_project_typed`` (blocking sibling):\n      * Returns immediately rather than blocking until completion.\n      * Derives ``output_dir`` from the workspace instead of taking\n        it as an input; the blocking sibling keeps the explicit\n        ``output_dir`` shape for callers that own the path layout\n        themselves.\n\n    Pilot constraints: one async run in flight at a time per MCP\n    server process (the ``ORCHO_RUN_ID`` env var is serialized at\n    start). Concurrent async runs are part of the future widening\n    story, not this slice.\n    ",
      "inputSchema": {
        "properties": {
          "max_rounds": {
            "default": 1,
            "title": "Max Rounds",
            "type": "integer"
          },
          "mock": {
            "default": true,
            "title": "Mock",
            "type": "boolean"
          },
          "profile": {
            "default": "task",
            "title": "Profile",
            "type": "string"
          },
          "project_dir": {
            "title": "Project Dir",
            "type": "string"
          },
          "task": {
            "title": "Task",
            "type": "string"
          }
        },
        "required": [
          "task",
          "project_dir"
        ],
        "title": "orcho_run_project_typed_asyncArguments",
        "type": "object"
      },
      "name": "orcho_run_project_typed_async",
      "outputSchema": {
        "description": "Returned by ``orcho_run_project_typed_async``.\n\nCompact start-acknowledgement from the **non-blocking** typed\nsilent pilot path. The pipeline runs in a background asyncio task\nvia ``asyncio.to_thread``; this response surfaces the run handle\nimmediately so the MCP client can begin polling.\n\nThe blocking sibling ``orcho_run_project_typed`` returns\n:class:`TypedRunResult` with the final status in one round-trip;\nthe async path returns ``status=\"running\"`` here and the caller\nfollows up with the standard read tools to observe progress and\ncompletion:\n\n  * ``orcho_run_status(run_id)`` — summary meta + metrics; flips to\n    ``done`` / ``failed`` / ``awaiting_phase_handoff`` when the\n    background task settles the file sink.\n  * ``orcho_run_events_tail(run_id)`` — incremental events\n    (``run.start`` → ``phase.start`` / ``phase.end`` →\n    ``run.end``).\n\nThe async path is workspace-aware: it derives ``output_dir``\nunder ``<workspace>/runspace/runs/<run_id>/`` so the existing\nread tools resolve the run by ``run_id`` through the same path\nas supervisor-backed runs. The blocking sibling keeps its\nexplicit-``output_dir`` shape; both can coexist.",
        "properties": {
          "output_dir": {
            "description": "Absolute path to the run directory. Equals ``<workspace>/runspace/runs/<run_id>/`` — the same path the workspace-aware SDK resolves for ``run_id``.",
            "title": "Output Dir",
            "type": "string"
          },
          "run_id": {
            "description": "Pipeline session identifier — the directory name under ``<workspace>/runspace/runs/``. Passed to the existing read tools (``orcho_run_status`` / ``orcho_run_events_tail``) for status polling.",
            "title": "Run Id",
            "type": "string"
          },
          "started_at": {
            "description": "ISO 8601 UTC timestamp when the background task was scheduled.",
            "title": "Started At",
            "type": "string"
          },
          "status": {
            "description": "Always ``running`` at this point. Final status surfaces via ``orcho_run_status(run_id)`` after the background task completes.",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "output_dir",
          "status",
          "started_at"
        ],
        "title": "TypedRunStartedResult",
        "type": "object"
      }
    },
    {
      "description": "Continue an interrupted run by spawning a new pipeline subprocess\n    that loads the existing checkpoint via ``--resume``.\n\n    ``runtime_override=None`` (default): a plain checkpoint resume — the run\n    continues under its original per-phase runtimes/models. This is the\n    overwhelmingly common case.\n\n    ``runtime_override={phase, runtime, model}``: deliver an operator\n    *replace* decision after a terminal provider-access failure.\n    Before the resume subprocess spawns, the chosen pair is validated against\n    the phase's configured replacement candidates and persisted into the run's\n    durable ``meta.json``; the resumed pipeline re-reads that record and applies\n    the override to exactly the named phase. A non-candidate pair aborts the\n    resume (no silent fallback). Pass the value verbatim from the *replace*\n    ``next_actions`` Action surfaced by ``orcho_run_status`` /\n    ``orcho_run_evidence`` on a provider-access failure.\n\n    ``profile=None`` (default): inherit the original run's profile from\n    ``meta.profile``. This is the right default for most callers —\n    review and final-acceptance prompt envelopes depend on the active\n    profile (``feature`` carries the full plan/validate envelope;\n    leaner scoped profiles do not), and resuming under a different\n    profile silently changes which prompt parts the reviewer agent\n    sees. Runs without a recorded profile fall back to the semantic\n    default ``\"feature\"``.\n\n    ``profile=\"<name>\"`` is a deliberate profile switch — e.g. pass\n    ``\"small_task\"`` for a lean scoped continuation, or ``\"planning\"``\n    to refine the plan only.\n\n    A pre-flight guard classifies the run before spawning, so the typed\n    ``resume_outcome`` carries one of these outcomes (the success\n    ``outputSchema`` is unchanged — an inspect-only run is signalled via a\n    typed error, see below, never as a success member):\n\n    - ``applied`` (:class:`RunResumeResult`) — a resumable run (running\n      restart / ``failed`` / ``interrupted`` / non-terminal ``halted``)\n      spawned a fresh subprocess; ``pid`` / ``run_dir`` / ``started_at`` /\n      ``command`` are populated. ``suggested_next_action`` is a ready\n      ``ready_call`` to ``orcho_run_watch`` (pre-filled with ``run_id``) so\n      the client can re-enter the watch loop on the resumed subprocess.\n    - ``pending_decision`` (:class:`ResumePendingDecisionResult`) — paused\n      on ``awaiting_phase_handoff`` with no recorded decision; resolve it\n      with ``orcho_phase_handoff_decide`` first, then resume.\n    - ``superseded_by_child`` (:class:`ResumeBlockedResult`) — a newer\n      unfinished follow-up child is continuing this run; resume that child\n      (``recommended_run_id``) instead of this parent. No subprocess spawns.\n    - ``rejected_terminal`` (:class:`ResumeBlockedResult`) — the run is\n      terminal; resuming is inert, so this is **not** success-shaped (no\n      ``pid``). Inspect via ``orcho_run_status`` / ``orcho_run_evidence``\n      instead. No subprocess spawns.\n\n    A run that was NOT started by this MCP server (no durable\n    ``mcp_supervisor.json`` with a resolvable ``project_dir`` —\n    ``control='inspect_only'`` on ``orcho_run_diagnose``) is refused BEFORE the\n    supervisor is touched: it raises ``InspectOnlyControlError`` rather than\n    returning, so the success ``outputSchema`` above is unchanged. MCP cannot\n    resume such a foreign / CLI-started run; manage it through the CLI that\n    started it and, from MCP, inspect it via ``orcho_run_status`` /\n    ``orcho_run_evidence``. Read the typed ``control`` / ``control_reason``\n    classification first with ``orcho_run_diagnose``.\n\n    Errors:\n      - InspectOnlyControlError — the run is ``inspect_only`` (not started by\n        this MCP server); resume is refused before any spawn. The carried\n        ``result`` holds the typed read-only next actions.\n      - RunNotFoundError — only when ``run_id`` is genuinely unresolvable /\n        corrupt (a foreign run dir is classified ``inspect_only`` above, not\n        raised).\n    ",
      "inputSchema": {
        "$defs": {
          "RuntimeOverrideArg": {
            "additionalProperties": false,
            "description": "Operator runtime/model override for ``orcho_run_resume``.\n\nDelivers the operator's *replace* decision after a terminal provider-access\nfailure: switch the named phase to a different configured ``(runtime,\nmodel)`` pair and resume. The shape mirrors\n``sdk.run_control.types.RuntimeOverride`` in orcho-core; the resume path\nvalidates the pair against the configured replacement candidates and\npersists it into the run's durable ``meta.json`` *before* spawning the\nresume subprocess, so a non-candidate pair aborts the resume rather than\nsilently falling back.\n\nThe candidate ``(runtime, model)`` pairs are exactly the ones the SDK\n``next_actions`` *replace* Action carries (``orcho_run_resume`` with\n``runtime_override``); callers should pass a value taken from that Action\nrather than inventing a pair.",
            "properties": {
              "model": {
                "description": "The replacement model id paired with ``runtime`` in the configured candidate set.",
                "title": "Model",
                "type": "string"
              },
              "phase": {
                "description": "The pipeline phase whose runtime/model is overridden (e.g. ``plan`` / ``implement`` / ``review_changes``). The override is isolated to this phase.",
                "title": "Phase",
                "type": "string"
              },
              "runtime": {
                "description": "The replacement runtime name — must be a registered runtime and part of a configured replacement candidate pair for the phase.",
                "title": "Runtime",
                "type": "string"
              }
            },
            "required": [
              "phase",
              "runtime",
              "model"
            ],
            "title": "RuntimeOverrideArg",
            "type": "object"
          }
        },
        "properties": {
          "profile": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Profile"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "runtime_override": {
            "anyOf": [
              {
                "$ref": "#/$defs/RuntimeOverrideArg"
              },
              {
                "type": "null"
              }
            ],
            "default": null
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_resumeArguments",
        "type": "object"
      },
      "name": "orcho_run_resume",
      "outputSchema": {
        "$defs": {
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "ResumeBlockedResult": {
            "description": "Returned by ``orcho_run_resume`` when resume is refused before spawn.\n\nCarries **no** spawn fields (no ``pid`` / ``run_dir`` / ``command`` /\n``started_at``) — the supervisor was never asked to resume. Three typed\noutcomes share this shape, distinguished by ``resume_outcome``:\n\n- ``rejected_terminal`` — the run is terminal (terminal success or a\n  terminal halt reason) with no resumable lineage subject; resuming is\n  inert. ``next_actions`` point only at read-only inspection\n  (``orcho_run_status`` / ``orcho_run_evidence`` ``slice='errors'``),\n  never a resume. ``recommended_run_id`` stays ``None`` (a terminal run\n  has no resume target).\n- ``superseded_by_child`` — a newer unfinished follow-up child is\n  continuing this run's change session; ``recommended_run_id`` is that\n  child and the single ``ready_call`` next action resumes the child\n  instead of this parent.\n- ``recover_via_source_run`` — this run is a terminal / rejected recovery\n  run, but durable lineage points at a *resumable source* run that still\n  owns the retained checkpoint / worktree. ``recommended_run_id`` is that\n  source and the single ``ready_call`` next action resumes the source\n  instead of spawning a no-op resume against this inert run.",
            "properties": {
              "kind": {
                "const": "resume_blocked",
                "default": "resume_blocked",
                "title": "Kind",
                "type": "string"
              },
              "message": {
                "description": "Operator-facing explanation of why the resume was refused and what to do instead.",
                "title": "Message",
                "type": "string"
              },
              "next_actions": {
                "description": "Typed follow-up calls — every record is a ``ready_call`` carrying all required args: resume-the-child for ``superseded_by_child``, read-only inspection (``orcho_run_status`` / ``orcho_run_evidence``) for ``rejected_terminal``. Never a resume of a terminal run.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "reason": {
                "description": "One-line factual reason from the run diagnosis (status / halt_reason / lineage ids), never parsed from log prose.",
                "title": "Reason",
                "type": "string"
              },
              "recommended_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The run to resume instead of this one: the active follow-up child for ``superseded_by_child``, or the resumable source run for ``recover_via_source_run``. ``None`` for ``rejected_terminal`` (a terminal run with no resumable lineage subject has no resume target).",
                "title": "Recommended Run Id"
              },
              "resume_outcome": {
                "description": "Typed reason the resume was refused before spawning.",
                "enum": [
                  "rejected_terminal",
                  "superseded_by_child",
                  "recover_via_source_run"
                ],
                "title": "Resume Outcome",
                "type": "string"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "status": {
                "description": "Merged run status at interception time.",
                "title": "Status",
                "type": "string"
              },
              "suggested_next_action": {
                "description": "One-line pointer at the recommended next step.",
                "title": "Suggested Next Action",
                "type": "string"
              }
            },
            "required": [
              "run_id",
              "resume_outcome",
              "status",
              "reason",
              "message",
              "suggested_next_action"
            ],
            "title": "ResumeBlockedResult",
            "type": "object"
          },
          "ResumePendingDecisionResult": {
            "description": "Returned by ``orcho_run_resume`` when the run is still paused.\n\nA run paused on ``awaiting_phase_handoff`` cannot advance until the\noperator records a phase-handoff decision. Calling ``orcho_run_resume``\nbefore that decision exists used to surface a raw SDK error; this\nstructured response is returned instead so the agent gets an\nactionable next step (``orcho_phase_handoff_decide``) rather than an\nopaque traceback.\n\nThe success path of ``orcho_run_resume`` returns\n:class:`RunResumeResult`; the terminal / superseded refusals return\n:class:`ResumeBlockedResult`. This shape is the third arm of the\nunion, which is why the tool's return type spans all three.",
            "properties": {
              "available_actions": {
                "description": "Decision verbs the runtime published for this handoff (``continue`` / ``retry_feedback`` / ``halt`` / ``continue_with_waiver``), verbatim.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "decision_artifact_exists": {
                "default": false,
                "description": "Always ``False`` for this response — the resume was intercepted precisely because no decision artifact exists yet.",
                "title": "Decision Artifact Exists",
                "type": "boolean"
              },
              "handoff_id": {
                "description": "Active handoff id the operator must decide on, from ``meta.phase_handoff.id``.",
                "title": "Handoff Id",
                "type": "string"
              },
              "issue": {
                "description": "Human-readable explanation of why the run could not resume and what the operator must do first.",
                "title": "Issue",
                "type": "string"
              },
              "kind": {
                "const": "pending_phase_handoff_decision",
                "default": "pending_phase_handoff_decision",
                "title": "Kind",
                "type": "string"
              },
              "next_actions": {
                "description": "Suggested follow-up tool calls — a single non-optional ``orcho_phase_handoff_decide`` action pre-filled with ``run_id`` and ``handoff_id`` (the operator chooses ``action`` / ``feedback``).",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the pending handoff, when known.",
                "title": "Phase"
              },
              "resume_outcome": {
                "const": "pending_decision",
                "default": "pending_decision",
                "description": "Typed outcome discriminator — always ``pending_decision`` for this shape; the run is paused awaiting an operator phase-handoff decision.",
                "title": "Resume Outcome",
                "type": "string"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "status": {
                "default": "awaiting_phase_handoff",
                "description": "Merged run status at interception time — always ``awaiting_phase_handoff`` for this response.",
                "title": "Status",
                "type": "string"
              },
              "suggested_next_action": {
                "description": "One-line pointer at the decide-then-resume path.",
                "title": "Suggested Next Action",
                "type": "string"
              }
            },
            "required": [
              "run_id",
              "handoff_id",
              "issue",
              "suggested_next_action"
            ],
            "title": "ResumePendingDecisionResult",
            "type": "object"
          },
          "RunResumeResult": {
            "description": "Returned by ``orcho_run_resume`` when the resume is actually applied.\n\nA subclass of :class:`RunStartedResult` (same spawn handle: ``run_id`` /\n``run_dir`` / ``pid`` / ``started_at`` / ``project_dir`` / ``command``)\nplus the typed ``resume_outcome='applied'`` discriminator. The\npre-flight guard reaches this spawn only when the run is genuinely\nresumable (running-restart, ``failed``, ``interrupted``, or a\nnon-terminal ``halted``). A terminal or superseded run never spawns and\nreturns :class:`ResumeBlockedResult` instead, so a non-applied resume is\nnever success-shaped (no ``pid`` on a no-op).",
            "properties": {
              "command": {
                "description": "Subprocess argv (for diagnostics — clients should not need to inspect).",
                "items": {
                  "type": "string"
                },
                "title": "Command",
                "type": "array"
              },
              "message": {
                "description": "Human-readable confirmation that the resume spawned a new subprocess loading the existing checkpoint.",
                "title": "Message",
                "type": "string"
              },
              "next_actions": {
                "description": "Suggested follow-up tool calls. After a successful ``orcho_run_start`` spawn this carries a single ready ``ready_call`` to ``orcho_run_watch`` (``args`` pre-filled with ``run_id``), so the client can enter the watch loop immediately and be woken on the next event / phase change / handoff / terminal. The applied ``orcho_run_resume`` shape (:class:`RunResumeResult`) carries its ready watch pointer on ``suggested_next_action`` instead and leaves this list empty.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "pid": {
                "title": "Pid",
                "type": "integer"
              },
              "project_dir": {
                "title": "Project Dir",
                "type": "string"
              },
              "resume_outcome": {
                "const": "applied",
                "default": "applied",
                "description": "Typed outcome discriminator — always ``applied`` for this shape; the supervisor spawned a fresh checkpoint-loading subprocess.",
                "title": "Resume Outcome",
                "type": "string"
              },
              "run_dir": {
                "title": "Run Dir",
                "type": "string"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "started_at": {
                "title": "Started At",
                "type": "string"
              },
              "suggested_next_action": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/NextActionRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional single follow-up pointer. On an applied resume this is a ready ``ready_call`` to ``orcho_run_watch`` (``args`` pre-filled with ``run_id``) so the client can re-enter the watch loop on the freshly-spawned subprocess; ``None`` when no specific follow-up is warranted at spawn time."
              }
            },
            "required": [
              "run_id",
              "run_dir",
              "pid",
              "started_at",
              "project_dir",
              "command",
              "message"
            ],
            "title": "RunResumeResult",
            "type": "object"
          }
        },
        "properties": {
          "result": {
            "anyOf": [
              {
                "$ref": "#/$defs/RunResumeResult"
              },
              {
                "$ref": "#/$defs/ResumeBlockedResult"
              },
              {
                "$ref": "#/$defs/ResumePendingDecisionResult"
              }
            ],
            "title": "Result"
          }
        },
        "required": [
          "result"
        ],
        "title": "orcho_run_resumeOutput",
        "type": "object"
      }
    },
    {
      "description": "Spawn an orcho pipeline run in the background; return run_id immediately.\n\n    The run executes asynchronously in a detached subprocess. For live\n    status, prefer ``orcho_run_watch`` — it holds the request open and\n    returns the moment something meaningful changes (next event, phase\n    change, handoff, or terminal), and emits ordered\n    ``notifications/progress`` when the request carries a\n    ``progressToken``. The response's ``next_actions`` already carries a\n    ready ``ready_call`` to ``orcho_run_watch`` pre-filled with the new\n    ``run_id``, so the client can enter that watch loop immediately\n    without re-deriving the call. For one-shot polling,\n    ``orcho_run_status``, ``orcho_run_events_summary``,\n    ``orcho_run_metrics``, and ``orcho_run_events_tail`` (or the matching\n    ``orcho://runs/{run_id}/...`` resources) all work without blocking.\n\n    Provide either ``task`` (inline string) or ``task_file`` (markdown\n    path). ``project_dir`` accepts an absolute path or a path relative\n    to the MCP server's working directory (e.g. ``\"orcho-web\"`` from the\n    workspace root, or ``\".\"`` for the current directory); it is resolved\n    once at spawn time and the absolute result is used for both the\n    subprocess cwd and the orchestrator's ``--project`` flag. A missing\n    or non-directory path fails fast with ``PipelineSpawnError`` rather\n    than dying inside the subprocess. ``mock=true`` runs the full\n    pipeline flow with fake agents (no LLM tokens consumed) — recommended\n    for first invocations.\n    ``profile`` selects the active pipeline profile by semantic work\n    kind (``feature``, ``small_task``, ``complex_feature``,\n    ``planning``, ``code_review``, …); default ``\"feature\"``.\n    ``profile=\"auto-detect\"`` is a supported *selector* value (not an\n    executable recipe): core classifies the task, picks the matching\n    semantic work kind, and in this non-interactive surface proceeds\n    with the chosen profile when confidence is sufficient (otherwise it\n    falls back to the default profile). The decision is read back\n    typed via ``orcho_run_status`` — the ``auto_detect`` projection\n    carries ``requested_selector`` (the ``auto-detect`` token), the\n    selected / recommended profile + mode, ``detection_state``, and a\n    deterministic ``next_action``. ``orcho_profiles_list`` lists\n    selectors separately from executable profiles under ``selectors``.\n\n    ``output_mode`` controls subprocess transcript verbosity:\n    ``\"summary\"`` (default), ``\"live\"``, or ``\"debug\"``.\n    ``session_mode`` controls implement → repair_changes provider-session\n    continuation: ``\"auto\"`` (default), ``\"stateless\"``, ``\"chain\"``, or\n    ``\"hybrid\"``.\n\n    Pause semantics are driven by each phase's declared ``handoff``\n    policy in the active profile (e.g. ``feature`` declares\n    ``human_feedback_on_reject`` on ``validate_plan``). When the\n    pipeline pauses, ``meta.status`` becomes ``awaiting_phase_handoff``\n    and the subprocess exits rc=4; the caller resolves the pause via\n    ``orcho_phase_handoff_decide`` and follows up with\n    ``orcho_run_resume`` for ``continue`` / ``retry_feedback`` /\n    ``continue_with_waiver`` actions. ``halt`` is terminal and needs no\n    resume.\n\n    Attachments:\n        ``attach``: paths whose kind is auto-detected from extension.\n        ``attach_text`` / ``attach_image`` / ``attach_binary``: paths\n        whose kind is forced. All four parameters are repeatable; each\n        path becomes one ``Attachment`` threaded into the agent's\n        prompt context. TEXT attachments inject as XML-style block;\n        IMAGE / BINARY currently flow through ``state.attachments``.\n\n    From-run-plan follow-up:\n        ``from_run_plan`` (optional): parent run id or absolute path\n        whose ``parsed_plan.json`` the new run inherits. When set:\n          * loads the parent's parsed plan via the typed artefact\n            loader — no markdown round-trip;\n          * projects the selected ``profile`` to drop the leading\n            plan + validate_plan block, so the child run starts at\n            implement with ``state.parsed_plan`` already hydrated;\n          * stamps ``plan_source=\"run\"`` + ``plan_source_run_id``\n            on the child's meta.json for child → parent correlation.\n        The parent run must contain ``parsed_plan.json``; otherwise\n        the call fails with an actionable diagnostic.\n        Useful when an operator wants to iterate on implementation\n        without re-running the planning phases against the same task.\n        Distinct from ``orcho_run_resume`` — this spawns a NEW run\n        that inherits a parent's plan, while ``orcho_run_resume``\n        continues the SAME run from its checkpoint.\n    ",
      "inputSchema": {
        "properties": {
          "attach": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Attach"
          },
          "attach_binary": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Attach Binary"
          },
          "attach_image": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Attach Image"
          },
          "attach_text": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Attach Text"
          },
          "from_run_plan": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "From Run Plan"
          },
          "max_rounds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Max Rounds"
          },
          "mock": {
            "default": false,
            "title": "Mock",
            "type": "boolean"
          },
          "mock_validate_plan_reject": {
            "default": 0,
            "title": "Mock Validate Plan Reject",
            "type": "integer"
          },
          "output_mode": {
            "default": "summary",
            "enum": [
              "summary",
              "live",
              "debug"
            ],
            "title": "Output Mode",
            "type": "string"
          },
          "profile": {
            "default": "feature",
            "title": "Profile",
            "type": "string"
          },
          "project_dir": {
            "default": ".",
            "title": "Project Dir",
            "type": "string"
          },
          "session_mode": {
            "default": "auto",
            "enum": [
              "auto",
              "stateless",
              "chain",
              "hybrid"
            ],
            "title": "Session Mode",
            "type": "string"
          },
          "task": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Task"
          },
          "task_file": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Task File"
          }
        },
        "title": "orcho_run_startArguments",
        "type": "object"
      },
      "name": "orcho_run_start",
      "outputSchema": {
        "$defs": {
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_run_start`` and ``orcho_run_resume``.\n\nThe pipeline subprocess starts in the background; the run_id, run_dir,\npid, and started_at are returned immediately so the client can begin\ntracking via ``orcho_run_status`` / ``orcho_run_events_tail`` / progress\nnotifications without waiting for completion.",
        "properties": {
          "command": {
            "description": "Subprocess argv (for diagnostics — clients should not need to inspect).",
            "items": {
              "type": "string"
            },
            "title": "Command",
            "type": "array"
          },
          "next_actions": {
            "description": "Suggested follow-up tool calls. After a successful ``orcho_run_start`` spawn this carries a single ready ``ready_call`` to ``orcho_run_watch`` (``args`` pre-filled with ``run_id``), so the client can enter the watch loop immediately and be woken on the next event / phase change / handoff / terminal. The applied ``orcho_run_resume`` shape (:class:`RunResumeResult`) carries its ready watch pointer on ``suggested_next_action`` instead and leaves this list empty.",
            "items": {
              "$ref": "#/$defs/NextActionRecord"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "pid": {
            "title": "Pid",
            "type": "integer"
          },
          "project_dir": {
            "title": "Project Dir",
            "type": "string"
          },
          "run_dir": {
            "title": "Run Dir",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "started_at": {
            "title": "Started At",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "run_dir",
          "pid",
          "started_at",
          "project_dir",
          "command"
        ],
        "title": "RunStartedResult",
        "type": "object"
      }
    },
    {
      "description": "Summary snapshot for a single run — status, metrics, lineage, next steps.\n\n    Raises RunNotFoundError if ``run_id`` doesn't exist on disk.\n\n    Merges supervisor truth into ``meta.status`` when the pipeline exits\n    before it can update ``meta.json``. The pipeline owns ``meta.json``;\n    the supervisor owns ``mcp_supervisor.json``. When meta status is\n    missing or stuck on ``running`` while the supervisor knows the\n    process is dead, this tool surfaces the supervisor's terminal\n    status so wire consumers see one consistent value.\n\n    **Summary-only by default.** This tool is the supervisor's\n    highest-frequency poll, so ``meta`` is projected to a bounded shape:\n    top-level scalars and gate verdicts pass through, but the heavy phase\n    *bodies* are elided. Inside ``meta.phases`` you get size / count\n    markers instead of full text — e.g. ``plan[i].output_chars`` (not the\n    plan markdown), ``implement.output_chars``, ``validate_plan[i]``\n    keeps ``verdict`` + ``findings_count`` (not the critique), and\n    implementation receipts collapse to ``subtask_id`` + ``state``. The\n    long task text remains in ``meta.task`` but is truncated there, with\n    ``task_chars`` / ``task_truncated`` markers. Per-attempt observability dicts\n    (``prompt_render``, ``context_*``) are dropped.\n\n    The full bodies are never lost — they live on disk under ``run_dir``\n    and are reachable via ``orcho_run_evidence`` (plan / findings /\n    receipts slices), ``orcho_run_metrics`` (per-phase token/duration\n    breakdown), or a direct read of ``runspace/runs/<run_id>/``.\n\n    ``auto_detect`` is a typed projection of the run's profile-selector\n    decision, present only when the run started via\n    ``profile=\"auto-detect\"`` (else ``None``). It carries\n    ``requested_selector`` (the ``auto-detect`` token), the selected /\n    recommended profile + mode, ``detection_state`` / ``disposition`` /\n    ``trusted``, and a deterministic ``next_action``: ``None`` for a\n    trusted recommendation, otherwise an ``operator_input_required``\n    pointer to re-run with an explicit profile.\n\n    Args:\n        run_id: the run to inspect.\n        include: opt back in to full phase bodies. Tokens — ``\"task\"``,\n            ``\"plan\"`` (plan markdown + file lists), ``\"output\"`` (implement\n            agent output), ``\"critiques\"`` (validate_plan / repair /\n            final_acceptance critiques + findings), ``\"receipts\"`` (full\n            implementation / repair receipts), ``\"all\"`` (identity — the\n            pre-summary payload, for callers that relied on it).\n            Unrecognised tokens are ignored. Default ``None`` = summary.\n    ",
      "inputSchema": {
        "properties": {
          "include": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Include"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_statusArguments",
        "type": "object"
      },
      "name": "orcho_run_status",
      "outputSchema": {
        "$defs": {
          "ArtefactRefRecord": {
            "description": "Wire model for one readable artefact of a run.\n\nMirrors ``sdk.ArtefactRef`` with one deliberate tightening:\n``kind`` is a closed ``Literal`` on the wire side so clients can\nbranch on a known enum, while the SDK keeps ``kind: str`` for\nforward compatibility. Adding a future kind is a paired core+MCP\nchange.\n\n``size_bytes`` is ``None`` for composable resources — today only\n``evidence``, which ``sdk.evidence.collect_evidence`` assembles\nfrom multiple files at read time, so no single ``Content-Length``\nis meaningful.",
            "properties": {
              "kind": {
                "enum": [
                  "parsed_plan",
                  "evidence",
                  "diff"
                ],
                "title": "Kind",
                "type": "string"
              },
              "mime": {
                "description": "MIME type of the artefact content.",
                "title": "Mime",
                "type": "string"
              },
              "size_bytes": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Physical artefact size from ``os.stat``. ``None`` for composable resources assembled at read time (evidence).",
                "title": "Size Bytes"
              },
              "uri": {
                "description": "MCP resource URI for fetching the artefact, e.g. ``orcho://runs/<id>/parsed_plan.json``.",
                "title": "Uri",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "uri",
              "mime"
            ],
            "title": "ArtefactRefRecord",
            "type": "object"
          },
          "AutoDetectProjection": {
            "description": "Typed projection of ``meta.auto_detect`` for ``orcho_run_status``.\n\norcho-core persists ``meta.auto_detect`` ONLY for runs started through\nthe ``auto-detect`` selector channel (a manual concrete profile never\nwrites it), so the block's *presence* is equivalent to \"this run was\nrequested via the auto-detect selector\". ``requested_selector`` records\nthat request fact (the core ``auto-detect`` token) — it is NOT a detector\ndecision and is always populated whenever this projection exists.\n\nThe detector's own outcome is ``detection_state`` /\n``selected_profile`` + ``selected_mode`` (the profile + mode the run\nactually started with) / ``recommended_profile`` + ``recommended_mode``\n(what the detector proposed). ``disposition`` is the normalised\ntrust label and ``trusted`` is ``True`` only for a clean\n``recommended`` resolution.\n\n``next_action`` is a deterministic agent-control contract. For a\ntrusted (``recommended``) resolution it is ``None`` — trust the\nselected profile. For a fallback resolution\n(``low_confidence_fallback`` / ``detector_error_fallback`` /\n``failed``) it is always an ``operator_input_required`` pointer at\n``orcho_run_start``: when a concrete profile is available it carries a\nNON-empty ``args.profile`` to re-run with; when no profile could be\nresolved it carries NO ``profile`` key and asks the operator to choose\none (or inspect the decision). It NEVER emits ``args.profile`` as\n``None`` / empty.",
            "properties": {
              "confidence": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Detector confidence for the recommendation, when known.",
                "title": "Confidence"
              },
              "confirmation_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "How the recommendation was confirmed (e.g. ``auto`` / ``accepted`` / ``override``), if any.",
                "title": "Confirmation State"
              },
              "delivery_scope": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Delivery scope the run resolved under (core ``delivery_scope``): ``strict_mono`` / ``expanded_mono`` / ``cross``. A trusted / non-interactive resolution keeps ``strict_mono`` regardless of the recommended topology.",
                "title": "Delivery Scope"
              },
              "detection_state": {
                "anyOf": [
                  {
                    "enum": [
                      "recommended",
                      "low_confidence_fallback",
                      "detector_error_fallback",
                      "failed"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Raw detector outcome state from core. ``None`` when the persisted value is missing/unrecognised.",
                "title": "Detection State"
              },
              "disposition": {
                "anyOf": [
                  {
                    "enum": [
                      "recommended",
                      "low_confidence_fallback",
                      "detector_error_fallback",
                      "failed"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Normalised trust disposition derived from ``detection_state``. ``None`` for an unknown/missing state.",
                "title": "Disposition"
              },
              "error_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Why the detector errored, on an error / failed resolution.",
                "title": "Error Reason"
              },
              "fallback_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Why a fallback profile was used, when applicable.",
                "title": "Fallback Reason"
              },
              "fallback_used": {
                "default": false,
                "description": "True when the resolution fell back to a default profile instead of the detector's recommendation.",
                "title": "Fallback Used",
                "type": "boolean"
              },
              "next_action": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/NextActionRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Deterministic next step. ``None`` when trusted. For a fallback disposition, an ``operator_input_required`` pointer at ``orcho_run_start``: with a NON-empty ``args.profile`` when a concrete profile is available, else with NO ``profile`` key (operator must choose / inspect). Never carries an empty ``args.profile``."
              },
              "policy": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Auto-detect policy that governed the resolution (e.g. ``confirm`` / ``trust_above_threshold``).",
                "title": "Policy"
              },
              "projects": {
                "description": "Project aliases the topology recommendation implicates (core ``delivery_projects``), primary alias first. Empty for a mono recommendation.",
                "items": {
                  "type": "string"
                },
                "title": "Projects",
                "type": "array"
              },
              "rationale": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Detector rationale for the recommendation, if recorded.",
                "title": "Rationale"
              },
              "recommended_mode": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Operating mode the detector recommended (``None`` on a detector error / failed resolution).",
                "title": "Recommended Mode"
              },
              "recommended_profile": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Profile the detector recommended (``None`` on a detector error / failed resolution).",
                "title": "Recommended Profile"
              },
              "recommended_topology": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Recommended run topology (core ``recommended_topology``): ``mono`` or ``cross_recommended``. A ``cross_recommended`` value is a recommendation only — it never changed the selected profile or started a cross run.",
                "title": "Recommended Topology"
              },
              "requested_selector": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Run-start selector token the run was requested through (the core ``auto-detect`` token). Present exactly when this projection exists — a request fact derived from the ``meta.auto_detect`` presence invariant, not a detector decision.",
                "title": "Requested Selector"
              },
              "risk_flags": {
                "description": "Risk flags the detector attached to the recommendation.",
                "items": {
                  "type": "string"
                },
                "title": "Risk Flags",
                "type": "array"
              },
              "selected_mode": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Operating mode the run actually started with (core ``actual_mode``).",
                "title": "Selected Mode"
              },
              "selected_profile": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Profile the run actually started with (core ``actual_profile``).",
                "title": "Selected Profile"
              },
              "topology_next_actions": {
                "description": "The three typed topology choices surfaced only for a ``cross_recommended`` topology: (1) start a cross-project run over ``projects``, (2) continue mono with expanded delivery disclosing sibling changes, (3) continue strict mono. Each is an ``operator_input_required`` record; none carries an empty ``args.profile`` (the invariant holds — the key is absent or a non-empty profile). Empty for a mono recommendation.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Topology Next Actions",
                "type": "array"
              },
              "topology_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Short, provider-neutral rationale for the recommended topology (core ``topology_reason``).",
                "title": "Topology Reason"
              },
              "trusted": {
                "default": false,
                "description": "True only for a clean ``recommended`` resolution — the selected profile can be trusted without operator input.",
                "title": "Trusted",
                "type": "boolean"
              }
            },
            "title": "AutoDetectProjection",
            "type": "object"
          },
          "FollowupLineage": {
            "description": "Follow-up lineage around a run, surfaced on ``orcho_run_status``.\n\n``parent_*`` describe the run this one continues (when it is itself a\n``resume_mode=\"followup\"`` child). The ``active_child_*`` fields plus\nthe recommendation describe the newest *unfinished* follow-up child of\nthis run: when one exists, ``recommended_action=\"resume_child\"`` and\n``recommended_run_id`` names the child to resume — the structured\nequivalent of the CLI's \"Resume active follow-up <child>\" guidance, so\nan operator does not resume a parent that a live child has moved past.\n\nAll fields are ``None`` / ``False`` when there is no lineage to report\n(a fresh single-project run with no parent and no follow-up child).",
            "properties": {
              "active_child_handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Active ``meta.phase_handoff.id`` of the child when it is itself paused awaiting a decision; ``None``.",
                "title": "Active Child Handoff Id"
              },
              "active_child_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The newest unfinished follow-up child's run id, when one exists.",
                "title": "Active Child Run Id"
              },
              "active_child_status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Status of the active follow-up child.",
                "title": "Active Child Status"
              },
              "has_active_child_followup": {
                "default": false,
                "description": "True when a newer, still-unfinished follow-up child of this run exists — resuming this run is then discouraged in favour of the child.",
                "title": "Has Active Child Followup",
                "type": "boolean"
              },
              "parent_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Parent run this one continues when it is a follow-up child (``meta.parent_run_id``); ``None`` otherwise.",
                "title": "Parent Run Id"
              },
              "parent_status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Merged status of ``parent_run_id`` when resolvable.",
                "title": "Parent Status"
              },
              "recommendation": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable recommendation mirroring the CLI's 'Resume active follow-up <child>' guidance; ``None`` when no active child follow-up exists.",
                "title": "Recommendation"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "const": "resume_child",
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "``resume_child`` when an active follow-up child should be resumed instead of this run; ``None`` otherwise.",
                "title": "Recommended Action"
              },
              "recommended_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The run id to resume per ``recommended_action`` (the active child); ``None`` when no recommendation.",
                "title": "Recommended Run Id"
              },
              "resume_mode": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "This run's ``meta.resume_mode`` (``followup`` / ``checkpoint`` / ``None`` for a fresh run).",
                "title": "Resume Mode"
              },
              "run_id": {
                "description": "The inspected run.",
                "title": "Run Id",
                "type": "string"
              }
            },
            "required": [
              "run_id"
            ],
            "title": "FollowupLineage",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "RecoveryLineage": {
            "description": "Durable recovery-lineage facts behind a continuation recommendation.\n\nThe structured facts a captain can branch on without reading output logs:\nwhich source run was resolved and whether it is checkpoint-resumable,\nwhether an active follow-up child exists, whether a persisted plan\nartifact is the subject, and — for an ``unknown`` dead-end — exactly which\ndurable facts are missing. All fields are projected from ``meta`` / SDK\nreaders (never parsed from log prose), so a missing fact reads as\n``None`` / ``False`` / an explicit ``missing_facts`` entry.\n\nReused verbatim by ``RunDiagnosis`` (``orcho_run_diagnose``) and\n``RecoveryRecommendation`` (``orcho_run_status``) so the two surfaces never\ndrift.",
            "properties": {
              "active_child_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The newest unfinished follow-up child of the inspected run, when one supersedes it; ``None`` otherwise.",
                "title": "Active Child Run Id"
              },
              "missing_facts": {
                "description": "For an ``unknown`` dead-end, the exact durable facts that are absent (e.g. ``no source/parent run id`` / ``no plan artifact`` / ``no delivery gate`` / ``no active child``). Empty when a continuation subject was resolved.",
                "items": {
                  "type": "string"
                },
                "title": "Missing Facts",
                "type": "array"
              },
              "plan_subject_available": {
                "default": false,
                "description": "True when a durable, readable ``parsed_plan.json`` artifact is the continuation subject (a plan-only / research run with no undelivered diff).",
                "title": "Plan Subject Available",
                "type": "boolean"
              },
              "source_resumable": {
                "default": false,
                "description": "True when the source run is NOT a terminal-resume-parent and still owns retained work (a preserved worktree or a persisted plan) — i.e. resuming it would advance the change session.",
                "title": "Source Resumable",
                "type": "boolean"
              },
              "source_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The resolved source/parent run a terminal recovery run should continue, when one is durably linked; ``None`` otherwise.",
                "title": "Source Run Id"
              },
              "source_status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Merged status of ``source_run_id`` when readable.",
                "title": "Source Status"
              }
            },
            "title": "RecoveryLineage",
            "type": "object"
          },
          "RecoveryRecommendation": {
            "description": "Lineage-aware recovery recommendation, surfaced on ``orcho_run_status``.\n\nProjected from the SAME ``services.run_lineage`` resolver that backs\n``orcho_run_diagnose`` so a captain gets the typed continuation subject\nwithout a separate diagnose call, and the two surfaces never drift. For a\ngiven run the ``continuation_subject`` + ``recommended_next_action`` +\n``recommended_run_id`` here equal those on ``orcho_run_diagnose``.\n\n``None`` on ``orcho_run_status`` when the run has no non-trivial\nrecommendation (e.g. an ordinary running run with no active child and no\nterminality). The durable facts behind the recommendation ride in the\nreused :class:`RecoveryLineage` submodel.\n\n``from_run_plan`` is the right primitive ONLY for\n``recommended_next_action='plan_artifact_continuation'`` (implement a\npersisted plan artifact as a new run) — never for finishing a retained diff\nor checkpoint, which is what ``resume_source_run`` /\n``resume_active_child`` cover.",
            "properties": {
              "continuation_subject": {
                "description": "The typed durable subject this run's continuation should act on.",
                "enum": [
                  "source_run_checkpoint",
                  "active_child_run",
                  "delivery_gate",
                  "plan_artifact",
                  "none",
                  "unknown"
                ],
                "title": "Continuation Subject",
                "type": "string"
              },
              "lineage": {
                "$ref": "#/$defs/RecoveryLineage",
                "description": "Durable recovery-lineage facts (source pointer + resumability, active child, plan-subject availability, and the missing durable facts for a dead-end). Reused verbatim from ``orcho_run_diagnose``."
              },
              "reason": {
                "default": "",
                "description": "One-line factual reason assembled from persisted lineage facts; never parsed from log prose.",
                "title": "Reason",
                "type": "string"
              },
              "recommended_next_action": {
                "description": "The typed next action a captain should take.",
                "enum": [
                  "resume_source_run",
                  "resume_active_child",
                  "delivery_decision",
                  "start_followup",
                  "plan_artifact_continuation",
                  "stop_unknown"
                ],
                "title": "Recommended Next Action",
                "type": "string"
              },
              "recommended_run_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The source / active-child / plan-owning run to act on; ``None`` for a ``stop_unknown`` dead-end.",
                "title": "Recommended Run Id"
              }
            },
            "required": [
              "continuation_subject",
              "recommended_next_action"
            ],
            "title": "RecoveryRecommendation",
            "type": "object"
          },
          "WorktreeContinuity": {
            "description": "Worktree-continuity mode for a run, surfaced on ``orcho_run_status``.\n\nProjects the persisted ``meta['worktree']`` block (and its\n``followup_continuity`` sub-block) so a client sees how the run picked\nup its worktree without parsing logs. ``subject_mode`` is the\nnormalised classification:\n\n- ``same_run_retained`` — not a follow-up; the run kept its own\n  worktree (a fresh / checkpoint / provider-fallback run — the\n  same-run worktree is preserved);\n- ``reused_parent`` — a follow-up that reused the parent's dirty\n  worktree, carrying the parent's uncommitted diff into the child\n  (``diff_source='worktree'``);\n- ``clean_head_no_undelivered_diff`` — a follow-up that started fresh\n  from HEAD because the parent had no undelivered diff\n  (``diff_source='none'``);\n- ``blocked_parent_diff_unavailable`` — a follow-up blocked before any\n  write phase because the parent's undelivered diff exists only as a\n  ``diff.patch`` artifact this run will not replay\n  (``diff_source='artifact'``); ``block_message`` carries the core\n  warning that starting on a clean HEAD would silently drop the\n  change and that resuming the parent recovers it;\n- ``unknown_continuity`` — a follow-up block with an unrecognised\n  ``diff_source`` (defensive).",
            "properties": {
              "block_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core warning explaining the block — that starting on a clean HEAD would silently drop the parent's change and that resuming the parent recovers it. ``None`` when not blocked. Surfaced verbatim so clients show it without parsing logs.",
                "title": "Block Message"
              },
              "blocked": {
                "default": false,
                "description": "True when the follow-up was blocked before any write phase (parent diff/worktree unavailable).",
                "title": "Blocked",
                "type": "boolean"
              },
              "degraded_reason": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Non-fatal isolation degrade reason from core, if any.",
                "title": "Degraded Reason"
              },
              "diff_source": {
                "anyOf": [
                  {
                    "enum": [
                      "worktree",
                      "artifact",
                      "none"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Raw follow-up diff source from core: ``worktree`` (parent's dirty worktree reused), ``artifact`` (diff.patch only — blocked), ``none`` (no undelivered diff). ``None`` for a non-follow-up run.",
                "title": "Diff Source"
              },
              "has_worktree": {
                "description": "True when the run has a persisted ``meta['worktree']`` block; False when no worktree state was recorded.",
                "title": "Has Worktree",
                "type": "boolean"
              },
              "is_followup_continuity": {
                "default": false,
                "description": "True when a ``followup_continuity`` sub-block was present (the run is a follow-up).",
                "title": "Is Followup Continuity",
                "type": "boolean"
              },
              "isolation": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Worktree isolation mode (``off`` / ``per_run`` / …).",
                "title": "Isolation"
              },
              "mode_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's verbatim follow-up mode label (e.g. ``reused parent <path>`` / ``clean HEAD (parent had no undelivered diff)``). ``None`` for a non-follow-up run.",
                "title": "Mode Label"
              },
              "path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Worktree checkout path on disk.",
                "title": "Path"
              },
              "subject_mode": {
                "anyOf": [
                  {
                    "enum": [
                      "same_run_retained",
                      "reused_parent",
                      "clean_head_no_undelivered_diff",
                      "blocked_parent_diff_unavailable",
                      "unknown_continuity"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Normalised worktree-continuity mode; ``None`` when no worktree block exists.",
                "title": "Subject Mode"
              },
              "worktree_preserved": {
                "default": false,
                "description": "True when a usable worktree exists (every mode except ``blocked``) — e.g. a same-run provider fallback keeps the worktree.",
                "title": "Worktree Preserved",
                "type": "boolean"
              }
            },
            "required": [
              "has_worktree"
            ],
            "title": "WorktreeContinuity",
            "type": "object"
          }
        },
        "description": "Summary state snapshot for a single run.\n\n``meta`` is the default summary projection of ``meta.json`` (phase bodies\nelided unless the caller opts back in via ``include``), while ``metrics`` is\nsurfaced as a plain dict. The runtime shape of meta.json/metrics.json\nevolves and over-tightening here would force schema changes for every\nobservability tweak.",
        "properties": {
          "artefacts": {
            "description": "Readable artefacts available for this run, with MCP resource URIs. Agent reads this to discover what it can fetch without scanning the run directory or remembering filename conventions.",
            "items": {
              "$ref": "#/$defs/ArtefactRefRecord"
            },
            "title": "Artefacts",
            "type": "array"
          },
          "auto_detect": {
            "anyOf": [
              {
                "$ref": "#/$defs/AutoDetectProjection"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Typed projection of the persisted ``meta.auto_detect`` decision (requested selector + detector outcome + a deterministic next_action). ``None`` for a run that did not start through the ``auto-detect`` selector."
          },
          "lineage": {
            "anyOf": [
              {
                "$ref": "#/$defs/FollowupLineage"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Follow-up lineage around this run — parent linkage and, when present, a recommendation to resume an active follow-up child instead of this run. ``None`` when lineage could not be projected."
          },
          "meta": {
            "additionalProperties": true,
            "title": "Meta",
            "type": "object"
          },
          "metrics": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Metrics"
          },
          "next_actions": {
            "description": "Suggested follow-up tool calls derived from the run's current state (MCP UX A1, Principle 1). Empty when the run is mid-flight or terminal-success.",
            "items": {
              "$ref": "#/$defs/NextActionRecord"
            },
            "title": "Next Actions",
            "type": "array"
          },
          "provider_pressure": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderPressure"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Core-typed provider runtime/access failure (rate-limit / transient runtime fault / access loss) projected from the same ``project_provider_pressure`` source as ``orcho_run_diagnose`` and ``orcho_run_evidence`` — so all surfaces report the same condition with the same conservative ``next_actions``. ``None`` for a generic failure with no core-typed provider source. The provider-pressure follow-ups live here in ``provider_pressure.next_actions``; the legacy SDK-derived ``next_actions`` field is unaffected."
          },
          "recovery_recommendation": {
            "anyOf": [
              {
                "$ref": "#/$defs/RecoveryRecommendation"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Lineage-aware recovery recommendation projected from the same ``services.run_lineage`` resolver as ``orcho_run_diagnose`` — the typed continuation subject + next action (resume the source checkpoint, resume the active child, continue a plan artifact, or stop with the missing facts) so a captain need not call diagnose separately. ``None`` for an ordinary run with no non-trivial recommendation (no terminality, no active child). Additive — does NOT overload ``lineage``."
          },
          "run_dir": {
            "title": "Run Dir",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "sub_runs": {
            "description": "Child run aliases for cross-project runs (empty for single-project).",
            "items": {
              "type": "string"
            },
            "title": "Sub Runs",
            "type": "array"
          },
          "worktree_continuity": {
            "anyOf": [
              {
                "$ref": "#/$defs/WorktreeContinuity"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Worktree-continuity mode (same-run retained / reused parent / clean HEAD / blocked) projected from ``meta['worktree']``, including the core clean-HEAD recovery warning. ``None`` when not projected."
          }
        },
        "required": [
          "run_id",
          "run_dir",
          "meta"
        ],
        "title": "RunStatus",
        "type": "object"
      }
    },
    {
      "description": "Long-poll a run until something meaningful happens.\n\n    Holds the MCP request open until the chosen ``until`` condition fires\n    or ``timeout_s`` expires. Designed to replace manual re-polling of\n    ``orcho_run_status`` / ``orcho_run_events_summary`` during long\n    phases (implement, repair) where minutes can pass between\n    user-relevant events. When the MCP request carries a\n    ``progressToken``, ordered ``notifications/progress`` are emitted as\n    the event sequence advances (one notification per observed seq\n    advance, not per individual event) so clients can show live status\n    without the operator-side LLM having to round-trip.\n\n    Reconnect rule: pass ``result.summary.next_seq`` (or\n    ``result.trigger.seq`` when ``summary=False``) as the next\n    ``since_seq``. Both fields carry the same value at return time.\n\n    Resilient observation: clients whose transport caps a single\n    tool-call's duration should pass a short bounded ``timeout_s``\n    (120–240s is a convenient window) rather than the 3600s default, so\n    each watch returns promptly with a fresh reconnect cursor. On a\n    ``timeout`` trigger — or if the transport drops the long-poll\n    mid-call — fall back to\n    ``orcho_run_events_summary(since_seq=trigger.seq)`` to catch up, then\n    resume ``orcho_run_watch`` from that summary's ``next_seq``. The loop\n    is ``orcho_run_watch`` → ``orcho_run_events_summary`` →\n    ``orcho_run_watch``. A disconnected watch is not a failed run: the run\n    keeps executing in its worktree, and lifecycle decisions are read only\n    from the typed ``status`` / ``handoff`` / terminal / evidence signals,\n    never from a watch call ending early.\n\n    Args:\n        run_id: the run to watch.\n        since_seq: reconnect cursor. Defaults to ``0`` (watch from the\n            start of the event stream). Must be ≥ 0.\n        until: trigger condition.\n            ``\"next_event\"`` — any event with seq > since_seq.\n            ``\"phase_change\"`` — phase transitions (including end-to-None),\n            or handoff/terminal.\n            ``\"subtask\"`` — each subtask_dag boundary (a subtask starts or\n            ends) during a long implement phase, or handoff/terminal. The\n            result's ``summary.current_subtask`` carries the live\n            index/total/goal/state, and progressToken notifications read\n            \"implement: subtask 3/12 done (<goal>)\".\n            ``\"handoff_or_terminal\"`` — run pauses\n            (``awaiting_phase_handoff`` / ``awaiting_gate_decision``) or\n            ends.\n            ``\"terminal\"`` — run reaches a terminal status.\n        timeout_s: max seconds to hold the request open. Default 3600 (1h);\n            hard ceiling 7200 (2h). Must be in (0, 7200].\n        summary: when True, include a bounded ``RunEventsSummary`` in the\n            result. When False, ``result.summary`` is ``None`` and the\n            reconnect cursor lives on ``result.trigger.seq``.\n        interaction_client: **presentation hint only** for the\n            ``handoff.client_hints`` payload when the run pauses.\n            First-class values: ``generic``, ``claude-code``, ``codex``.\n            Unknown values normalise to ``generic`` so future clients\n            stay forward-compatible. Does **not** affect decision\n            correctness, ``available_actions``, ``default_action``,\n            trigger kind, summary, or progress — the agent reads this to\n            decide *how* to render the prompt, not *what* to do.\n\n    Errors:\n        - RunNotFoundError — unknown run_id.\n        - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE.\n        - InvalidPlanError — input out of range, or unknown ``until``.\n\n    Returns ``RunWatchResult``. On timeout, ``triggered=False`` and\n    ``trigger.kind==\"timeout\"``; on any other path ``triggered=True``.\n    Raw event payloads are never returned — use ``orcho_run_events_tail``\n    for forensic replay.\n    ",
      "inputSchema": {
        "properties": {
          "interaction_client": {
            "default": "generic",
            "title": "Interaction Client",
            "type": "string"
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "since_seq": {
            "default": 0,
            "title": "Since Seq",
            "type": "integer"
          },
          "summary": {
            "default": true,
            "title": "Summary",
            "type": "boolean"
          },
          "timeout_s": {
            "default": 3600,
            "title": "Timeout S",
            "type": "integer"
          },
          "until": {
            "default": "handoff_or_terminal",
            "enum": [
              "next_event",
              "phase_change",
              "subtask",
              "handoff_or_terminal",
              "terminal"
            ],
            "title": "Until",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "orcho_run_watchArguments",
        "type": "object"
      },
      "name": "orcho_run_watch",
      "outputSchema": {
        "$defs": {
          "CompactRunEvent": {
            "description": "Bounded mirror of ``EventRecord``.\n\nNever carries the full raw payload. The handler extracts a small set of\nwell-known payload keys (``summary``/``text``/``message``, ``tool``,\n``status``) and applies hard character-length caps so a single event\nwith a 10 KB ``command`` field cannot inflate the wire payload of a\n200-event summary past the bounded-observe budget. Truncation uses\nPython slicing (Unicode-safe at code-point boundaries); the limits\nare in characters, not bytes.",
            "properties": {
              "kind": {
                "description": "Event kind. Truncated to 64 chars to keep the bounded summary honest under future emitter changes.",
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase tag if present. Truncated to 64 chars (same reasoning as ``kind``).",
                "title": "Phase"
              },
              "seq": {
                "title": "Seq",
                "type": "integer"
              },
              "status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Status value when the event reports a phase/run state transition. Truncated to 64 chars.",
                "title": "Status"
              },
              "summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable summary pulled from one of the canonical payload keys (summary / text / message) and truncated to 256 chars.",
                "title": "Summary"
              },
              "tool": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Tool name when the event reports an agent tool call. Truncated to 64 chars.",
                "title": "Tool"
              },
              "ts": {
                "title": "Ts",
                "type": "string"
              }
            },
            "required": [
              "seq",
              "ts",
              "kind"
            ],
            "title": "CompactRunEvent",
            "type": "object"
          },
          "CurrentSubtaskRecord": {
            "description": "Live progress coordinate for the in-flight ``subtask_dag`` subtask.\n\nLets a watcher render step-by-step progress (\"subtask 3/12 (Patch\ntarget)\") during a long implement phase without scanning raw events.\nComputed from the latest ``subtask.start`` / ``subtask.end`` event up to\nthe summary horizon; cleared when the implement phase ends. ``None`` on\nthe summary means no subtask is currently in flight.",
            "properties": {
              "goal": {
                "default": "",
                "description": "One-line subtask goal, bounded.",
                "title": "Goal",
                "type": "string"
              },
              "index": {
                "description": "1-based position of this subtask in the whole DAG.",
                "title": "Index",
                "type": "integer"
              },
              "seq": {
                "description": "Event seq of the start/end boundary that produced this snapshot. Advances on every subtask transition — the ``until=\"subtask\"`` watch fires when it passes the caller's cursor.",
                "title": "Seq",
                "type": "integer"
              },
              "state": {
                "description": "\"running\" right after start; \"done\" / \"incomplete\" / \"failed\" once the subtask ends (incomplete = done-criteria attestation gate did not close).",
                "title": "State",
                "type": "string"
              },
              "subtask_id": {
                "title": "Subtask Id",
                "type": "string"
              },
              "total": {
                "description": "Total subtask count in the DAG (the denominator).",
                "title": "Total",
                "type": "integer"
              }
            },
            "required": [
              "subtask_id",
              "index",
              "total",
              "state",
              "seq"
            ],
            "title": "CurrentSubtaskRecord",
            "type": "object"
          },
          "HandoffClientHints": {
            "description": "Hints for the operator-side LLM on how to render the handoff.\n\nKeyed by ``client`` — one of the first-class profiles ``generic`` /\n``claude-code`` / ``codex``. Unknown ``interaction_client`` values on\nthe watch call normalise to ``generic`` (in\n``orcho_mcp.client_interactions.get_interaction_profile``), so this\nfield always carries a known label.\n\nThis is **presentation metadata only**. None of these flags affect\nrun lifecycle, available actions, decision correctness, or the\nbehaviour of ``orcho_phase_handoff_decide``. They tell the agent\n*how* to render the prompt, not *what* to do.",
            "properties": {
              "action_field": {
                "default": "action",
                "description": "Name of the structured ``action`` field the agent should pass to ``orcho_phase_handoff_decide``.",
                "title": "Action Field",
                "type": "string"
              },
              "allow_feedback_text": {
                "default": true,
                "description": "Whether the agent should accept free-form feedback from the user (relevant when ``retry_feedback`` or ``continue_with_waiver`` is in ``available_actions``).",
                "title": "Allow Feedback Text",
                "type": "boolean"
              },
              "clarify_on_ambiguous_reply": {
                "default": true,
                "title": "Clarify On Ambiguous Reply",
                "type": "boolean"
              },
              "client": {
                "default": "generic",
                "description": "Profile this hint set was rendered for. First-class values: ``generic``, ``claude-code``, ``codex``. Unknown ``interaction_client`` inputs to ``orcho_run_watch`` are normalised to ``generic`` before this field is populated.",
                "title": "Client",
                "type": "string"
              },
              "feedback_field": {
                "default": "feedback",
                "description": "Name of the free-form ``feedback`` field the agent should fill when the chosen action requires it.",
                "title": "Feedback Field",
                "type": "string"
              },
              "include_followup_tools": {
                "default": true,
                "description": "Whether the agent should also mention the matching follow-up tools (``decision_tool`` / ``resume_tool``) in the rendered prompt.",
                "title": "Include Followup Tools",
                "type": "boolean"
              },
              "interaction_style": {
                "default": "free_form",
                "enum": [
                  "structured_choice",
                  "free_form",
                  "ask"
                ],
                "title": "Interaction Style",
                "type": "string"
              },
              "preferred_render": {
                "default": "chat",
                "enum": [
                  "chat",
                  "ask"
                ],
                "title": "Preferred Render",
                "type": "string"
              },
              "show_actions": {
                "default": true,
                "description": "Whether the agent should render the action list explicitly to the user.",
                "title": "Show Actions",
                "type": "boolean"
              }
            },
            "title": "HandoffClientHints",
            "type": "object"
          },
          "HandoffDecisionChoice": {
            "description": "One ready-to-call decision action surfaced inside a handoff hint.\n\nPairs a human-readable ``label`` with the structured tool call the\nagent should make: ``tool`` + ``args``. When ``requires_feedback``\nis ``True`` the agent must collect user input first and merge it\nunder ``feedback_field`` before calling — ``args`` deliberately\nomits the feedback key so a weak agent cannot forward a placeholder\nstring. When ``requires_feedback`` is ``False``, ``args`` is\ncomplete and safe to send as-is.\n\n``followup`` carries the next step after the decision is recorded\n(today: ``orcho_run_resume`` for non-halt actions). ``None`` for\nterminal actions (``halt``).",
            "properties": {
              "action": {
                "description": "The decision verb (``continue`` / ``retry_feedback`` / ``continue_with_waiver`` / ``halt``).",
                "title": "Action",
                "type": "string"
              },
              "args": {
                "additionalProperties": true,
                "description": "Safely pre-filled tool arguments. NEVER contains a placeholder string the agent might forward verbatim. When ``requires_feedback=True``, the feedback kwarg is omitted — the agent collects user input and merges it in under ``feedback_field``.",
                "title": "Args",
                "type": "object"
              },
              "elicitation": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/HandoffElicitationHint"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Native MCP elicitation metadata for feedback-gated choices. Present for ``retry_feedback`` and ``continue_with_waiver`` so clients with form elicitation support can collect feedback natively; ``None`` for choices that do not require feedback."
              },
              "feedback_field": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Name of the kwarg to fill when ``requires_feedback`` is True (e.g. ``feedback``). ``None`` otherwise.",
                "title": "Feedback Field"
              },
              "feedback_placeholder": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable hint the agent surfaces to the user when collecting feedback. ``None`` when no feedback is needed.",
                "title": "Feedback Placeholder"
              },
              "followup": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/HandoffFollowupCall"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The tool call to make after the decision is recorded. Today: ``orcho_run_resume`` with ``{run_id}`` for ``continue`` / ``retry_feedback`` / ``continue_with_waiver``; ``None`` for ``halt`` (terminal — no resume)."
              },
              "label": {
                "description": "One-line human-readable description of the choice.",
                "title": "Label",
                "type": "string"
              },
              "requires_feedback": {
                "description": "``True`` when the action needs a free-form feedback string from the user before the call. ``False`` means ``args`` is complete and safe to send as-is.",
                "title": "Requires Feedback",
                "type": "boolean"
              },
              "tool": {
                "description": "MCP tool to call for the decision (today: ``orcho_phase_handoff_decide``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "label",
              "action",
              "tool",
              "args",
              "requires_feedback"
            ],
            "title": "HandoffDecisionChoice",
            "type": "object"
          },
          "HandoffDecisionHint": {
            "description": "Structured decision packet surfaced when the run pauses for a human.\n\nCarries compact ``findings``, a ``default_action`` heuristic,\n``feedback_required_for`` derived from available actions, and\n``client_hints`` for rendering. The MCP-layer presentation adapter\nbehind ``client_hints`` is selected by\n``orcho_run_watch.interaction_client``. The agent is responsible for\nthe actual conversation; Orcho returns the data and the agent renders\nthe prompt.\n\n``choices`` is the ready-to-call decision menu: one\n``HandoffDecisionChoice`` per known runtime action with\npre-filled ``args``, ``requires_feedback`` flag, and ``followup``\npointer. Agents can branch on ``choices`` without parsing\n``available_actions`` and without re-deriving the\n``orcho_phase_handoff_decide`` argument shape.\n\nAdditive: existing clients that only read ``handoff_id`` /\n``phase`` / ``available_actions`` continue to work. Client-specific\nruntime integrations (IDE-side UI, native pickers, automatic\ndecision execution) remain out of scope here — this is presentation\nmetadata only.",
            "properties": {
              "available_actions": {
                "description": "Normalised list of decision actions from ``meta.phase_handoff.available_actions``. Empty when meta does not carry them.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "choices": {
                "description": "Ready-to-call decision menu. One entry per known action in ``available_actions`` (today: ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` / ``halt``). Each choice carries pre-filled tool args, a ``requires_feedback`` flag, and a ``followup`` pointer to the resume step (when applicable). Unknown runtime verbs are filtered out so the menu only contains callable actions — ``available_actions`` keeps the verbatim runtime offering for callers that need the raw set.",
                "items": {
                  "$ref": "#/$defs/HandoffDecisionChoice"
                },
                "title": "Choices",
                "type": "array"
              },
              "client_hints": {
                "$ref": "#/$defs/HandoffClientHints",
                "description": "Rendering hints keyed by the resolved interaction client. Tells the agent which fields to fill, whether to clarify ambiguous replies, and the preferred render style. Client-specific runtime integrations (IDE UI, native pickers, automatic decision execution) remain out of scope."
              },
              "decision_tool": {
                "default": "orcho_phase_handoff_decide",
                "title": "Decision Tool",
                "type": "string"
              },
              "default_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Suggested action when the agent has no explicit user preference. Chosen from ``available_actions`` only (``retry_feedback`` > ``continue`` > ``halt`` > ``continue_with_waiver``; the waiver override sits last and is never suggested unsolicited). ``None`` if no actions are available — Orcho never suggests an action the runtime did not offer.",
                "title": "Default Action"
              },
              "feedback_required_for": {
                "description": "Actions that require a free-form ``feedback`` string alongside the structured ``action`` field.",
                "items": {
                  "type": "string"
                },
                "title": "Feedback Required For",
                "type": "array"
              },
              "findings": {
                "description": "Up to 5 compact reviewer findings. Source of truth: ``meta.phase_handoff.findings`` if list-like; otherwise a defensive fallback to the SDK findings API scoped to the handoff phase. Empty list is a valid value — never invented.",
                "items": {
                  "$ref": "#/$defs/HandoffFindingSummary"
                },
                "title": "Findings",
                "type": "array"
              },
              "findings_summary": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded human-readable summary built from the compact ``findings`` list (severity + title only). ``None`` when no findings are available. Capped at 500 chars.",
                "title": "Findings Summary"
              },
              "handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Handoff identifier from ``meta.phase_handoff.id`` when available.",
                "title": "Handoff Id"
              },
              "kind": {
                "const": "requires_user_decision",
                "default": "requires_user_decision",
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the handoff. Prefers ``meta.phase_handoff.phase``; falls back to the run's current_phase. May be ``None`` when neither is known.",
                "title": "Phase"
              },
              "recommended_user_prompt": {
                "description": "Bounded prompt the operator-side LLM can render to the user. Includes phase, findings (when available), and the structured-choice action list. Capped at 1500 chars. No raw event payload text.",
                "title": "Recommended User Prompt",
                "type": "string"
              },
              "resume_tool": {
                "default": "orcho_run_resume",
                "title": "Resume Tool",
                "type": "string"
              },
              "run_id": {
                "description": "Run this handoff belongs to. Duplicated from the top-level ``RunWatchResult.run_id`` so the handoff object is self-contained when copied into a prompt.",
                "title": "Run Id",
                "type": "string"
              },
              "title": {
                "description": "Short one-line label for prompts/UI. Conservative — never invents review findings.",
                "title": "Title",
                "type": "string"
              }
            },
            "required": [
              "run_id",
              "title",
              "recommended_user_prompt"
            ],
            "title": "HandoffDecisionHint",
            "type": "object"
          },
          "HandoffElicitationHint": {
            "description": "Native MCP elicitation metadata for a decision choice.\n\nThis is a progressive-enhancement hint. The choice remains the\ncanonical decision contract; clients with form elicitation support\ncan let the server request this field natively, while clients\nwithout elicitation support ask the user in chat and merge the\nanswer into ``choices[].args`` under ``field``.",
            "properties": {
              "client_capability": {
                "default": "elicitation.form",
                "description": "Client capability required for native elicitation. Without it, the agent asks the user in chat.",
                "title": "Client Capability",
                "type": "string"
              },
              "field": {
                "description": "Name of the field the elicitation response provides and the decision tool expects.",
                "title": "Field",
                "type": "string"
              },
              "message": {
                "description": "Prompt shown by the MCP client when native elicitation is available.",
                "title": "Message",
                "type": "string"
              },
              "mode": {
                "const": "form",
                "default": "form",
                "description": "Elicitation mode used for this choice.",
                "title": "Mode",
                "type": "string"
              },
              "requested_schema": {
                "additionalProperties": true,
                "description": "Flat JSON Schema for the native form elicitation request.",
                "title": "Requested Schema",
                "type": "object"
              }
            },
            "required": [
              "field",
              "message",
              "requested_schema"
            ],
            "title": "HandoffElicitationHint",
            "type": "object"
          },
          "HandoffFindingSummary": {
            "description": "One compact reviewer finding attached to a paused handoff.\n\nMirrors a tiny subset of ``FindingRecord`` (severity / title / body /\nrequired_fix / file / line) with hard length caps so a handoff hint\ncan carry up to 5 of these without inflating the wire payload past\nthe bounded-observe budget. Raw evidence detail is intentionally\nomitted — the agent renders these to the user as numbered choices,\nnot as a debug dump. For full forensic detail, use\n``orcho_run_evidence(slice=\"findings\")``.",
            "properties": {
              "body": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Short prose explanation when available. Capped at 300 chars.",
                "title": "Body"
              },
              "file": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Path of the file the finding targets, if any. Capped at 240 chars.",
                "title": "File"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Id"
              },
              "line": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Line"
              },
              "required_fix": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Suggested remediation when the runtime carries one. Capped at 240 chars.",
                "title": "Required Fix"
              },
              "severity": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "``P1`` / ``P2`` / etc. when the runtime carries one; passed through verbatim.",
                "title": "Severity"
              },
              "title": {
                "description": "One-line finding label. Capped at 160 chars.",
                "title": "Title",
                "type": "string"
              }
            },
            "required": [
              "title"
            ],
            "title": "HandoffFindingSummary",
            "type": "object"
          },
          "HandoffFollowupCall": {
            "description": "Pre-filled follow-up tool call attached to a decision choice.\n\nToday the only follow-up is ``orcho_run_resume`` after a non-halt\ndecision. ``args`` is **always complete** for the followup — there\nis no feedback in resume, ever; the agent can forward it verbatim\nwithout merging anything in.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Complete, ready-to-send arguments for the follow-up tool. Safe to forward verbatim — no placeholders.",
                "title": "Args",
                "type": "object"
              },
              "tool": {
                "description": "Name of the follow-up MCP tool to call after the decision is recorded (today: ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "tool",
              "args"
            ],
            "title": "HandoffFollowupCall",
            "type": "object"
          },
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "PendingHandoffSummary": {
            "description": "Active phase-handoff state surfaced inside a run events summary.\n\nPopulated only when the merged run status is\n``awaiting_phase_handoff`` so polling callers see the pause —\nhandoff id, phase, trigger, machine verdict, a coherent round label,\nthe available decision actions, a bounded preview of the reviewer's\nlast output, whether a decision artifact already exists, and a\none-line ``suggested_next_action`` — without having to fire an\n``orcho_run_watch`` handoff trigger. The full structured decision\npacket (findings, choices, elicitation) still rides in\n``orcho_run_watch``'s ``HandoffDecisionHint``; this is the compact\nat-a-glance projection for status polling.",
            "properties": {
              "available_actions": {
                "description": "Decision verbs the runtime published for this handoff, verbatim.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "decision_artifact_exists": {
                "default": false,
                "description": "True when a phase-handoff decision artifact already exists for this handoff (the operator has decided and only ``orcho_run_resume`` remains).",
                "title": "Decision Artifact Exists",
                "type": "boolean"
              },
              "handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Handoff identifier from ``meta.phase_handoff.id``.",
                "title": "Handoff Id"
              },
              "last_output_preview": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded preview of the reviewer's last critique / output. Truncated; never the full raw text.",
                "title": "Last Output Preview"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the handoff (falls back to the run's current phase when the payload omits it).",
                "title": "Phase"
              },
              "round_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Coherent operator round label — ``<phase> automatic round R/M`` for an auto round, ``<phase> human retry K`` for a human-directed retry round. Never an impossible ``R/M`` with ``R > M``.",
                "title": "Round Label"
              },
              "suggested_next_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "One-line pointer at the right next tool: decide then resume when no decision exists yet, resume when one already does.",
                "title": "Suggested Next Action"
              },
              "trigger": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Why the handoff fired (e.g. ``rejected`` / ``approved`` / ``incomplete``).",
                "title": "Trigger"
              },
              "verdict": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Machine verdict label carried verbatim from the payload (``REJECTED`` / ``APPROVED``).",
                "title": "Verdict"
              }
            },
            "title": "PendingHandoffSummary",
            "type": "object"
          },
          "PhaseEventSummary": {
            "description": "Per-phase aggregate over the windowed event slice.",
            "properties": {
              "count": {
                "title": "Count",
                "type": "integer"
              },
              "kinds": {
                "description": "Distinct event kinds observed in this phase, sorted.",
                "items": {
                  "type": "string"
                },
                "title": "Kinds",
                "type": "array"
              },
              "phase": {
                "description": "Phase name, or ``\"(unknown)\"`` for events emitted without a phase tag.",
                "title": "Phase",
                "type": "string"
              }
            },
            "required": [
              "phase",
              "count",
              "kinds"
            ],
            "title": "PhaseEventSummary",
            "type": "object"
          },
          "ProviderPressure": {
            "description": "Core-typed provider-pressure condition projected for MCP clients.\n\nA run is under *provider pressure* when core attaches a typed\n``ProviderRuntimeFailure`` (``provider_runtime``) or\n``ProviderAccessRecovery`` (``provider_access``) to its errors/halt\nrollup — a rate-limit, a transient provider runtime fault, or a loss of\nprovider access — as opposed to a generic code/test/review failure. The\nclassification comes ONLY from the core-typed source; MCP never derives it\nby parsing raw provider output or logs.\n\nEvery field is additive-optional so a surface that has nothing to report\nomits it cleanly (``None`` / ``False`` / empty). ``next_actions`` are\nconservative and never imply a passed review/delivery, and never ask the\noperator to invent retry feedback.",
            "properties": {
              "condition": {
                "const": "provider_pressure",
                "default": "provider_pressure",
                "description": "Fixed marker identifying this as the typed provider-pressure condition. Always ``provider_pressure`` when the model is present.",
                "title": "Condition",
                "type": "string"
              },
              "failure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's typed failure kind — ``provider_runtime`` for a runtime fault/rate-limit, ``provider_access`` for a loss of access. ``None`` when core attached no typed provider failure.",
                "title": "Failure Kind"
              },
              "next_actions": {
                "description": "Conservative, typed follow-up calls built by the single shared ``build_provider_pressure_next_actions`` helper. They inspect the provider-pressure evidence and resume/retry the interrupted phase (or wait for a reset window) — never imply a passed review/delivery and never require invented operator feedback.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The phase that was interrupted by the provider failure (core's ``failed_phase``); ``None`` when absent.",
                "title": "Phase"
              },
              "pressure_kind": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Finer provider-pressure classification (future core field, e.g. rate-limit vs overload). ``None`` until core emits it; never fabricated by MCP.",
                "title": "Pressure Kind"
              },
              "recommended_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's recommended action for this provider failure (``resume_or_retry_phase`` for a runtime fault, ``switch_runtime_or_restore_access`` for an access loss). ``None`` when absent.",
                "title": "Recommended Action"
              },
              "recoverable": {
                "default": false,
                "description": "True when core marks the provider failure as recoverable (the interrupted phase can be retried/resumed). False for an exhausted/terminal provider failure.",
                "title": "Recoverable",
                "type": "boolean"
              },
              "reset_at": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When the provider reset window passes (future core field). ``None`` until core emits it — MCP never fabricates a reset time.",
                "title": "Reset At"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's retry lifecycle for the provider failure (future field, e.g. ``parked_until_reset`` / ``exhausted``). ``None`` until core emits it.",
                "title": "Retry State"
              },
              "sanitized_message": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core's sanitized provider message (``provider_message``) — never raw provider output. ``None`` when core left it empty.",
                "title": "Sanitized Message"
              },
              "wait_hint": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-readable wait guidance until the provider recovers (future core field). ``None`` until core emits it.",
                "title": "Wait Hint"
              }
            },
            "title": "ProviderPressure",
            "type": "object"
          },
          "ProviderSessionFallback": {
            "description": "A recovered missing-provider-session fallback, surfaced in a summary.\n\norcho-core emits ``phase.provider_session_fallback`` ONLY after a\nfresh-session retry already succeeded — the stale-session error was\ncaught and fully handled, no ``failed`` record leaks out, and the run\ncontinues in the same worktree with persisted context. This record\nmakes that recovery observable as structured data and, via\n``phase_succeeded=True``, makes explicit that the fallback is a\nrecovery notice — never a phase failure.",
            "properties": {
              "fallback_mode": {
                "const": "fresh_provider_session",
                "default": "fresh_provider_session",
                "description": "Recovery mode — a fresh provider session was started in place of the missing one.",
                "title": "Fallback Mode",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase whose provider session was missing and recovered.",
                "title": "Phase"
              },
              "phase_succeeded": {
                "default": true,
                "description": "True for a recovered fallback — the event is emitted only after the fresh-session retry succeeded, so this is NOT a phase failure.",
                "title": "Phase Succeeded",
                "type": "boolean"
              },
              "stale_session_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Redacted preview of the missing provider session id (a short prefix + ``…``); never the full identifier. The sentinel ``unknown`` passes through when core had no id.",
                "title": "Stale Session Id"
              },
              "worktree_preserved": {
                "default": true,
                "description": "True — the fallback continues in the same run worktree with persisted context (no checkout was lost).",
                "title": "Worktree Preserved",
                "type": "boolean"
              }
            },
            "title": "ProviderSessionFallback",
            "type": "object"
          },
          "RetryState": {
            "description": "Human-retry / repeated-reject lifecycle state, alongside the pause.\n\nDerived from raw persisted inputs (round counters, verdict, and the\n``retry_feedback`` decision artifacts) — never a re-rendered CLI\nbanner. Distinguishes an automatic reject, a human-directed retry in\nprogress, a human retry that was rejected again, and a retry that was\naccepted (handoff closed).",
            "properties": {
              "operator_feedback_preview": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Bounded preview of the operator's most recent retry_feedback text; truncated, never the full text. ``None`` when no retry feedback was recorded.",
                "title": "Operator Feedback Preview"
              },
              "pending_operator_decision": {
                "default": false,
                "description": "True while the run is paused awaiting an operator decision on the active handoff (no decision artifact recorded for it yet).",
                "title": "Pending Operator Decision",
                "type": "boolean"
              },
              "retry_attempt_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Coherent operator label for the attempt — ``<phase> automatic round R/M``, ``<phase> human retry K after REJECTED verdict``, or ``<phase> human retry K rejected; operator decision required``. Never an impossible ``R/M`` with ``R > M``.",
                "title": "Retry Attempt Label"
              },
              "retry_context": {
                "description": "Reject / retry lifecycle state. ``automatic_reject`` — paused on a rejected verdict at an automatic round; ``human_retry_in_progress`` — a retry_feedback decision exists and the run is executing the human-directed round; ``retry_rejected_again`` — paused again on a rejected verdict at a human-directed round; ``retry_accepted_closed`` — the retry was accepted and the handoff closed. The two non-paused states are best-effort lifecycle hints.",
                "enum": [
                  "automatic_reject",
                  "human_retry_in_progress",
                  "retry_rejected_again",
                  "retry_accepted_closed"
                ],
                "title": "Retry Context",
                "type": "string"
              }
            },
            "required": [
              "retry_context"
            ],
            "title": "RetryState",
            "type": "object"
          },
          "RunEventsSummary": {
            "description": "Bounded summary of a run's recent events.\n\nDesigned for status polling: an operator-side LLM can read this once\nper check instead of dumping the raw event list. ``status`` and\n``current_phase`` come from the authoritative meta/supervisor merge so\ncallers see the same answers ``orcho_run_status`` would give. The\nrest of the aggregate is computed over the ``(since_seq, limit)``\nwindow. ``next_actions`` are short imperative strings derived from\n``status`` only — conservative, no invention.\n\nThis is also the fallback half of the resilient observation loop:\n``orcho_run_watch`` → ``orcho_run_events_summary`` → ``orcho_run_watch``.\nWhen a bounded watch times out or the client transport drops the\nlong-poll, poll this summary with ``since_seq=next_seq`` and then\nresume watching from the returned ``next_seq``. A client-side watch\ndisconnect is observer loss, not run failure — the run keeps\nexecuting in its worktree, and run decisions are taken only from the\ntyped ``status`` / ``pending_handoff`` / terminal / evidence signals,\nnever from the fact that a watch call ended.",
            "properties": {
              "by_kind": {
                "additionalProperties": {
                  "type": "integer"
                },
                "description": "Total event count per kind across the window.",
                "title": "By Kind",
                "type": "object"
              },
              "by_phase": {
                "description": "Per-phase counts and kind vocabulary, in first-seen phase order.",
                "items": {
                  "$ref": "#/$defs/PhaseEventSummary"
                },
                "title": "By Phase",
                "type": "array"
              },
              "current_phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Latest ``phase.start`` seen anywhere up to ``next_seq`` and not yet closed by a ``phase.end``. Computed over the full event stream — not just the windowed slice — so polling stays accurate.",
                "title": "Current Phase"
              },
              "current_subtask": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/CurrentSubtaskRecord"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Live progress coordinate for the in-flight subtask_dag subtask (index/total/goal/state), or ``None`` when no subtask is currently running. Computed over the full event stream up to ``next_seq``; cleared when the implement phase ends."
              },
              "eof": {
                "description": "True when every event past ``since_seq`` fit in this batch.",
                "title": "Eof",
                "type": "boolean"
              },
              "last_n": {
                "description": "The most recent ``last_n`` events from the window, in seq order. Each entry is a ``CompactRunEvent`` — no raw payload spill.",
                "items": {
                  "$ref": "#/$defs/CompactRunEvent"
                },
                "title": "Last N",
                "type": "array"
              },
              "next_actions": {
                "description": "Conservative operator guidance derived from ``status`` only. Empty when status is unknown.",
                "items": {
                  "type": "string"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "next_seq": {
                "description": "Reconnect cursor: the seq to pass as the next ``since_seq``. Carry it back into ``orcho_run_watch`` (or another ``orcho_run_events_summary``) to resume observation exactly where this batch ended after a bounded watch times out or the client transport drops the long-poll.",
                "title": "Next Seq",
                "type": "integer"
              },
              "pending_handoff": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/PendingHandoffSummary"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Active phase-handoff state when the run is paused on ``awaiting_phase_handoff``; ``None`` otherwise. Lets a polling caller see the pause and its decision surface without firing an ``orcho_run_watch`` handoff trigger."
              },
              "provider_pressure": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/ProviderPressure"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Core-typed provider runtime/access failure for a terminal run, projected from the same ``project_provider_pressure`` source and shared ``build_provider_pressure_next_actions`` helper as ``orcho_run_status`` / ``orcho_run_diagnose`` / ``orcho_run_evidence`` — so every surface agrees on the condition and its conservative typed next_actions. ``None`` for a non-terminal run or a generic failure with no core-typed provider source. Additive: the legacy ``next_actions: list[str]`` field is unchanged."
              },
              "provider_session_fallbacks": {
                "description": "Recovered missing-provider-session fallbacks observed up to ``next_seq`` (phase / redacted stale session id / fallback mode / worktree preserved / phase succeeded). Each entry is a recovery notice, not a phase failure — ``phase_succeeded`` is True. Empty when none occurred.",
                "items": {
                  "$ref": "#/$defs/ProviderSessionFallback"
                },
                "title": "Provider Session Fallbacks",
                "type": "array"
              },
              "retry_state": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/RetryState"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Human-retry / repeated-reject lifecycle state when the run is in a reject / retry lifecycle (alongside ``pending_handoff``); ``None`` otherwise. Lets a client tell an automatic reject from a human retry, a repeated reject, and an accepted-and-closed retry."
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              },
              "status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Merged run status (meta.json + supervisor terminal fallback). Same value ``orcho_run_status`` returns.",
                "title": "Status"
              },
              "total_count": {
                "description": "Number of events in the considered window (after ``since_seq`` and ``limit``).",
                "title": "Total Count",
                "type": "integer"
              }
            },
            "required": [
              "run_id",
              "total_count",
              "next_seq",
              "eof"
            ],
            "title": "RunEventsSummary",
            "type": "object"
          },
          "WatchTrigger": {
            "description": "Why ``orcho_run_watch`` returned.\n\nA single record so callers (and prompts) can branch on ``kind`` without\nparsing free text. ``seq`` is the run's latest event sequence at return\ntime and doubles as the reconnect cursor when the caller passed\n``summary=False``.",
            "properties": {
              "kind": {
                "enum": [
                  "next_event",
                  "phase_change",
                  "subtask",
                  "handoff",
                  "terminal",
                  "timeout"
                ],
                "title": "Kind",
                "type": "string"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Phase"
              },
              "reason": {
                "description": "Short human-readable reason. Bounded; never quotes raw event payload text.",
                "title": "Reason",
                "type": "string"
              },
              "seq": {
                "description": "Run's ``next_seq`` at return time — the reconnect cursor. Use this as the next ``since_seq`` for reconnect when ``summary=False``; when ``summary=True`` it carries the same value as ``summary.next_seq``. On a ``timeout`` trigger, or when the client transport drops the long-poll, continue observing via ``orcho_run_events_summary(since_seq=seq)`` and then resume ``orcho_run_watch`` from the summary's ``next_seq`` — a disconnected watch loses the observer, not the run.",
                "title": "Seq",
                "type": "integer"
              },
              "status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Status"
              }
            },
            "required": [
              "kind",
              "reason",
              "seq"
            ],
            "title": "WatchTrigger",
            "type": "object"
          }
        },
        "description": "Result of one ``orcho_run_watch`` call.\n\nReconnect rule: the caller's next ``since_seq`` is\n``result.summary.next_seq`` when ``summary=True`` and\n``result.trigger.seq`` otherwise — both fields carry the same value\nat return time, so callers may always use ``trigger.seq`` as a uniform\nfallback. Raw event payloads are never returned.\n\nThe reconnect cursor anchors the resilient observation loop\n``orcho_run_watch`` → ``orcho_run_events_summary`` → ``orcho_run_watch``:\non a ``timeout`` trigger, or when the client transport drops the\nlong-poll, continue with\n``orcho_run_events_summary(since_seq=trigger.seq)`` and then resume\nwatching from that summary's ``next_seq``. A client-side disconnect of\nthe watch is observer loss, not a failed run — the run keeps executing\nand lifecycle decisions are read only from the typed ``status`` /\n``handoff`` / terminal / evidence signals, never from a watch call\nending early.",
        "properties": {
          "handoff": {
            "anyOf": [
              {
                "$ref": "#/$defs/HandoffDecisionHint"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Populated only when ``trigger.kind == \"handoff\"`` and the meta carries a usable ``phase_handoff`` block."
          },
          "run_id": {
            "title": "Run Id",
            "type": "string"
          },
          "summary": {
            "anyOf": [
              {
                "$ref": "#/$defs/RunEventsSummary"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Bounded ``RunEventsSummary`` snapshot at return time when the caller requested ``summary=True``; otherwise ``None``."
          },
          "trigger": {
            "$ref": "#/$defs/WatchTrigger"
          },
          "triggered": {
            "description": "True when a trigger condition fired before timeout. False only on the timeout path (``trigger.kind == \"timeout\"``).",
            "title": "Triggered",
            "type": "boolean"
          }
        },
        "required": [
          "run_id",
          "triggered",
          "trigger"
        ],
        "title": "RunWatchResult",
        "type": "object"
      }
    },
    {
      "description": "Discover Agent Skills packages visible to the project.\n\n    Walks the canonical multi-source chain (project / compat / workspace\n    / user / entry-point packages); returns a deterministic registry\n    snapshot. Trust gating defaults to the safe policy\n    (project + compat sources OFF until the user opts in via plugin\n    config); MCP introspection is read-only and per-call.\n\n    Returns an empty list when no source yields a SKILL.md — that's the\n    'flat developer-agent mode' fallback, not an error.\n    ",
      "inputSchema": {
        "properties": {
          "project_dir": {
            "title": "Project Dir",
            "type": "string"
          }
        },
        "required": [
          "project_dir"
        ],
        "title": "orcho_skills_listArguments",
        "type": "object"
      },
      "name": "orcho_skills_list",
      "outputSchema": {
        "$defs": {
          "SkillRecord": {
            "description": "Wire-shape for a discovered Agent Skills package.\n\nMirrors the portable :class:`pipeline.skills.SkillPackage` surface —\nskill metadata never carries runtime / model / provider fields, so\nconsumers cannot mistakenly treat a skill as a runtime selector.",
            "properties": {
              "checksum": {
                "default": "",
                "title": "Checksum",
                "type": "string"
              },
              "description": {
                "default": "",
                "title": "Description",
                "type": "string"
              },
              "name": {
                "title": "Name",
                "type": "string"
              },
              "root_dir": {
                "default": "",
                "title": "Root Dir",
                "type": "string"
              },
              "source": {
                "default": "unknown",
                "title": "Source",
                "type": "string"
              }
            },
            "required": [
              "name"
            ],
            "title": "SkillRecord",
            "type": "object"
          }
        },
        "properties": {
          "project_dir": {
            "title": "Project Dir",
            "type": "string"
          },
          "skills": {
            "items": {
              "$ref": "#/$defs/SkillRecord"
            },
            "title": "Skills",
            "type": "array"
          }
        },
        "required": [
          "project_dir",
          "skills"
        ],
        "title": "SkillsListResult",
        "type": "object"
      }
    },
    {
      "description": "Return the machine-readable catalogue of workflow recipes.\n\n    Recipes describe how a sequence of MCP tool calls fits together\n    (plan-then-implement, review a paused run, resume a crashed run,\n    inspect a finished run). The server does not execute recipes —\n    the client agent reads the steps and decides which tool to call\n    next. Same payload is also published as the ``orcho://workflows``\n    resource; this tool exists so tools-only clients (which cannot\n    read MCP resources) can still discover the catalogue.\n\n    The result is stable across calls: recipe order, step order, and\n    step ``id`` values do not shuffle between invocations.\n    ",
      "inputSchema": {
        "properties": {},
        "title": "orcho_workflows_listArguments",
        "type": "object"
      },
      "name": "orcho_workflows_list",
      "outputSchema": {
        "$defs": {
          "RecipeBranchStep": {
            "description": "A step that records a non-linear continuation.\n\n``when`` is a small ``key: value`` pattern the client agent\nmatches against a prior step's observed result (typically\n``status`` from ``orcho_run_status``). When every key matches,\nthe client jumps to the step named by ``next``. The server does\nnot evaluate ``when`` — recipes describe the path, the agent\nwalks it.",
            "properties": {
              "id": {
                "description": "Step identifier. Unique within the recipe.",
                "title": "Id",
                "type": "string"
              },
              "kind": {
                "const": "branch",
                "default": "branch",
                "title": "Kind",
                "type": "string"
              },
              "next": {
                "description": "Target step ``id`` to jump to when ``when`` matches. Must resolve to another step inside the same recipe.",
                "title": "Next",
                "type": "string"
              },
              "when": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Pattern the agent matches against the most recent observed payload before deciding to follow this branch. Empty dict means 'always take this branch'.",
                "title": "When",
                "type": "object"
              }
            },
            "required": [
              "id",
              "next"
            ],
            "title": "RecipeBranchStep",
            "type": "object"
          },
          "RecipeInput": {
            "description": "One declared input the recipe expects from the operator.\n\nInputs are referenced inside step args as ``${name}`` placeholders.\nThe catalogue is read-only; client agents resolve placeholders\nthemselves before issuing tool calls.",
            "properties": {
              "name": {
                "description": "Input identifier. Matched verbatim against ``${name}`` placeholders inside step args.",
                "title": "Name",
                "type": "string"
              },
              "required": {
                "description": "True when the recipe cannot proceed without an operator value. False when a sensible default exists at the calling tool boundary.",
                "title": "Required",
                "type": "boolean"
              }
            },
            "required": [
              "name",
              "required"
            ],
            "title": "RecipeInput",
            "type": "object"
          },
          "RecipeResourceStep": {
            "description": "A step that points the client agent at an MCP resource URI.\n\nResource steps are descriptive like tool steps: the server never\nsubscribes on behalf of the client. The agent decides whether its\nMCP client can subscribe; when it cannot, ``fallback_tool`` names the\ntool path that preserves the workflow.",
            "properties": {
              "fallback_tool": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Tool to use when the client cannot read or subscribe to resources.",
                "title": "Fallback Tool"
              },
              "id": {
                "description": "Step identifier. Unique within the recipe.",
                "title": "Id",
                "type": "string"
              },
              "kind": {
                "const": "resource",
                "default": "resource",
                "title": "Kind",
                "type": "string"
              },
              "purpose": {
                "description": "Short instruction for what the agent should learn from this resource and why it is preferred.",
                "title": "Purpose",
                "type": "string"
              },
              "subscribe": {
                "default": false,
                "description": "True when clients that support ``resources/subscribe`` should subscribe and refresh on ``notifications/resources/updated``.",
                "title": "Subscribe",
                "type": "boolean"
              },
              "uri": {
                "description": "MCP resource URI to read or subscribe to. ``{run_id}`` means the live run id returned by an earlier run-start/resume step.",
                "title": "Uri",
                "type": "string"
              }
            },
            "required": [
              "id",
              "uri",
              "purpose"
            ],
            "title": "RecipeResourceStep",
            "type": "object"
          },
          "RecipeToolStep": {
            "description": "A step that maps to one MCP tool invocation.\n\n``args`` values may carry ``${input_name}`` placeholders referring\nto the recipe's declared inputs. The server does not substitute\nthese — the client agent does, immediately before calling the\ntarget tool.",
            "properties": {
              "args": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Literal or ``${var}``-templated argument values. Values stay as strings on the wire so the recipe contract does not collide with per-tool input schemas; the client coerces to the target tool's parameter types.",
                "title": "Args",
                "type": "object"
              },
              "id": {
                "description": "Step identifier. Unique within the recipe; used as a ``branch.next`` target and as a stable handle for clients that render the recipe as a checklist.",
                "title": "Id",
                "type": "string"
              },
              "kind": {
                "const": "tool",
                "default": "tool",
                "title": "Kind",
                "type": "string"
              },
              "tool": {
                "description": "Name of the MCP tool to invoke (e.g. ``orcho_run_start``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "id",
              "tool"
            ],
            "title": "RecipeToolStep",
            "type": "object"
          },
          "WorkflowRecipe": {
            "description": "One named workflow recipe.\n\nRecipes are descriptive: every concrete tool call still goes\nthrough the operator's LLM agent. The recipe gives that agent\na deterministic skeleton (which tools, in which order, with\nwhich arguments) so different clients converge on the same\nmulti-step behaviour.",
            "properties": {
              "description": {
                "description": "Short human-readable summary. Clients SHOULD surface this when listing available recipes.",
                "title": "Description",
                "type": "string"
              },
              "format_version": {
                "default": 1,
                "description": "Schema revision for this individual recipe. Bumped when a recipe's own step shape changes in a way clients need to adapt to. Independent of the envelope ``format_version``.",
                "title": "Format Version",
                "type": "integer"
              },
              "inputs": {
                "description": "Declared inputs the recipe consumes. Steps may reference inputs as ``${name}`` placeholders inside their ``args``.",
                "items": {
                  "$ref": "#/$defs/RecipeInput"
                },
                "title": "Inputs",
                "type": "array"
              },
              "name": {
                "description": "Recipe identifier. Lower-snake-case; used by the agent to refer to the recipe in subsequent messages.",
                "title": "Name",
                "type": "string"
              },
              "steps": {
                "description": "Ordered step list. Step ``id`` values are unique within the recipe; ``branch.next`` always points at another step in this list.",
                "items": {
                  "discriminator": {
                    "mapping": {
                      "branch": "#/$defs/RecipeBranchStep",
                      "resource": "#/$defs/RecipeResourceStep",
                      "tool": "#/$defs/RecipeToolStep"
                    },
                    "propertyName": "kind"
                  },
                  "oneOf": [
                    {
                      "$ref": "#/$defs/RecipeToolStep"
                    },
                    {
                      "$ref": "#/$defs/RecipeBranchStep"
                    },
                    {
                      "$ref": "#/$defs/RecipeResourceStep"
                    }
                  ]
                },
                "title": "Steps",
                "type": "array"
              }
            },
            "required": [
              "name",
              "description"
            ],
            "title": "WorkflowRecipe",
            "type": "object"
          }
        },
        "description": "Envelope for the recipe catalogue.\n\nCarried both as the ``orcho_workflows_list`` tool result and as\nthe ``orcho://workflows`` resource body — the contract is the\nsame regardless of delivery channel.",
        "properties": {
          "format_version": {
            "default": 2,
            "description": "Catalogue-level schema revision. Bumped when the envelope or step-union shape changes in a way that is not backwards-compatible for clients.",
            "title": "Format Version",
            "type": "integer"
          },
          "recipes": {
            "description": "Recipe entries; order is stable across calls.",
            "items": {
              "$ref": "#/$defs/WorkflowRecipe"
            },
            "title": "Recipes",
            "type": "array"
          }
        },
        "title": "WorkflowRecipeList",
        "type": "object"
      }
    },
    {
      "description": "Return where orcho reads/writes runs and which projects appear in recent history.\n\n    Useful as an initial discovery call so the client knows whether other\n    tools that need ``project_dir`` will succeed.\n    ",
      "inputSchema": {
        "properties": {},
        "title": "orcho_workspace_infoArguments",
        "type": "object"
      },
      "name": "orcho_workspace_info",
      "outputSchema": {
        "description": "Where this orcho instance reads/writes runs.\n\n``workspace_dir`` is None when the server can't resolve a workspace —\ntypical when MCP started outside any orcho-tracked project. Tools that\nrequire workspace context surface ``WorkspaceNotResolvedError`` instead.",
        "properties": {
          "recent_projects": {
            "description": "Distinct project paths seen across the most recent runs.",
            "items": {
              "type": "string"
            },
            "title": "Recent Projects",
            "type": "array"
          },
          "runs_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Runs Dir"
          },
          "workspace_dir": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Workspace Dir"
          }
        },
        "required": [
          "workspace_dir",
          "runs_dir"
        ],
        "title": "WorkspaceInfo",
        "type": "object"
      }
    },
    {
      "description": "List the runs awaiting an operator phase-handoff decision.\n\n    The typed way for a captain to recover pending operator decisions\n    *without chat memory*: it scans the workspace runs directory for runs on\n    ``status=awaiting_phase_handoff`` and returns one bounded row per run.\n\n    **Default view = the operator's decision inbox.** By default this answers\n    \"what needs my decision *now*\", not \"what historical artifact is paused\n    somewhere\". It returns only ``actionable`` rows — runs whose recorded\n    ``meta.project`` path still exists *and* lives under the resolved\n    workspace scope. In the standard ``workspace-orchestrator`` layout this\n    scope includes both the orchestrator directory and its parent project\n    group, where sibling project repos live. Non-actionable paused runs are\n    hidden but never silently dropped: each is tallied in ``hidden_count`` and\n    its per-reason breakdown (``hidden_missing_project_count`` for runs with\n    no/missing project, ``hidden_temp_project_count`` for temp/scratch project\n    paths, ``hidden_out_of_workspace_count`` for projects outside the\n    workspace scope).\n\n    **Workspace-valid beats temp.** A run whose project exists under the\n    workspace scope is ``actionable`` even when that scope itself lives in a\n    temp/test directory (e.g. ``/private/var/folders`` or under a ``pytest-*``\n    path). The temp/scratch heuristic is only applied *after* that check, so a\n    legitimate workspace launched from a temporary directory is never\n    mis-hidden as a throwaway demo run.\n\n    **Counters are bounded and view-independent.** ``hidden_count`` and its\n    breakdown are computed by classification over the scan window only\n    (bounded by the same internal run-directory ceiling as ``scanned_count``),\n    so they are a window-local tally, not a global count of every\n    non-actionable run that could exist. They are computed identically whether\n    or not ``include_stale`` returns the hidden rows: flipping\n    ``include_stale`` changes which rows appear, never the counter values for\n    the same set of scanned runs. The breakdown always sums to\n    ``hidden_count``.\n\n    **Forensic escape hatch.** Pass ``include_stale=True`` to also return the\n    hidden rows — every paused run, not just the actionable inbox. In that\n    mode each returned row carries its real ``classification``\n    (``actionable`` | ``missing_project`` | ``temp_project`` |\n    ``out_of_workspace``) so you can see *why* a run would otherwise be\n    hidden. Default rows are always ``actionable``.\n\n    **Backward compatible.** Existing ``limit``-only callers keep working\n    unchanged: the signature still accepts ``limit`` and ``include_stale`` is\n    a new optional argument defaulting to ``False`` (the actionable inbox).\n\n    **Artifact projector, not advisory.** Every row is built from the run's\n    durable artifacts — ``meta.json`` (merged with supervisor truth) plus the\n    ``phase_handoff_decisions/`` directory — NOT from the advisory\n    ``mcp/state.json`` cache that ``orcho_workspace_state`` reads. A run shows\n    up here iff its on-disk state says it is paused.\n\n    Each row carries the compact handoff read-model (handoff_id / phase /\n    trigger / verdict / round_label / available_actions /\n    decision_artifact_exists / suggested_next_action) and a typed\n    ``next_actions`` with the exact next-step form, coherent with\n    ``orcho_run_diagnose`` and the resume pre-flight guard:\n\n    - no decision recorded yet ⇒ an ``operator_input_required``\n      ``orcho_phase_handoff_decide`` whose ``choices`` are the\n      ``available_actions``;\n    - a decision artifact already exists ⇒ a ready ``orcho_run_resume`` call\n      (the run stays paused; it only needs resume to continue).\n\n    Rows never contain raw findings, reviewer output, event payloads, or\n    logs. The result is bounded: the scan stops at an internal run-directory\n    ceiling and the visible rows are capped by ``limit`` (default 50);\n    ``truncated`` and ``scanned_count`` expose the bound so a cap is never\n    mistaken for \"nothing more pending\". A corrupt / unreadable single run is\n    skipped, not fatal.\n\n    Args:\n        limit: optional cap on the number of rows returned (default 50,\n            clamped to the scan ceiling). Newest runs are returned first.\n        include_stale: when ``False`` (default) return only the actionable\n            inbox and hide non-actionable rows (still counted in\n            ``hidden_*``); when ``True`` also return the hidden rows, each\n            stamped with its real ``classification``.\n\n    Errors:\n        - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE / $ORCHO_WORKTREE.\n    ",
      "inputSchema": {
        "properties": {
          "include_stale": {
            "default": false,
            "title": "Include Stale",
            "type": "boolean"
          },
          "limit": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Limit"
          }
        },
        "title": "orcho_workspace_pending_decisionsArguments",
        "type": "object"
      },
      "name": "orcho_workspace_pending_decisions",
      "outputSchema": {
        "$defs": {
          "NextActionRecord": {
            "description": "One suggested follow-up tool call surfaced in tool responses.\n\nWorkflow-decision responses carry these records so the LLM does not\nhave to remember workflow patterns from documentation; the suggested\nnext steps ride in the payload.\n\nMirrors :class:`sdk.actions.Action` on the orcho-core side. The base\npass-through (``intent``/``tool``/``args``/``optional``) stays\nstate-derived and drift-free; the ``kind`` family below is MCP-wire\nenrichment computed entirely on the MCP side from state the server\nalready has, so it requires no new data from the core and the\ndefaults reproduce the historical pass-through record exactly.\n\nCall-readiness invariant (checked via the typed ``kind`` field, never\nby parsing ``intent``):\n\n- ``kind='ready_call'`` ⇒ ``args`` already contains every required\n  parameter of the target tool's actual signature (e.g.\n  ``orcho_run_resume`` ⇒ ``args`` has ``run_id``;\n  ``orcho_phase_handoff_decide`` ⇒ ``args`` has ``action`` and, where\n  the chosen verb mandates it, ``feedback``). The record is safe to\n  forward verbatim.\n- ``kind='operator_input_required'`` ⇒ the final decision args MAY be\n  omitted, but ONLY when ``choices`` and/or ``input_schema`` are\n  present to tell the operator what input is still needed.\n\n``intent`` stays human-readable and is NOT a contractual signal.",
            "properties": {
              "args": {
                "additionalProperties": true,
                "description": "Pre-filled arguments matching the target tool's input schema. Values are safe to forward as-is. When a follow-up requires operator input, that input is not represented as a placeholder inside this dict.",
                "title": "Args",
                "type": "object"
              },
              "choices": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is a choice among a fixed set (e.g. the available phase-handoff decision verbs), the allowed values. ``None`` (default) for ready-to-forward calls.",
                "title": "Choices"
              },
              "context": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Structured, machine-readable recovery context for this action that is NOT a parameter of the target tool (so it must not ride in ``args``), yet a typed client needs to branch on it. The from_run_plan follow-up after a rejected final acceptance carries here the parent's retained ``diff_path`` and its ``project_dir`` / ``retained_worktree`` checkout context, plus the ``from_run_plan`` parent id. Read these typed keys — never the ``intent`` prose — for the diff/worktree pointers. ``None`` (default) when the action carries no extra context.",
                "title": "Context"
              },
              "input_schema": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "When operator input is free-form (e.g. required retry ``feedback``), a description of the expected input. ``None`` (default) for ready-to-forward calls.",
                "title": "Input Schema"
              },
              "intent": {
                "description": "One-sentence human-readable description of what this action would accomplish. Clients SHOULD surface this verbatim in their UI.",
                "title": "Intent",
                "type": "string"
              },
              "kind": {
                "default": "ready_call",
                "description": "Typed call-readiness signal. ``ready_call`` (default) means ``args`` already carries every required parameter of the target tool, so the record is safe to forward verbatim. ``operator_input_required`` means a final decision argument is intentionally omitted and the caller must collect operator input first, as described by ``choices`` and/or ``input_schema``. This typed field — not ``intent`` text — is the contractual readiness signal.",
                "enum": [
                  "ready_call",
                  "operator_input_required"
                ],
                "title": "Kind",
                "type": "string"
              },
              "optional": {
                "default": true,
                "description": "True (default) when this is one of several valid follow-ups. False when the workflow has a single deterministic next step (e.g. ``orcho_run_resume`` after ``continue`` / ``retry_feedback`` / ``continue_with_waiver`` decision).",
                "title": "Optional",
                "type": "boolean"
              },
              "requires_operator_input": {
                "default": false,
                "description": "True iff ``kind == 'operator_input_required'``: the caller must gather operator input before invoking the tool. False (default) for ready-to-forward calls.",
                "title": "Requires Operator Input",
                "type": "boolean"
              },
              "tool": {
                "description": "Name of the MCP tool the caller should invoke (e.g. ``orcho_run_start``, ``orcho_phase_handoff_decide``, ``orcho_run_resume``).",
                "title": "Tool",
                "type": "string"
              }
            },
            "required": [
              "intent",
              "tool"
            ],
            "title": "NextActionRecord",
            "type": "object"
          },
          "WorkspacePendingDecisionRow": {
            "description": "One run paused awaiting an operator phase-handoff decision.\n\nBuilt from the run's durable artifacts (``meta.json`` +\n``phase_handoff_decisions/``) via the shared ``project_pending_handoff``\nprojection — never from the advisory ``mcp/state.json`` cache. Every\nfield is already bounded: there are no raw findings, reviewer output,\nevent payloads, or logs on this row by design.\n\n``next_actions`` carries the typed, decision-coherent next step:\n\n- no decision recorded yet ⇒ a single ``operator_input_required``\n  ``orcho_phase_handoff_decide`` record whose ``choices`` are the\n  ``available_actions`` (the operator still has to pick a verb and,\n  where required, supply feedback);\n- a decision artifact already exists ⇒ a single ``ready_call``\n  ``orcho_run_resume`` record (the decision is recorded; the run only\n  needs resume to continue), matching the resume routing the diagnose /\n  live-status / summary surfaces already use.",
            "properties": {
              "available_actions": {
                "description": "Decision verbs the runtime published for this handoff (``continue`` / ``retry_feedback`` / ``halt`` / ``continue_with_waiver``), verbatim.",
                "items": {
                  "type": "string"
                },
                "title": "Available Actions",
                "type": "array"
              },
              "classification": {
                "default": "actionable",
                "description": "Durable classification of this row: ``actionable`` (project recorded, path exists, and lies under the resolved workspace scope; for a standard ``workspace-orchestrator`` layout this includes the parent project group), ``missing_project`` (no project recorded or its path no longer exists), ``temp_project`` (project path lives in a temp/scratch location), or ``out_of_workspace`` (project path exists but is outside the resolved workspace scope). Default rows are always ``actionable``; the forensic ``include_stale`` mode sets the real classification reason on every returned row.",
                "title": "Classification",
                "type": "string"
              },
              "decision_artifact_exists": {
                "default": false,
                "description": "``True`` when a phase-handoff decision artifact already exists for ``handoff_id`` — the run stays paused, so the next step is resume, not a second decide.",
                "title": "Decision Artifact Exists",
                "type": "boolean"
              },
              "handoff_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Active handoff id from ``meta.phase_handoff.id``; ``None`` when the paused run did not record one.",
                "title": "Handoff Id"
              },
              "next_actions": {
                "description": "Typed follow-up calls for this row — a ready ``orcho_run_resume`` when a decision is recorded, else an ``operator_input_required`` ``orcho_phase_handoff_decide`` carrying the available verbs in ``choices``.",
                "items": {
                  "$ref": "#/$defs/NextActionRecord"
                },
                "title": "Next Actions",
                "type": "array"
              },
              "phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase that issued the pending handoff, when known.",
                "title": "Phase"
              },
              "project": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "The run's project path from ``meta.project``; ``None`` when the run did not record one.",
                "title": "Project"
              },
              "round_label": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Coherent operator round label (e.g. ``validate_plan automatic round 1/1``); ``None`` when round counters are absent.",
                "title": "Round Label"
              },
              "run_id": {
                "description": "The paused run's id (its run-dir name).",
                "title": "Run Id",
                "type": "string"
              },
              "suggested_next_action": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "One-line pointer at the right next tool (decide-then-resume when no decision exists, resume when one already does).",
                "title": "Suggested Next Action"
              },
              "trigger": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Normalised pause trigger (e.g. ``rejected`` / ``incomplete``), when recorded.",
                "title": "Trigger"
              },
              "verdict": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Runtime machine verdict label (``REJECTED`` / ``APPROVED``) carried verbatim, when present.",
                "title": "Verdict"
              }
            },
            "required": [
              "run_id"
            ],
            "title": "WorkspacePendingDecisionRow",
            "type": "object"
          }
        },
        "description": "Returned by ``orcho_workspace_pending_decisions``.\n\nA bounded, artifact-built index of every run currently paused on\n``status=awaiting_phase_handoff`` across the workspace — the typed way\nfor a captain to recover pending operator decisions without chat\nmemory. Rows are derived from each run's ``meta.json`` /\n``phase_handoff_decisions/`` artifacts (NOT the advisory\n``mcp/state.json`` cache) and are capped; ``truncated`` / ``scanned_count``\nexpose the bound so a silent cap is never read as \"nothing more pending\".\n\nBy default only ``actionable`` rows are returned; non-actionable rows\n(missing project, temp/scratch project path, or a project outside the\nworkspace scope) are hidden but never silently dropped — they are tallied\nin ``hidden_count`` and its per-reason breakdown. The standard\n``workspace-orchestrator`` scope includes its parent project group so\nsibling project repos remain actionable. The breakdown always sums to\n``hidden_count``. These counters are computed by ``classification`` over\nthe scan window only (bounded by the same internal ceiling as\n``scanned_count``), so they are a window-local tally, not a full global\ncount of every non-actionable run that could exist. The counters are\ncomputed identically whether or not ``include_stale`` returned the hidden\nrows: turning ``include_stale`` on surfaces the same rows the counters\nalready accounted for, with their real ``classification`` set, and does\nnot change any counter value for the same set of scanned runs.",
        "properties": {
          "hidden_count": {
            "default": 0,
            "description": "Number of non-actionable rows within the scan window that the default view hides. Computed by ``classification`` and bounded by the scan ceiling, so it is a window-local tally rather than a full global count. Equals the sum of the three ``hidden_*_count`` fields below. Independent of ``include_stale``: the same value holds whether or not the hidden rows are returned. These rows are never silently discarded — they are counted here and, with ``include_stale``, also returned.",
            "title": "Hidden Count",
            "type": "integer"
          },
          "hidden_missing_project_count": {
            "default": 0,
            "description": "Hidden rows classified ``missing_project`` (no project recorded or its path no longer exists). Part of the ``hidden_count`` breakdown; window-bounded and independent of ``include_stale``.",
            "title": "Hidden Missing Project Count",
            "type": "integer"
          },
          "hidden_out_of_workspace_count": {
            "default": 0,
            "description": "Hidden rows classified ``out_of_workspace`` (project path exists but is outside the resolved workspace scope). Part of the ``hidden_count`` breakdown; window-bounded and independent of ``include_stale``.",
            "title": "Hidden Out Of Workspace Count",
            "type": "integer"
          },
          "hidden_temp_project_count": {
            "default": 0,
            "description": "Hidden rows classified ``temp_project`` (project path lives in a temp/scratch location). Part of the ``hidden_count`` breakdown; window-bounded and independent of ``include_stale``.",
            "title": "Hidden Temp Project Count",
            "type": "integer"
          },
          "returned_count": {
            "default": 0,
            "description": "Number of rows returned (``len(runs)``).",
            "title": "Returned Count",
            "type": "integer"
          },
          "runs": {
            "description": "Paused-run rows, newest run id first. Length is capped; see ``truncated``. By default only ``actionable`` rows appear; with ``include_stale`` the hidden non-actionable rows are appended too, each carrying its real ``classification``.",
            "items": {
              "$ref": "#/$defs/WorkspacePendingDecisionRow"
            },
            "title": "Runs",
            "type": "array"
          },
          "scanned_count": {
            "default": 0,
            "description": "Number of run directories examined during the scan (bounded by an internal ceiling).",
            "title": "Scanned Count",
            "type": "integer"
          },
          "truncated": {
            "default": false,
            "description": "``True`` when the result was capped — either the row limit was reached or the scan ceiling stopped enumeration before the runs directory was exhausted. More paused runs may exist beyond what is shown.",
            "title": "Truncated",
            "type": "boolean"
          }
        },
        "title": "WorkspacePendingDecisionsResult",
        "type": "object"
      }
    },
    {
      "description": "Return advisory MCP workspace state — last observed cursor per run.\n\n    Reads ``<ORCHO_WORKSPACE>/mcp/state.json`` (written by every\n    ``orcho_run_events_summary`` and ``orcho_run_watch`` call). Useful\n    for reconnect after a client/session restart: instead of replaying\n    from ``since_seq=0``, callers pick the run's ``last_seq`` and pass\n    it back as ``since_seq``.\n\n    **Advisory only.** The canonical truth is each run's ``events.jsonl``\n    + ``meta.json``. A missing or corrupt state file is recreated as an\n    empty envelope on the next observation, so this tool never fails on\n    that account.\n\n    The state file contains no raw event payloads, prompts, findings,\n    env, or credentials — it is deliberately a small reconnect index.\n\n    Errors:\n        - WorkspaceNotResolvedError — no $ORCHO_WORKSPACE / $ORCHO_WORKTREE.\n    ",
      "inputSchema": {
        "properties": {},
        "title": "orcho_workspace_stateArguments",
        "type": "object"
      },
      "name": "orcho_workspace_state",
      "outputSchema": {
        "$defs": {
          "WorkspaceRunStateRecord": {
            "description": "One run's last observed cursor / status / phase.\n\nAdvisory — these fields are populated from the same data that\n``orcho_run_status`` and ``orcho_run_events_summary`` return, and\nare updated by the MCP server only when a tool call walks the run.\nThe authoritative state remains in the run's ``events.jsonl`` +\n``meta.json`` artifacts; this record is a cache hint for reconnect.",
            "properties": {
              "last_phase": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Phase active at the last observation. ``None`` when no phase is open.",
                "title": "Last Phase"
              },
              "last_seq": {
                "description": "Highest ``next_seq`` observed by an MCP tool call. Monotonic — never moves backward.",
                "title": "Last Seq",
                "type": "integer"
              },
              "last_status": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Status seen at the last observation. May lag the authoritative meta + supervisor merge by one poll.",
                "title": "Last Status"
              },
              "last_summary_at": {
                "description": "UTC ISO 8601 timestamp of the most recent update to this record.",
                "title": "Last Summary At",
                "type": "string"
              },
              "run_id": {
                "title": "Run Id",
                "type": "string"
              }
            },
            "required": [
              "run_id",
              "last_seq",
              "last_summary_at"
            ],
            "title": "WorkspaceRunStateRecord",
            "type": "object"
          }
        },
        "description": "Advisory MCP workspace state envelope.\n\nThis is *not* source of truth — it records what the MCP layer has\nmost recently observed across all runs in the workspace. Missing or\ncorrupt state files surface as a fresh empty envelope so callers can\nalways rely on the shape.\n\nContains no raw event payloads, prompts, findings, env, or\ncredentials by design.",
        "properties": {
          "runs": {
            "additionalProperties": {
              "$ref": "#/$defs/WorkspaceRunStateRecord"
            },
            "description": "Per-run last-observed records keyed by ``run_id``.",
            "title": "Runs",
            "type": "object"
          },
          "server_started_at": {
            "description": "UTC ISO 8601 timestamp captured when *this* MCP server process started.",
            "title": "Server Started At",
            "type": "string"
          },
          "updated_at": {
            "description": "UTC ISO 8601 timestamp of the most recent write.",
            "title": "Updated At",
            "type": "string"
          },
          "version": {
            "description": "Schema version. Bump only on shape changes — readers tolerate unknown versions by returning an empty envelope.",
            "title": "Version",
            "type": "integer"
          },
          "workspace_dir": {
            "title": "Workspace Dir",
            "type": "string"
          }
        },
        "required": [
          "version",
          "workspace_dir",
          "server_started_at",
          "updated_at"
        ],
        "title": "WorkspaceMcpStateResult",
        "type": "object"
      }
    }
  ]
}
